# Agent Notes

## Repository

This is a Next.js App Router project for the ACF Sports site. Keep changes aligned with the existing component structure in `src/components` and route structure in `src/app`.

## Commands

- `npm run dev` starts the local development server.
- `npm run build` verifies the production build.
- `npm run lint` is present in `package.json`, but the repository may need ESLint configuration before it can run successfully.

## Asset Handling

Prefer committed local assets under `public` for production UI. Avoid introducing new direct dependencies on temporary `https://www.figma.com/api/mcp/asset/...` URLs unless the task is explicitly an intermediate Figma extraction step.

## Commit Requirements

When preparing commits, keep `README.md` and this `AGENTS.md` updated with concise notes that reflect the completed change.

## Recent Changes (Figma Site Alignment & Breakpoint Standardization)
- Refactored media queries and Tailwind CSS theme to standardize responsive breakpoints across the entire codebase: `576px` (celulares grandes), `768px` (tablets), `992px` (laptops), and `1200px` (desktops).
- Refactored competitions, contact, history, and player profile UI (`CompetitionsContent.tsx`, `ContactContent.tsx`, `HistoryContent.tsx`, `PlayerDetailContent.tsx`, `globals.css`) according to current Figma frames.
- Replaced temporary external Figma URLs with committed local assets under `public/history`, `public/squad`, `public/header`, and `public/contact`.
- Added interactive competition filtering, match detail modal behavior, sponsorship plan tabs, and contact form confirmation state.
- Migrated roster and player profile navigation links to Next.js `<Link>`.
- Refactored `HeroNews.tsx` carousel: added 12-second automatic slide transition, animated progress bar on active thumbnail, grayscale styling & hidden progress bar on inactive thumbnails, restored full screen container width (100%), and optimized height (`min(620px, calc(100vh - 120px))`) so the hero fits vertically on display viewports without requiring scrolling.
- Aligned sponsorship reasons section (`SponsorsPageContent.tsx`, `globals.css`) left alignment (`width: min(1160px, calc(100% - 40px))`) to line up precisely with the page heading title ("patrocinadores.").
- Updated the top header to rotate committed GIF assets from `public/gif` and adjusted player profile figure/frame sizing to use the shared local player image.
- Standardized internal code identifiers to English for page component names, menu state keys, roster helpers, sponsorship plan keys, and roster query parameter constants while preserving Portuguese UI copy and routes.
- Refined home page interactions for hero slide fade animation, main menu dropdown active states, news CTA placement, roster hover overlays, sponsor strip marquee behavior, and sponsorship plan card/button feedback.
- Validated production build (`npm run build`).

## CI/CD

GitHub Actions workflows in `.github/workflows/` deploy to Vercel via the Vercel CLI: `prev-deploy.yaml` runs on push to `stage` (preview environment), `prod-deploy.yaml` runs on push to `main` (production environment). Both require `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID`, and `VERCEL_TOKEN` secrets configured in the `Deploy` GitHub environment. `prev-deploy.yaml`'s `vercel build`/`vercel deploy` steps take no `--preview` flag — Vercel CLI 59.x rejects it (`unknown or unexpected option: --preview`); preview is simply the default target when `--prod` is omitted.

The `acf-site` Vercel project has no Git repository connected (created via CLI), so the Vercel dashboard cannot list branches for domain assignment. `prev-deploy.yaml` works around this by capturing the deployment URL from `vercel deploy --prebuilt` and running `vercel alias set <url> stage.acfsports.com.br` right after, giving `stage` a fixed preview domain without needing Git integration. `prod-deploy.yaml` doesn't need this step because `acfsports.com.br` is assigned to the Production target and Vercel auto-aliases it on every `--prod` deploy. The `stage.acfsports.com.br` DNS record (CNAME to `cname.vercel-dns.com`) must be created in Cloudflare for the alias to resolve.

The project's SSO deployment protection (`vercel project protection`) is set to `all_except_custom_domains`, which only exempts domains formally registered on the project (`vercel domains add`), not bare aliases (`vercel alias set`). `stage.acfsports.com.br` was registered as a project domain via `vercel domains add stage.acfsports.com.br acf-site` so it resolves publicly without a Vercel login prompt, same as `acfsports.com.br`.

## Recent Changes (Mobile Navigation & Brand Refresh)
- Added a mobile hamburger menu to `MainMenu.tsx`: a toggle button and full-width collapsible panel, with a row-style accordion (plus/close icon) replacing the hover dropdown for the "clube" submenu below `768px`, and WhatsApp/Instagram quick-action icons shown only on mobile.
- Updated the primary brand orange from `#f56345`/`#cc4529` to `#ff3203` in `globals.css`, `ContactContent.tsx`, and `PlayerMainCardImage.tsx`.
- Switched `globals.css` font-family declarations to use the CSS custom properties (`--font-montserrat`, `--font-outfit`, `--font-rubik`, `--font-roboto`, `--font-poppins`) exposed by `next/font/google` in `layout.tsx`, instead of hardcoded font names.
- Replaced hero carousel and related news images with new local assets (`public/carousel/image-01.jpg`, `public/carousel/image-02.jpg`), removing the dependency on the old `public/home-news` PNGs for those entries.
- Validated production build (`npm run build`).

## Recent Changes (Carousel Image Update)
- Replaced `public/carousel/image-01.jpg`/`image-02.jpg` with `image-01.png`/`image-02.png` and updated references in `HeroNews.tsx` and `src/data/news.ts`.
- Renamed the new PNG files from their originally saved capitalized names (`Image-01.png`/`Image-02.png`) to lowercase, since Vercel's Linux build environment is case-sensitive and would 404 on the mismatch even though it works locally on macOS.

## Recent Changes (Favicon Fix)
- Moved `favicon.ico` from `src/favicon.ico` (not picked up by Next.js at all) to `src/app/favicon.ico`, the App Router's special-file location for automatic favicon serving.
- If the icon still doesn't appear to a browser after this fix, it's very likely favicon-specific browser caching (Chrome/Firefox cache favicons separately from the normal HTTP cache and often ignore hard-refresh) — check `/favicon.ico` directly or in a private window before assuming a server/build issue.
- Shortened the page `<title>` metadata in `layout.tsx` to "ACF Sports | Site Oficial".

## Recent Changes (News: Neon Postgres Integration)
- `src/data/news.ts` was rewritten from a static mock array to a data-access layer querying the `news` table in Neon Postgres (`@neondatabase/serverless`, lazy client in `src/db/index.ts`). This is the same database the `acf-site-manager` dashboard project writes to — `acf-site` only has read access via its own `DATABASE_URL` env var (copied from `acf-site-manager`'s Vercel env; not the same Vercel project).
- `NewsItem` fields now mirror the real DB columns (`id`, `tag`, `title`, `subtitle`, `content`, `author`, `image`, `createdAt`) instead of the old mock shape (`slug`, `category`, `description`, `caption`, `body[]`, `quote`, layered/mascot image variants). `content` is CMS-authored rich HTML rendered with `dangerouslySetInnerHTML` in `NewsDetail.tsx` — this is trusted content from the internal dashboard editor, not public user input.
- `getAllNews()` is wrapped in React's `cache()` (dedupes repeated calls within one request/render) and catches DB errors, returning `[]` so a DB outage degrades to an empty section instead of a 500.
- Renamed `src/app/noticias/[slug]` → `src/app/noticias/[id]` (no slug column in the DB; the route param is the row's UUID). Removed `generateStaticParams` — data now comes from an externally-edited DB, so pages use ISR (`export const revalidate = 60`) instead of being enumerated at build time.
- `HeroNews.tsx` lost its hardcoded `slides` array; it's still a client component (owns the auto-rotate timer/state) but now takes `news: NewsItem[]` as a prop. The fetch (`getFeaturedNews(3)`) happens server-side in `src/app/page.tsx`, which became `async`. `NewsGrid.tsx` and `NewsArchive.tsx` became `async` server components that fetch their own data (`getLatestNews(6)` and `getAllNews()` respectively).
- Validated with `npm run build` and by hitting the dev server (`/`, `/noticias`, `/noticias/[id]`, and a nonexistent id → 404) to confirm real rows render, including the CMS's HTML `content`.

## Recent Changes (Home Carousel: Highlight Filter & Pinned News)
- `NewsItem`/`NewsRow` in `src/data/news.ts` gained a `highlight: boolean` field mapping the `news` table's `highlight` column.
- Added `PINNED_CAROUSEL_NEWS_ID` (`cd288794-2014-4f37-8163-cb5082cd0b47`) as a shared constant in `src/data/news.ts`.
- `HeroNews.tsx` now builds its slide list itself: only news with `highlight === true` are shown, and the pinned news ID is always forced into the list at position 2 (index 1) regardless of its `highlight` value. `src/app/page.tsx` now passes it the full `getAllNews()` result instead of a pre-sliced `getFeaturedNews(3)` (that helper was removed as unused).
- `getLatestNews()` (used by the home `NewsGrid`) now always excludes the pinned news ID, so that item only ever appears in the hero carousel, never in the home "notícias" grid. `NewsArchive.tsx` (`/noticias` full listing) is unaffected — it still uses `getAllNews()` directly.
- Validated with `npm run build` and by querying the live Neon `news` table directly to confirm the pinned row lands at carousel position 2 and is absent from the home grid output.

## Recent Changes (Home Carousel: Pinned News Moved to Position 3)
- Moved the pinned carousel insertion point in `HeroNews.tsx` from index 1 (position 2) to index 2 (position 3): `slides.splice(Math.min(2, slides.length), 0, pinned)`.

## Recent Changes (Home News Grid: Fixed Layout for Fewer Than 3 Items)
- `.components-news-grid-grid` used a hardcoded `grid-template-columns: repeat(3, 330px)` (and `repeat(2, 330px)` at the 1200px breakpoint), which reserved layout space for a full row even when fewer news items were available (e.g. once the pinned carousel item is excluded from the grid, only 2 of 6 items remain) — the grid.
- Fixed by having `NewsGrid.tsx` compute `gridColumns = Math.min(newsItems.length, 3)` and pass it as a `--news-grid-columns` CSS custom property on `.components-news-grid-grid`. `globals.css` now reads `repeat(var(--news-grid-columns, 3), 330px)` at desktop and `repeat(min(var(--news-grid-columns, 2), 2), 330px)` at the 1200px breakpoint, so the grid always sizes to the actual item count instead of assuming 3.
- Validated with `npm run build`/`tsc --noEmit` and by checking the rendered home page HTML (`curl localhost:3000/`) to confirm `style="--news-grid-columns:2"` is emitted when only 2 news items are available.

## Recent Changes (Home "jogos" Panel: Wired to the `games` Table)
- Added `src/data/games.ts`, a data-access layer for the `games` table (`id`, `competition_id` → joined against `competitions.title`, `opponent`, `result`, `date`, `location`), following the same `cache()`-wrapped, error-swallowing pattern as `src/data/news.ts`. A game is treated as **finished** when `result` is non-empty and not `"-"` (the sentinel the dashboard's games form defaults upcoming games to), and **upcoming** otherwise.
- `getLatestFinishedGame()` returns the finished game with the latest `date`; `getNextUpcomingGame()` returns the upcoming game with the earliest `date`. Both return `null` when no matching row exists.
- `GamesPanel.tsx` became an async server component that fetches both via `Promise.all` and conditionally renders each "partida finalizada" / "próxima partida" block only when its game exists (both previously hardcoded, static example matches). Neither team has a stored logo for the opponent side in the DB, so both sides currently reuse the club's own crest icon (`/header/symbol.png`) as a generic placeholder, matching what the prior hardcoded markup already did.
- Verified against the live (currently empty) `games` table that the query runs without error and both blocks render as empty (`<div class="components-games-panel-blocks"></div>`), and validated the finished/upcoming selection + sort logic against mocked rows.

## Recent Changes (Home "jogos" Panel: Opponent Logo)
- The `games` table has no logo column of its own. Opponent crests instead live inside the linked `competitions.table` JSONB array (each entry has `team` and an optional `symbol` URL, e.g. from `COPA SESC FUTSAL ADULTO`'s standings table), so `getAllGames()` now also selects `c."table" AS competition_table` and `mapRow()` looks up `opponentLogo` by matching `team === opponent` (exact, trimmed).
- `GamesPanel.tsx` uses `game.opponentLogo` when present, falling back to the club's own crest icon (`/header/symbol.png`) when the opponent has no `symbol` in the competition table (e.g. "Oficina das Máquinas" currently has none) or when the game has no linked competition at all.
- Verified against the live `games` table (now with 2 real upcoming rows) that the nearest upcoming game ("Cyber Futsal / Arena Cassimiro", 2026-09-03) renders its real crest URL from Vercel Blob storage instead of the fallback.

## Recent Changes (Home "jogos" Panel: Text Overflow on Long Team Names)
- Long opponent/club names (e.g. "Cyber Futsal / Arena Cassimiro") could overflow `.components-games-panel-team`'s fixed 95px width, because the span had `white-space: nowrap` with no truncation and the team block is a flex item whose default `min-width: auto` lets its min-content (the full unbroken text) win over the explicit width.
- Fixed in `globals.css`: added `min-width: 0` to `.components-games-panel-team` (lets it actually shrink to 95px) and `display: block; width: 100%; overflow: hidden; text-overflow: ellipsis;` to `.components-games-panel-team span`, for both "partida finalizada" and "próxima partida" cards (shared styles).
- `GamesPanel.tsx`'s `MatchCard` now also sets a `title` attribute on each team name span with the full untruncated name, so it's still available on hover/inspection.

## Recent Changes (Home "jogos" Panel: Correct Club Name)
- `GamesPanel.tsx` had "ACF Sport Club" hardcoded as the home team's name — a leftover from the original static mock, mismatched with the club's actual name everywhere else in the codebase (`ACF Sports/Vila Mercado`, e.g. `CLUB_NAME` in `src/data/news.ts` and the `team` entries inside `competitions.table`).
- Added a matching `CLUB_NAME = "ACF Sports/Vila Mercado"` constant in `GamesPanel.tsx` and used it for both match cards' home team name.

## Recent Changes (Home "jogos" Panel: Text-Overflow on the Date/Competition Badge)
- `.components-games-panel-matchDate` (the orange/white date badge, e.g. "02/09/2026 - COPA SESC FUTSAL ADULTO / Ginásio Pedro Mariucci") had `white-space: nowrap` with no truncation; long competition/location strings just got hard-clipped by the parent card's `overflow: hidden`, cutting text off mid-word with no ellipsis.
- Added `overflow: hidden; text-overflow: ellipsis;` to `.components-games-panel-matchDate` in `globals.css`.
- `GamesPanel.tsx`'s `MatchCard` now also sets a `title` attribute on the date badge with the full untruncated string, same pattern already used for the team name spans.

## Recent Changes (Home "jogos" Panel: Location Moved Inside the Card)
- `formatGameDate()` (`src/data/games.ts`) no longer folds `location` into the top badge string — just date + competition now. Added `formatGameTime()`, extracting only the `HH:mm` portion of `game.date`, used on the competitions page.
- `MatchCard` in `GamesPanel.tsx` gained a `location` prop and now renders the game's venue inside the card, centered, below the team/score row (previously it was crammed into the top badge).
- `globals.css`: `.components-games-panel-matchBody` is now a flex-column container (the previous row layout for teams/score was extracted into `.components-games-panel-matchTeams`); added `.components-games-panel-matchLocation` for the venue text; fixed card heights (`.components-games-panel-matchCard` / `.components-games-panel-upcomingCard`) grew from 116px/136px to 140px/160px to fit the extra line.

## Recent Changes (News Page: Pinned Article Excluded from the Full Archive)
- `NewsArchive.tsx` (`/noticias`) now filters out `PINNED_CAROUSEL_NEWS_ID`, mirroring what `getLatestNews()` already did for the home grid — the pinned news item only ever shows in the home hero carousel, never alongside the rest in the archive.

## Recent Changes (News Page: Conditional Pagination)
- The pagination nav (page numbers + arrows) in `NewsArchive.tsx` only renders when there are more than 6 news items (after excluding the pinned one); page numbers are now generated dynamically (`Math.ceil(newsItems.length / 6)`) instead of the hardcoded `[1, 2, 3, 4, 5, 6]` array.

## Recent Changes (Competitions Page: "Próxima Partida" Card Wired to the Database)
- `src/app/clube/competicoes/page.tsx` became an async Server Component that fetches `getNextUpcomingGame()` (same source the home "jogos" panel uses) and passes the formatted data down as a `nextGame` prop to `CompetitionsContent`; added `export const revalidate = 60`, matching the home page.
- `CompetitionsContent.tsx` no longer uses the static `nextMatchData` mock — it builds the card and detail modal from the `nextGame` prop (teams, competition, date, time, location), and shows "Nenhuma partida agendada no momento." when there's no upcoming game in the database. The "Partidas Anteriores" list below is unchanged and still mock data (out of scope for this change).

## Recent Changes (Footer: Contact & Social Links Wired to `team_info`)
- Added `src/data/teamInfo.ts`, following the same `cache()` + error-fallback pattern as `games.ts`/`news.ts`, querying `facebook`, `instagram`, `youtube`, `phone`, `email`, `address`, and `symbol` from the single-row `team_info` table.
- `SiteFooter.tsx` became an async server component using `getTeamInfo()` to populate the social links, phone, email, and address instead of the previous hardcoded values. Empty fields in the DB (e.g. `email`) fall back to the prior static defaults.

## Recent Changes (Contact Page: Wired to `team_info`)
- `src/app/contato/page.tsx` became an async Server Component (`export const revalidate = 60`) that fetches `getTeamInfo()` and passes it down as a `teamInfo` prop to `ContactContent`.
- `ContactContent.tsx` (client component) now takes `teamInfo: TeamInfo` and builds `contactItems` (phone, email, address) and the social links (Instagram/Facebook/YouTube) from it instead of hardcoded values. The "Atendimento" (hours) field stays static — `team_info` has no matching column. The WhatsApp link is now derived from `teamInfo.phone` via a new `toWhatsappNumber()` helper: strips to digits only and prefixes `55` when the number is 11 digits (i.e. missing the country code).

## Recent Changes (Home Carousel: Pinned News Renders Image-Only, No Text Overlay)
- When the active carousel slide is the pinned news item (`PINNED_CAROUSEL_NEWS_ID`, used as a banner/ad slot — e.g. the "Patrocinadores" news row), `HeroNews.tsx` no longer renders the `storyCard` (category/title/summary text overlaid on the image), leaving just the background image visible. Behavior for all other carousel slides is unchanged.

## Recent Changes (Home Carousel: Pinned News Always in 4th Position)
- `buildCarouselSlides()` in `HeroNews.tsx` inserted the pinned news item (`PINNED_CAROUSEL_NEWS_ID`) at the 3rd slot (`Math.min(2, slides.length)`); changed to `Math.min(3, slides.length)` so it always lands in the 4th slot of the carousel when there are enough slides.

## Recent Changes (Home "tabelas" Panel: Wired to the `competitions` Table)
- Added `src/data/competitions.ts` (same `cache()` + error-fallback pattern as `games.ts`/`teamInfo.ts`), exposing `getHomeCompetitions()`, which queries `competitions` for rows where `home_page = true` only, ordered by `updated_at` desc; the jsonb `table` column is normalized and sorted by position.
- `StandingsPanel.tsx` became an async Server Component that fetches `getHomeCompetitions()` and hands the result to a new client component, `StandingsPanelClient.tsx`, which renders the real standings table (position, club, points, games played, goal difference, wins, losses) and shows "Nenhuma tabela disponível no momento." when no competition is flagged for the home page. When more than one competition has `home_page = true`, the previously decorative `ChevronDown` button now opens a working dropdown to switch between them — same visual pattern as the dropdown in `CompetitionsContent.tsx`.
- `globals.css`: `.components-standings-panel-tableCard` switched to `position: relative; overflow: visible` (was `overflow: hidden`) so the dropdown isn't clipped; added `.components-standings-panel-dropdownMenu`, `.components-standings-panel-dropdownItem`, and `.components-standings-panel-empty`.

## Recent Changes (Home "tabelas" Panel: Broken Table Layout with Real Club Names)
- With real data (e.g. "Cyber Futsal/Arena Cassimiro"), the table broke: default `table-layout: auto` sizes columns from content, and the club-name column (`white-space: nowrap`, no truncation) forced the numeric columns (Pts/Jgs/Sgs/Vit/De) to shrink to near-zero and their text to overlap — including in the header row.
- Fixed in `globals.css`: `.components-standings-panel-table` now uses `table-layout: fixed`, with `.components-standings-panel-clubColumn` pinned at `40%` and the remaining columns at `12%` each (sums to 100%); `.components-standings-panel-clubColumn` gained `overflow: hidden; text-overflow: ellipsis` so long names truncate instead of blowing out the cell.
- The card's fixed height (`316px`, which left empty space for competitions with few teams) became auto height; the table now lives inside a new `.components-standings-panel-tableScroll` wrapper (`max-height: 280px; overflow-y: auto`) with a `position: sticky` header, so competitions with many teams (e.g. 8) scroll internally instead of stretching the card.
- `StandingsPanelClient.tsx` now wraps the `<table>` in that scroll container and adds `title={entry.team}` on the club cell so the full name is available on hover when truncated.

## Recent Changes (Competitions Page: Standings Tables Wired to the Database)
- Added `getAllCompetitions()` to `src/data/competitions.ts`, fetching every row from `competitions` ordered by `home_page DESC, updated_at DESC` — the competition flagged `home_page = true` always lands first in the array (and is therefore pre-selected in the table).
- `src/app/clube/competicoes/page.tsx` now fetches `getAllCompetitions()` alongside `getNextUpcomingGame()` and passes the result down as a `competitions` prop to `CompetitionsContent`.
- `CompetitionsContent.tsx` no longer uses the static `competitionOptions`/`standingsData` mocks — the competition dropdown and standings table are built from the `competitions` prop (same `HomeCompetition`/`StandingEntry` shape used by the home panel); the dropdown is disabled when there's only one competition, and shows "Nenhuma tabela disponível no momento." when the database returns none.

## Recent Changes (Competitions Page: "Partidas Anteriores" Wired to the Database)
- Added `getPreviousGames()` to `src/data/games.ts`, reusing the same `isFinished` filter already used by `getLatestFinishedGame()` (a game counts as finished when `result` is neither empty nor `"-"`), returning every finished game sorted most-recent-first (instead of just the latest one).
- `src/app/clube/competicoes/page.tsx` now also fetches `getPreviousGames()` and builds a `previousMatches` prop (type `PreviousMatchData`, exported from `CompetitionsContent.tsx`) using the same formatting already used for `nextGame` (`formatGameDate`/`formatGameTime`, opponent logo via `competition.table`).
- `CompetitionsContent.tsx` dropped the static `previousMatchesData` mock — the "Partidas Anteriores" list is built from the `previousMatches` prop and only renders when at least one finished game exists in the database; otherwise it shows "Nenhuma partida anterior registrada no momento." (same pattern as the other sections). Since the `games` table has no structured per-side score (just the freeform `result` column, e.g. used as "3 x 1" in the home "jogos" panel), `MatchDetail`'s `homeTeam.score`/`awayTeam.score` fields (fixed numbers from the mock) were replaced by a single `result: string` field, rendered as-is from the database in both the card and the detail modal. The mock's `highlights`/`referee` fields (no matching database column) were removed, along with the "Observações da Partida" modal block that displayed them.

## Recent Changes (Home Carousel: Redesigned Slide Copy to Match Figma Reference)
- `HeroNews.tsx`: regular slides now render only the news title (category/summary paragraphs removed), left-aligned at the bottom of the stage in a width-capped box (`max-width: min(640px, calc(100% - 480px))` so it never collides with the thumbnail strip), over a new `.components-hero-news-scrim` bottom gradient for legibility on any background image.
- The pinned slide (`PINNED_CAROUSEL_NEWS_ID`, the sponsors banner) now renders its own database title (e.g. "seja um de nossos patrocinadores.") stacked above a new `.components-hero-news-ctaButton` ("Ver Planos ↗") linking to `/clube/patrocinadores`, styled with the site's existing orange gradient (`linear-gradient(165deg, #ff3203 0%, #8f301d 99%)`) and a pill radius.
- Fixed an earlier regression where the title used `font-size: 4rem` (64px) with centered text spanning nearly the full stage width, which visually overwhelmed the background image; title is now 32px/38px (left-aligned), scaled down further at the `768px`/`576px` breakpoints.

## Recent Changes (News Article Body: Inline Links Now Styled and Get a Hover State)
- Added `.components-news-detail-content a` in `globals.css`: inline links embedded in the rich-text `news.content` HTML (rendered via `dangerouslySetInnerHTML` in `NewsDetail.tsx`) now render in the brand orange (`var(--laranja)`, `#ff3203`) with an underline, and darken to `#8f301d` on hover with a `color` transition. Previously these links had no distinct color or hover feedback, inheriting the surrounding paragraph's default text styling.

## Recent Changes (Home "planos" Section: Corrected Colors Against the Figma Design System)
- Audited `PlansSection.tsx`/`globals.css` against a Figma-exported design system for the "planos" frame; most colors and typography already matched exactly. Fixed two confirmed mismatches: the featured ("master") card's headline and price color was `#7a210f`, corrected to `#661400` (the design system's `cor do acf sport/L-18` token); the featured card's CTA button gradient was using the sitewide `#ff3203 → #8f301d` gradient, corrected to the design system's specific `#ff7a5c → #8f1d00` gradient for this component.

## Recent Changes (Batch of Site-Wide Fixes from `improvements.md`)
- **Related news / pagination**: `getRelatedNews()` (`src/data/news.ts`) now excludes `PINNED_CAROUSEL_NEWS_ID` in addition to the current article, so the sponsors banner never shows up in "Mais notícias sobre o ACF". `NewsDetail.tsx` paginates that related list 4-per-page and only renders the pagination nav when there are more than 4 eligible items.
- **Pagination made functional**: both `NewsDetail.tsx`'s related-news pagination and `NewsArchive.tsx`'s (`/noticias`) pagination were previously decorative (static page numbers, all links pointing to `/noticias` with page 1 hardcoded active). Both now read a `page` prop (sourced from the route's `searchParams`), slice their item list accordingly, generate real `?page=N` links, and hide the next/last-page arrows once on the last page. `/noticias` and `/noticias/[id]` are consequently dynamic routes now (they depend on `searchParams`).
- **Header logo wired to `team_info`**: added `src/components/SiteHeader.tsx`, an async server component that calls `getTeamInfo()` and renders `MainMenu` with a new `logoSrc` prop (`MainMenu.tsx`, defaults to the existing static asset). Replaced `<MainMenu />` with `<SiteHeader />` (and the corresponding import) across all 9 page files that render the header.
- **Sponsors strip hidden behind a flag**: added `SPONSORS_STRIP_ENABLED = false` inside `SponsorsStrip.tsx`, which now returns `null` when disabled — hides the sponsor-logo marquee band everywhere it's rendered without deleting its markup/logic. The dedicated `/clube/patrocinadores` page is unaffected.
- **Roster ("elenco") section spacing fix**: `.components-roster-section-section` had a fixed `min-height: 1048px` (also duplicated as an inline style in `RosterSection.tsx`) that left a large empty gap below the "ver elenco completo" button whenever actual content was shorter than that height. Removed the fixed `min-height` (both the CSS rule and its `768px` breakpoint override, and the inline style prop) so the section sizes to its content; adjusted `.components-roster-section-inner`'s bottom padding to `40px` desktop / `32px` mobile, matching the design system's `lg` spacing token.
- **YouTube section gradient fix**: `.components-youtube-section-button` ("inscreva-se no canal do youtube") used the sitewide `#ff3203 → #8f301d` gradient; corrected to `#ff7a5c → #8f1d00`, the exact gradient documented for this button in its Figma-exported design system (typography, other colors, and the card's drop shadow already matched precisely).

## Recent Changes (Asset Updates)
- Replaced `public/footer/acf-footer-logo.png`, `public/footer/sovereign-footer.png`, and `public/youtube-section/youtube-bull.png` with updated exports (no code changes).

## Recent Changes (News: Fixed Timezone Bug in Displayed Dates)
- `formatNewsDate()` (`src/data/news.ts`) formatted dates with `Intl.DateTimeFormat("pt-BR")` without an explicit `timeZone`, so the rendered date depended on the server runtime's default timezone. In production (UTC runtime), news created late at night in Brasília time rendered with the next day's date (UTC), not matching what's actually stored in the database. Fixed by passing `timeZone: "America/Sao_Paulo"` explicitly to both `Intl.DateTimeFormat` calls.

## Recent Changes (History Page: Wired to the `team_history` Table)
- Added `src/data/teamHistory.ts` (`getTeamHistory()`), following the same `cache()` + error-fallback pattern as `teamInfo.ts`: fetches the most recent row from `team_history` (`title`, `content`, `symbol`, `mascot`, `content_image`, `mascot_images`, `created_at`). Only `title`/`content`/`contentImage` (the page's core article) get a static fallback; `symbol`/`mascot`/`mascotImages` pass through raw (empty when the DB has no data).
- `src/app/clube/historia/page.tsx` became an async Server Component (`export const revalidate = 60`) that fetches `getTeamHistory()` and passes it down as a `history` prop. `HistoryContent.tsx` no longer has hardcoded copy/images — `content`/`mascot`/`symbol` are CMS-authored HTML rendered via `dangerouslySetInnerHTML` (same trust model as `news.content` in `NewsDetail.tsx`).
- The "símbolos" and "mascote" sections are now conditionally rendered: "símbolos" only shows when `history.symbol` is non-empty; "mascote" only shows when `history.mascot` is non-empty or `history.mascotImages` has at least one entry. No placeholder content is shown when the CMS hasn't filled these fields yet.
- `.components-history-content-articleText` had its `text-indent` removed and typography aligned with the news article body (`.components-news-detail-body`: `24px`/`40px` line-height, `#4e4e4e`, left-aligned) per user request to match the news page's text styling.

## Recent Changes (Competitions Page: Removed Standings Legend)
- Removed the promoted/relegated legend block ("Classificados para próxima fase" / "Rebaixados para a segunda divisão") below the standings table in `CompetitionsContent.tsx`, per user request.

## Recent Changes (Club Logo Wired to `team_info` in Match Cards)
- `TeamBadge` (`CompetitionsContent.tsx`) gained a `home` prop, applying a visual highlight (dark circular background) only to the home team's crest. `CompetitionsContent` and `GamesPanel.tsx` now receive/fetch `teamInfo.symbol` (via `getTeamInfo()`) and use it as the ACF crest in the "Próxima Partida"/"Partidas Anteriores" cards and the home "jogos" panel, falling back to `/header/symbol.png` when the database field is empty.

## Recent Changes (Games: Separate Time Field)
- `src/data/games.ts` now also selects the `time` column from the `games` table (`GameItem.time`, `string | null`), separate from `date`. Sorting by most-recent/soonest date (`gameDateTime`) now compares `"YYYY-MM-DD HH:MM"` strings directly instead of converting to `Date`/timestamp, avoiding timezone ambiguity in the comparison.

## Recent Changes (Sponsors Page: Redesigned Plan Cards)
- `SponsorsPageContent.tsx` gained `SHOW_PARTNERS`/`SHOW_ONE_OFFS` flags (both `false`) to hide the "Parceiros" and "Pontuais" logo groups without deleting their data.
- `PlanCard` now splits the price into amount and period (`plan.price.split(" / ")`), rendered in separate spans (`.components-sponsors-page-content-planPriceAmount`/`...PricePeriod`), and gained a divider (`.components-sponsors-page-content-planDivider`) below the title. The WhatsApp CTA button dropped its Tailwind utility classes in favor of dedicated `globals.css` styles, with the icon absolutely positioned inside the pill button.

## Recent Changes (Responsive Fixes: Header Submenu, Home Mobile, Large Screens)
- **Mobile "Clube" submenu rendering blank**: the desktop-only `:hover`/`:focus-within` dropdown rule (`.components-main-menu-clubMenu:hover .components-main-menu-dropdown` and its `dropdownOpen` counterpart) has higher specificity than the mobile `.components-main-menu-dropdown` override and kept applying `transform: translateX(-50%)` even inside the `768px` breakpoint, shifting the open panel half its own width off-screen — the submenu links were still there in the DOM, just rendered outside the visible viewport, which looked like empty rows. Fixed by setting `transform: none` on both mobile hover/focus-within variants in `globals.css`. Confirmed root cause and fix using headless Chrome (Puppeteer + system Chrome via `puppeteer-core`, no download) simulating a real touch tap (mouseenter immediately followed by click, matching mobile browsers' synthetic hover-then-click event order), since the Claude in Chrome extension wasn't connected this session.
- `TopCf.tsx`: removed the auto-rotating timer between the two header GIFs (`headerGifs`); now always renders `headerGifs[0]`. Also dropped the now-unnecessary `"use client"` directive and `useState`/`useEffect` since the component is static.
- **Home mobile "jogos" section**: `.components-standings-panel-panel` (the "tabelas" standings block) now gets `display: none` inside the existing `768px` breakpoint, per request to hide standings on mobile game listings.
- **Home mobile "planos" section**: `.components-plans-section-cards` becomes a horizontal scroll-snap carousel (`overflow-x: auto; scroll-snap-type: x mandatory`, one card per `flex: 0 0 100%` slide with `margin-right` instead of `gap` — needed because percentage `flex-basis` doesn't itself subtract `gap`, which was causing snap points to land mid-card) only inside the `768px` breakpoint; desktop/tablet layout is untouched.
- **Home mobile "elenco" section**: `.components-roster-section-athletes` switched from a horizontally-scrolling single row (fixed 210px cards inside a 330px container — only one full card plus a sliver of the next ever fit) to a 2-column CSS grid inside the `768px` breakpoint, using `aspect-ratio: 256 / 448` on each card so the custom SVG shield shape doesn't distort at the new width.
- **Home mobile "YouTube" section**: `.components-youtube-section-section` has a fixed `height` (`774px` desktop / `760px` at `992px`) with `overflow: hidden`; below `768px` the absolutely-positioned `.components-youtube-section-card` content (heading + CTA block with an unreduced `margin-top: 80px`) grew taller than that fixed height, silently clipping the bottom "Rede ACF Soberano" block. Added a `768px` override: `height: auto` on the section, `.components-youtube-section-card` switched from `position: absolute` to `position: relative` (in normal flow, so the section's auto height actually accounts for it) with `margin-top: 32px` on the CTA block instead of `80px`.
- **Footer mobile**: added explicit `components-site-footer-address` / `components-site-footer-addressDivider` classes in `SiteFooter.tsx` (previously untargetable, both `<hr>`s and the address `<p>` had no distinguishing class) and hid them, plus `.components-site-footer-column:first-child` ("Informações" nav column), via `display: none` inside the `768px` breakpoint. Phone/email in the same "Contato" section stay visible.
- **Large-screen layout (e.g. 2560×842) and cross-resolution alignment**: `.components-main-menu-menu` used `padding-inline: clamp(24px, 13.75vw, 220px)`, a viewport-relative formula with no relationship to the `width: min(1440px, calc(100% - 40px))` container convention used by every other section (`NewsGrid`, `PlansSection`, `RosterSection`, etc.) — on ultra-wide screens the header stretched edge-to-edge while body content stayed capped at 1440px, and even at "normal" desktop widths the two didn't line up exactly. Replaced with `max(24px, calc((100% - 1440px) / 2))`, which resolves to the same left/right edges as the shared 1440px container above ~1490px viewport width, and falls back to a flat `24px` gutter below that (matching prior behavior in the already-fine 1200–1470px range). Applied the identical formula to `.components-hero-news-storyCard` (left/right) and `.components-hero-news-thumbnails` (right) in the home hero carousel, which had a flat `32px` inset from the viewport edge instead of following the 1440px container — both only affect viewports above the existing `992px` hero breakpoint, which already has its own fixed mobile/tablet overrides.
- Validated with `npm run build` (all 51 routes generate cleanly) and headless Chrome screenshots at 390px and 2560px widths. Full page-by-page alignment audit (competitions, roster, history, sponsors, contact) beyond the home page was out of scope for this pass.

## Recent Changes (Second Mobile Polish Batch, from `improvements.md`)
- **Home "elenco" is now a real carousel**: `RosterSection.tsx` groups the 5 athletes into slides of 2 (client component, `useRef`/`useState`), using `scroll-snap-type: x mandatory` inside the `768px` breakpoint with clickable dot navigation (`.components-roster-section-dots`). On desktop/tablet the slide wrapper becomes `display: contents` so the original single-row layout (5 cards) is unaffected.
- **Home "ver mais notícias" button centered and bottom-aligned with the sidebar**: `.components-news-grid-action` swapped `justify-content: flex-end` + a fixed `padding-right: 116px` for `justify-content: center`; `.app-page-contentRow` switched to `align-items: stretch` and `.components-news-grid-section` became `display: flex; flex-direction: column` with `margin-top: auto` on the button, so its bottom edge lines up with the bottom of the games+standings sidebar column. The home "tabelas." card (`.components-standings-panel-tableCard`) gained inner padding (`padding: 0 12px`) so its content no longer touches the card border.
- **Competitions page table had no border**: `.components-competitions-content-tableBlock` (previously unstyled) now gets a border, rounded corners, and gray background, matching the home "tabelas." widget.
- **Competitions page had zero mobile responsiveness**: there was no `768px` breakpoint at all for `CompetitionsContent`; added a full mobile block centering "Próxima Partida" and the "Partidas Anteriores" cards (which broke with fixed pixel-width grid columns) and keeping the standings table horizontally scrollable inside its new bordered box.
- **Footer mobile had excessive top spacing**: `.components-site-footer-columns` used `padding-top: calc(64px + var(--main-menu-height))` (`160px` total) inside the `768px` breakpoint, leaving a large gap before "MÍDIAS SOCIAIS"; reduced to a flat `40px` (the fixed-header scroll compensation is already handled separately via `scroll-margin-top` and doesn't need duplicating in visual padding).
- **Mobile font sizes adjusted per visual reference**: hero title (`.components-hero-news-title`) and "notícias." (`.components-news-grid-title`) to `32px`; YouTube card headings (`.components-youtube-section-intro h2`/`.components-youtube-section-ctaBlock h3`) to `24px` and their paragraphs to `16px`; footer "Soberano TV"/phone/email (`.components-site-footer-acfTv`/`...menu a`/`...contact p`) to `16px`.
- **Home "jogos" panel on mobile**: team crests (`.components-games-panel-team img`) and team name text (`.components-games-panel-team span`) enlarged (`48px→56/64px`, font `14px`); match card heights (`.components-games-panel-matchCard`/`...upcomingCard`) bumped so the location text no longer risks clipping.
- **Toned down the club-name highlight in news articles**: `.components-news-detail-clubName` swapped bold pure orange (`--laranja`, `#ff3203`) for a softer, non-bold `#e0673f`.
- **Sponsors page plan cards became a mobile carousel**: `.components-sponsors-page-content-planGrid` switches to `grid-auto-flow: column` with scroll-snap only inside the `768px` breakpoint (previously stacked vertically); desktop/tablet unchanged.
- **Contact page: form moved to the end on mobile**: `ContactContent.tsx` restructured — the form block moved out of the left column to become a direct sibling of `.components-contact-content-planPanel`; `.components-contact-content-inner` now uses `grid-template-areas` (`"left plan" "form plan"` on desktop, stacked `"left" "plan" "form"` on mobile) to reorder visually without depending on DOM order.
- **Contact page plan cards became a mobile carousel**: added `.components-contact-content-planCarousel` (all 3 plans, scroll-snap) shown only below `768px`; the existing tabs + single-card view (`.components-contact-content-planTabs`/`...planCardDesktop`) remain the behavior on larger screens.
- **Fixed a horizontal-overflow bug on Contact/Competitions mobile**: grid items (`.components-contact-content-leftColumn`/`...planPanel`/`...formBlock`, `.components-competitions-content-previous`/`...tableBlock`) were missing `min-width: 0`; the address card's text (unable to wrap below the grid item's automatic content-based minimum size) forced the whole column wider than the viewport on mobile, causing horizontal scroll. Fixed by setting `min-width: 0` explicitly on those grid items.
- Validated with `npm run build` and real visual testing via Claude in Chrome at `390px`/`500px` widths (elenco, competitions, contact, sponsors, news article, footer).

## Recent Changes (Third Mobile Polish Batch, from `improvements.md`)
- **Home carousel title too large on mobile**: `.components-hero-news-title` reduced from `32px` to `20px` at the `992px`/`576px` breakpoints, per feedback after seeing the previous batch on a real device.
- **Wrong-colored icon on the "Ver Planos" CTA**: `HeroNews.tsx` used a bare Unicode `↗` character (`<span>↗</span>`), which iOS renders with the colored emoji font instead of inheriting the button's text color. Replaced with the `ArrowUpRight` icon from `lucide-react`, matching every other CTA button on the site.
- **"inscreva-se no canal do youtube" button wrapping ugly on mobile**: `.components-youtube-section-button` had a fixed `height: 56px` with no extra padding; the long label wrapped to two lines and overflowed that fixed height, leaving the icon visually disconnected. Added `height: auto; min-height: 52px` with adjusted padding and a smaller `14px` font inside the `768px` breakpoint.
- **Header had excessive internal padding on mobile**: `.components-main-menu-menu` at the `768px` breakpoint went from `padding-inline: 20px` (inheriting `16px` top/bottom from the desktop rule) to an explicit `padding: 8px 20px`; the logo shrank from `52×68px` to `44×58px`.
- **Home "jogos" panel "ver mais" link realigned**: the link lived inside `<header>` next to the "jogos." title; moved out to a sibling rendered after `.components-games-panel-blocks` (`GamesPanel.tsx`) — desktop keeps it absolutely positioned top-right (unchanged look), while mobile (`768px`) makes it static and right-aligned at the bottom of the panel. Color stayed on `var(--cinza-medio)` (`#959595`), which was already the value in use.
- **Competitions page: title and broken team-badge row**: page title changed from "tabelas & jogos" to "competições" in `src/app/clube/competicoes/page.tsx`. `.components-competitions-content-matchTeams` used `flex-wrap: wrap` on mobile, which broke the team badge and the "X" separator onto separate lines whenever a team name was long enough to overflow. Replaced with `flex-wrap: nowrap`, capped `.components-competitions-content-team` (`max-width: 120px`, `flex: 1 1 0`), and added `text-overflow: ellipsis` on the team name so everything stays on one row.
- **Consolidated conflicting competitions-page CSS**: a second, older `@media` block (missed in the previous pass) redefined `.matchTeams`/`.previousCard` further down in `globals.css` and — being later in file order — silently overrode part of the newer mobile block (and kept `.cardSeparator`/`.previousMeta` hidden via a tablet-only `≤1200px` rule that also matched mobile). Removed the duplicate `768px` block and scoped the hide rule to `769px–1200px` (tablet only) so it no longer leaks into mobile.
- **Contact page had too much top spacing on mobile**: `.app-contato-page-heading` min-height reduced from `144px` to `110px`; `.components-contact-content-section` top padding reduced from `56px` to `32px`; the gap between the title block and the info card (`.components-contact-content-leftColumn`) reduced from `56px` to `28px`.
- **Duplicate outer card around the Contact page's plan carousel**: `.components-contact-content-planPanel` had its own white background/border/shadow on mobile, wrapping carousel cards that already carry the same card treatment individually — a "card inside a card." Removed the panel's background/border/shadow at the `768px` breakpoint, keeping just the heading and the carousel.
- Validated with `npm run build` and real visual testing via Claude in Chrome at `420px`/`500px` widths (home, competitions, contact).

## Recent Changes (Asset Update: Header GIF)
- Swapped `public/gif/acf-na-veia.gif` for `public/gif/cf_gif.gif` as the first entry in `headerGifs` (`TopCf.tsx`); `public/gif/25-anos.gif` updated with a newer export. Removed `public/gif/header.gif` (not referenced by any component).

## Recent Changes (Desktop Fixes, from `improvements.md`)
- **Sponsors plan cards had mismatched heights**: `.components-sponsors-page-content-planGrid` used `align-items: start`, so the MASTER card (shorter copy) rendered shorter than PONTUAL/MENSAL and their price/button rows didn't line up. Changed to `align-items: stretch`.
- **Contact page "Nossos Planos" panel misaligned with the info card**: the "CONTATE-NOS" title block was nested inside the left column, so the plan panel (spanning both grid rows) aligned its top with the title instead of the info card below it. Moved the title out of the left column into its own grid row (`grid-template-areas` with a "title" row on top) in `ContactContent.tsx`/`globals.css`.
- **Competitions page standings table overflowed its card's right edge**: `.components-competitions-content-tableBlock` was missing `min-width: 0`; since the inner `<table>` has a non-shrinkable `min-width: 500px`, the card grew past its grid column. Fixed with `min-width: 0` — the classic CSS Grid/Flexbox "intrinsic min-width:auto" overflow trap.
- **Page content width not matching the header/menu width**: the page heading bands ("CLUBE / competições.", "SOCIAL / contato.", etc. — 8 pages) and the Competitions page content used `width: min(1160px, ...)`, narrower than the menu's effective ~1440px. Changed to `min(1440px, ...)`, matching the convention already used on the home page. Scope intentionally limited to these bands + Competitions (per explicit user decision); other sections with fixed-column grids (footer, roster cards) were left untouched to avoid introducing empty gutters.
- **Home hero title wrapping to 3 lines on desktop**: added `-webkit-line-clamp: 2` to `.components-hero-news-title` at `min-width: 993px`, leaving mobile untouched.
- **Excessive top spacing in the footer**: `.components-site-footer-columns` had `padding-top: calc(120px + var(--main-menu-height))` (~216px) by mistake, double-compensating for the fixed header height (already handled via `scroll-margin-top`). Reduced to a flat `120px`.
- **WhatsApp CTA button didn't match Figma at all**: removed the `MessageCircle` icon, changed `border-radius` from a full pill shape to `10px`, and constrained the label to one line — applied to both the Sponsors and Contact plan-card buttons.
- **Root cause of the button text rendering gray instead of white — a real CSS cascade bug, not just wrong values**: `globals.css` declared `a { color: inherit; text-decoration: none; }` *before* `@layer components { ... }` (which wraps virtually the entire rest of the file). Per the CSS Cascade Layers spec, unlayered rules always beat layered rules regardless of specificity, so that generic reset was silently overriding every `.foo a { color: ... }` rule site-wide. Fixed by moving the `a`/`button` resets to be the first rules inside `@layer components`, restoring normal specificity-based cascading.
- Validated with `npm run build` and visual testing via Claude in Chrome at `1440px` (all affected pages) and a `390px` mobile regression pass.

## Recent Changes (Home/Patrocinadores Plan Card Hover & Contact Spacing, from `improvements.md`)
- **Patrocinadores plan cards got the home "planos" hover lift**: initial reading of the feedback had it backwards (unified heights + killed the hover jump on Home instead). Correct intent: Home's `.components-plans-section-card` keeps its original mismatched heights (`440px` vs. `516px` for the MASTER `...featuredCard`) and its `translateY(-10px)` hover lift unchanged; `.components-sponsors-page-content-planCard` (Patrocinadores) gained the same `transform: translateY(-10px)` + shadow transition on `:hover` so its cards now move like Home's.
- **Contact page had too much space above "CONTATE-NOS"**: `.components-contact-content-section` top padding reduced from `64px` to `32px` (desktop; the `768px` breakpoint was already `32px`).
- **Contact page "Nossos Planos" panel reverted to top-aligned**: a first pass stretched `.components-contact-content-planPanel` (`display: flex; justify-content: center; align-self: stretch;`) to fill the same height as the left column, closing the empty grid gap below the card — but centering the card next to the much taller form looked worse than the original gap, per feedback. Reverted the panel to its original content-sized box.
- Validated with `npm run build` and visual testing via Claude in Chrome at `1440px` on Home (planos hover), Patrocinadores (hover), and Contact.

## Recent Changes (Patrocinadores Plan Cards: Featured MASTER Sizing, from `improvements.md`)
- **Patrocinadores MASTER card enlarged relative to PONTUAL/MENSAL, matching Home's proportions**: `SponsorsPageContent.tsx`'s `master` plan gained a `featured: true` flag, applied as `.components-sponsors-page-content-featuredPlanCard` on its card.
- **First attempt (`0.86fr 1fr 0.86fr` columns, `align-items: stretch`→`center`, `min-height: 566px`/`666px`) rendered backwards**: PONTUAL/MENSAL's benefit text is long/verbose (unlike Home's short phrases), so narrowing those columns made their text wrap onto more lines, and their *content-driven* height (up to `764px`, measured) ended up taller than the MASTER card's `666px` floor — the opposite of the intent. Grid `align-items: center`/`stretch` only sizes items *within* a row that's already tall enough; it can't shrink a card below what its own text needs.
- **Fix**: `.components-sponsors-page-content-planGrid` now uses fixed `grid-template-columns: 340px 400px 340px` (identical absolute widths to Home's card/featured-card, `justify-content: center` added since fixed columns no longer auto-fill the row) instead of `fr` fractions. To keep PONTUAL/MENSAL's *actual* rendered height reliably below MASTER's, benefit-list typography was tightened (`.components-sponsors-page-content-planCard li` `20px/28px` → `18px/26px`; `ul` `gap` `32px` → `24px`) — measured this drops the side cards' natural content height from ~708–764px to ~628px at `340px` width. `.components-sponsors-page-content-featuredPlanCard` `min-height` raised from `666px` to `720px` so it's unambiguously taller (720 vs. 628, ~15%) as well as wider (400 vs. 340, ~18%) than the side cards, mirroring Home's ratio. Verified via computed `getBoundingClientRect()` measurements in the browser, not just visually.
- Desktop-only: the existing `≤1200px` breakpoint already stacks the grid to one column and resets `min-height`/`width`/`padding` on `.planCard` at equal specificity later in the cascade (and the `768px` breakpoint already had its own smaller `li` font-size), so mobile/tablet are unaffected.

## Recent Changes (Figma Desktop Parity: News Card Height Overflow)
- **Root cause of the designer-reported "desktop layout doesn't match Figma" complaint (news section)**: measured against real Figma Design-panel data (no Dev Mode MCP available), almost all spacing/width values in `.app-page-contentRow`/`.components-news-grid-*` already matched the design exactly (1440px content width, 330px cards, 40px/80px gaps, 290px sidebar, etc.). The actual defect was that `.components-news-grid-copy h3`/`p` had no line clamp, so real news titles (longer than Figma's placeholder copy) wrapped to 3 lines instead of the design's fixed 2-line card (356px), growing cards to 402px and breaking grid-row alignment. Added `-webkit-line-clamp: 2` (+ `display: -webkit-box`, `-webkit-box-orient: vertical`, `overflow: hidden`) to both. Same missing-clamp defect found and fixed in `.components-news-archive-copy h2`/`p` (`/noticias` page, same card pattern). Spot-checked Patrocinadores plan cards and Elenco athlete cards — both already handle variable-length content correctly (min-height/flex-grow or existing clamp), no change needed there.
- **Separate, unrelated bug hit repeatedly while investigating**: the Next.js dev server intermittently serves `_next/static/css/app/layout.css` as a 404 after several `globals.css` edits (`.next/static/css/app/` ends up empty while the real hashed CSS sits one directory up), rendering the page with zero styles applied — easy to mistake for a real layout bug. Fix is `pkill -f "next dev" && rm -rf .next && npm run dev`. Documented as Step 0 of the new `.claude/skills/figma-desktop-parity/SKILL.md` skill, along with the full Figma-comparison workflow (no Dev Mode required — Design-panel screenshots + Alt-hover distance screenshots from the user), so this diagnosis doesn't have to be re-derived from scratch each session.
- Still unverified against Figma: `components-standings-panel-tableCard`, `components-youtube-section-card`, `components-sponsors-strip-logoCard`, `components-contact-content-*Card`, `components-history-content-symbolCard`.
- Validated with `npm run build` and live `getBoundingClientRect()` measurements via Claude in Chrome at the 1600px reference width (Home, `/noticias`, Patrocinadores, Elenco).

## Recent Changes (Fixed-Width Rows Overflowing Between 1200px–1480px Viewports)
- **Root cause, reported against a real 1470px-wide MacBook screen**: several "desktop" rows/grids use a fixed pixel total that exactly equals the site's max content width (1440px), with no responsive step between the `1200px` breakpoint and that max. `min(1440px, calc(100% - 40px))` containers degenerate to a flat `20px` side margin below `1480px` viewport width, so any child row whose fixed track/item widths sum to exactly `1440px` (a design tuned only for the widest case) overflows its own shrunk container in the `1200–1480px` range — invisible via `overflow: hidden` on the section, with content silently clipped/bleeding to the very edge of the viewport. Audited every `min(1440px, ...)`/`min(1160px, ...)` container and every fixed-`Npx` `grid-template-columns`/`flex` row in `globals.css`; most (page-heading bands, Competitions' `1fr`-based grid, Patrocinadores/Home plans, `/noticias` archive grid) already fit cleanly because either they use a flexible track or their fixed total lines up with an existing breakpoint. Two did not:
  - `.components-roster-section-athlete` (Home "elenco" carousel, 5 × `256px` + 4 × `40px` gap = `1440px` exactly): switched from fixed `width: 256px; height: 448px` to `width: min(256px, calc((100% - 160px) / 5)); aspect-ratio: 256 / 448`. Safe because the card is an inline `<svg viewBox="0 0 256 448">` — its internal `<path>`/`<image>` scale proportionally with the element's rendered size, so shrinking it introduces no distortion. The existing `≤1200px` breakpoint (horizontally scrollable tier) already resets both `width`/`height` back to fixed `256/448`, so it's unaffected.
  - `.components-news-grid-grid` (Home "notícias", 3 × `330px` + 2 × `40px` gap = `1070px`, needed inside a column whose max is itself derived from the same `1440px` container): `grid-template-columns` changed from `repeat(var(--news-grid-columns, 3), 330px)` to `repeat(var(--news-grid-columns, 3), min(330px, calc((100% - (var(--news-grid-columns, 3) - 1) * 40px) / var(--news-grid-columns, 3))))`, and `.components-news-grid-card`/`.components-news-grid-cardImage` (+ the unused `layeredImage`/`mascotImage` variants) changed from `width: 330px` to `width: 100%` to fill the now-fluid track.
  - `.components-roster-roster-page-content-grid` (`/clube/elenco` full listing, 5 × `256px` + 4 × `40px` = `1440px`): **not** shrunk like the above two — its card (`.components-roster-athlete-card-card`) uses a CSS `clip-path: path(...)` with hardcoded absolute-pixel coordinates on a plain HTML element, which does not scale when the box is resized (unlike an SVG `viewBox`), so shrinking it would visibly distort the hexagon notch shape. Fixed via breakpoints instead: added a `@media (max-width: 1480px)` tier dropping to `repeat(4, 256px)` (`4 × 256 + 3 × 40 = 1144px`, safe down to ~`1184px` viewport). This also fixed a **pre-existing, unrelated dead-CSS bug**: two separate `@media (max-width: 1200px)` blocks both redefined this grid's `grid-template-columns` (`repeat(4, 256px)` then, immediately after in source order, `repeat(3, 256px)`) — same specificity, same condition, so the first was always silently overridden and never actually rendered. Removed the now-redundant duplicate.
  - `.components-main-menu-menu` header padding's floor (`max(24px, ...)`) didn't match the content containers' floor (`min(1440px, calc(100% - 40px))` degenerates to a flat `20px`), so below `1480px` the header sat 4px further in than every section below it. Changed the header's floor from `24px` to `20px` so both formulas produce the exact same inset at every viewport width, not just above `1480px`.
- Validated with `npm run build` and live `getBoundingClientRect()`/`scrollWidth` vs `clientWidth` overflow checks via Claude in Chrome across `1200px`, `1250px`, `1300px`, `1470px` (the reported MacBook width), `1480px`, and `1600px` on Home and `/clube/elenco`.

## Recent Changes (Header Realigned to the 1160px Container, Confirmed by the Designer)
- **The previous fix in this doc's entry above unified the header to the wrong container**: it matched `.components-main-menu-menu`'s padding to the `1440px`-based container used by Home's "notícias"/"elenco" sections. The designer reviewed a screenshot of the Home "planos." section (`.components-plans-section-inner`, `width: min(1160px, calc(100% - 40px))`, title flush with its left edge, no extra margin) and confirmed: **the header should align with that narrower 1160px container specifically — the wider 1440px content sections (notícias, elenco, etc.) are correct as they are and were not to be changed.**
- `.components-main-menu-menu` padding changed from `16px max(20px, calc((100% - 1440px) / 2))` to `16px max(20px, calc((100% - 1160px) / 2))`. This intentionally makes the header narrower than the 1440px-based sections below it (e.g. at 1600px viewport: header inset `220px` vs. notícias/elenco content inset `80px`) — that asymmetry is the confirmed correct look, not a bug to reconcile further.
- Verified live: `.components-main-menu-logoLink` and `.components-plans-section-inner` now share the exact same `left`/`right` `getBoundingClientRect()` values at every tested viewport width.

## Recent Changes (Footer Realigned to Match the Header, Same 1160px Container)
- Designer follow-up: the footer should close the Home page on the same left/right edges as the (now `1160px`-aligned) header. `.components-site-footer-columns` previously used a fixed `max-width: 1600px; padding: 120px 220px 0` — a rigid `220px` side padding that only coincidentally matched the header at exactly `1600px` viewport and, below that, drifted out of alignment (and, since its 3 fixed-`px` columns + gaps also total exactly `1160px`, was silently overflow-clipped by the section's `overflow: hidden` in the `1200–1600px` range — same bug class as the earlier fixed-width-row fixes, caught as a side effect of this change).
- Changed to `width: min(1160px, calc(100% - 40px)); margin: 0 auto; padding-top: 120px;` — the same formula family as the header/`.components-plans-section-inner`. `.components-site-footer-backTop`'s `right` offset changed from `calc(max(0px, (100% - 1600px) / 2) + 220px)` to `max(20px, calc((100% - 1160px) / 2))` to match. The existing `≤1200px` (2-column fluid grid) and `≤768px` (single column, explicit `width` override) breakpoints were untouched and remain correct.
- Verified live: header logo/last-link and footer columns/back-to-top share identical `left`/`right` `getBoundingClientRect()` values at `1470px` and `1600px`; no `scrollWidth`/`clientWidth` overflow on the footer grid.

## Recent Changes (Page-Heading Bands and Competitions Content Realigned to 1160px; Contact Already Correct)
- Follow-up QA from the user found two more misalignments against the now-`1160px`-aligned header: every page's heading band ("CLUBE / competições.", "SOCIAL / contato.", etc. — all 8: Competitions, Elenco listing, Elenco player detail, História, Patrocinadores, Contato, Notícias listing, Notícias detail) still used `width: min(1440px, calc(100% - 40px))` for its inner `<div>`, and Competitions' page content (`.components-competitions-content-inner`) did too. Both changed to `min(1160px, calc(100% - 40px))` to match the header.
- **Contato needed no change** — `.components-contact-content-inner` already used `min(1160px, calc(100% - 40px))` before this session touched anything; it only *looked* misaligned earlier because the header itself was still targeting `1440px` at the time. Once the header moved to `1160px` (see the entry above), Contato lined up automatically.
- Before narrowing Competitions' container, audited its internal fixed-width layout for the same overflow risk as the earlier fixed-row bugs: `.components-competitions-content-contentGrid` is `560px 1fr` (first column rigid, second flexible) and its standings table has a `min-width: 500px` inner `<table>` (already patched against overflowing its own column in an earlier session, per the entry above titled "Desktop Fixes, from `improvements.md`"). `560 + 80px gap + 500 = 1140px`, which fits inside the new `1160px` container with only `20px` to spare — tight but confirmed safe empirically (`scrollWidth`/`clientWidth` checked with no overflow) at `1600px`, `1200px`, and `850px`.
- Validated with `npm run build` and live `getBoundingClientRect()`/overflow checks via Claude in Chrome on `/clube/competicoes` and `/contato`.

## Mobile Regression Check (After All the Above Desktop-Parity Changes)
- User asked to confirm none of the desktop-parity work above broke mobile. Checked `scrollWidth`/`clientWidth` overflow at ~500px viewport (the narrowest this Chrome-automation setup could resize to — true `375px` device widths weren't reachable, but `500px` still exercises the `≤768px` mobile CSS tier fully) across every page/section touched today: Home (header, elenco carousel, news grid, footer), `/clube/elenco` (listing grid), `/clube/competicoes`, `/contato`, `/clube/historia`, `/clube/patrocinadores`, `/noticias`, `/noticias/[id]`, `/clube/elenco/[slug]`. No `document.body` horizontal overflow on any page, and no new overflow traceable to today's edits — every `@media (max-width: 768px)` override touched by today's work already fully replaces the desktop-only values this session changed (`width`, `padding`, `grid-template-columns` are all explicitly re-set at `≤768px`, so the desktop `1160px`/`min()`/`aspect-ratio` formulas never apply there).
- **Two pre-existing issues found, confirmed unrelated to today's work (verified via `git diff`/`git show HEAD` — neither touched by any commit this session)**, worth a follow-up pass:
  1. `.components-site-footer-columns` at `≤768px`: computed `grid-template-columns` resolves to `400px` instead of the CSS's own `1fr`, causing ~70px of horizontal overflow *inside* the footer (contained by the section's `overflow: hidden`, so it doesn't create a page scrollbar, but the mobile single-column footer isn't actually the width it's supposed to be). Investigated at length: ruled out CSS Cascade Layers/specificity (all three candidate rules — base, `≤1200px`, `≤768px` — are confirmed via CSSOM inspection to sit in the same `@layer components`, in the correct source order, and the `≤768px` one does win in an *isolated* test element with the identical class, even nested inside the real footer element); ruled out the classic CSS Grid intrinsic-min-content-size trap (measured every grid child's own min-content width — max is `190px`, nowhere near `400px`); ruled out zoom/transform/iframe on the real page. The discrepancy only reproduces on the *actual* rendered footer instance, not a synthetic one — root cause not found within a reasonable time budget.
  2. `/clube/patrocinadores` page heading at `≤768px`: the title "patrocinadores." (the longest single word among all 8 page titles) overflows its own heading band by `~15px` (`345px` rendered vs. `330px` container) at the mobile `42px` font-size — likely just this specific word being long enough to not quite fit; the other 7 page titles fit fine at the same width/font-size.

## Recent Changes (Mobile Header Hamburger/Instagram Icon Inset, Following the Desktop Precedent)
- User QA: at `≤768px`, the header's hamburger toggle and the mobile `WhatsApp`/`Instagram` icon row sat almost flush against the viewport edges (`padding: 8px 20px`), while every content section on mobile centers itself with `width: min(330px, calc(100% - 32px))` — for any viewport wider than `362px` (`330 + 32`), that content formula's actual side inset is `(100% - 330px) / 2`, which is *larger* than the header's fixed `20px`, so the header visibly sat closer to the edges than the content below it. Same principle as the desktop header/`1160px`-container alignment done earlier this session, just for the mobile tier.
- `.components-main-menu-menu`'s `≤768px` padding changed from `8px 20px` to `8px max(16px, calc((100% - 330px) / 2))` — algebraically the exact same side-inset formula the `min(330px, calc(100% - 32px))` content pattern produces (verified: both reduce to a flat `16px` floor below `362px`, and `(100% - 330px) / 2` above it), so the header's toggle/mobile-actions now land on the identical left/right edges as the content sections at every mobile viewport width, not just one.
- Verified live at `500px` viewport: `.components-main-menu-toggle` and `.components-news-archive-grid`/`.components-site-footer-columns` share identical `left` (`78px`) and the mobile-actions row's right edge matches the content's right edge (`408px`). Hamburger menu open/close still works correctly.

## Recent Changes (HeroNews Desktop Title Card Widened)
- User report: `.components-hero-news-storyCard` (the title card overlaid on the featured-news hero, `HeroNews.tsx:56`) had `max-width: min(640px, calc(100% - 480px))` — a flat `640px` ceiling that kicks in on any viewport past ~1120px. On wide desktop monitors (1440px+) the headline was wrapping to 2 lines or getting cut by `-webkit-line-clamp: 2` even though there's plenty of free space between the text and the thumbnail rail (`.components-hero-news-thumbnails`) to its right.
- Changed only the fixed ceiling, `640px` → `760px` (`max-width: min(760px, calc(100% - 480px))`); left the `480px` thumbnail-rail reserve untouched, so behavior in the 993–1120px range (where that reserve, not the `640px` cap, was already the binding constraint) is unchanged.
- Verified live via Claude in Chrome (`getBoundingClientRect()` on the card vs. the thumbnail rail): at ~1100px width the card/thumbnail spacing is byte-for-byte identical to before (no regression); with the new `760px` cap injected standalone, the gap between card and thumbnails at wide viewports stays large (>200px). Also confirmed, but explicitly out of scope for this change: with 3 thumbnails, the card's bounding box already slightly overlaps the thumbnail rail's bounding box (no visible clipping today) somewhere in the ~993–1120px range — pre-existing, since the `480px` reserve doesn't scale with how many featured-news thumbnails are rendered.

## Recent Changes (Mobile Footer Grid Overflow — Root Cause Found and Fixed)
- A prior session had documented (but not root-caused) `.components-site-footer-columns` computing `grid-template-columns` as `400px` instead of the `1fr` the `≤768px` CSS specifies, causing ~70px of internal overflow. Cascade-layer bugs and the classic Grid intrinsic-min-content trap were already ruled out at the time.
- Actual root cause: `SiteFooter.tsx`'s "mídias sociais" `<section>` (the grid's second column) carries a stray `w-100` class (`className="components-site-footer-column text-acf-gray w-100"`) — a leftover Bootstrap-style convention (meant as "100% width") that does nothing of the sort here. This project runs Tailwind CSS v4, whose dynamic spacing-scale utilities resolve any bare numeric utility without a defined scale entry as `n × 0.25rem` — so `w-100` silently compiled to `width: 25rem` = **400px literal**, matching the exact number in the original bug report. That fixed-400px item forced the mobile grid's single `1fr` track to grow to fit it (grid-item default `min-width: auto` blowout), overflowing the 330px mobile container by ~70px.
- `w-100` had zero other usages in the codebase (`grep -rn "w-100\b" src` matched only this one line) and served no real purpose at any breakpoint — it was silently forcing the same column to 400px on desktop too, overflowing that grid's 176px track there as well (never reported, but confirmed and fixed by the same change).
- Fix: removed `w-100`, kept `text-acf-gray`. Verified live via Claude in Chrome (using a width-controlled `<iframe>` to get a real narrow viewport, since this environment's window resize doesn't affect the actual rendered viewport): at 500px, `grid-template-columns` now computes to `330px` (matches the container, no overflow, `scrollWidth === clientWidth`); at 1440px (desktop), the second column now renders at exactly the grid's intended `176px` (previously 400px, overflowing).

## Recent Changes (Patrocinadores Mobile Title Overflow Fixed)
- A prior session had documented but not fixed: the page heading "patrocinadores." overflows its `≤768px` title band by ~15px — a single hyphenation-less word (plus a colored "." span) that `white-space: normal` can't wrap.
- At `42px` Montserrat 700, the word measures ~345px wide, but the mobile container (`width: min(330px, calc(100% - 32px))`) shrinks to as little as `288px` on real 320px-wide phones.
- Fix, scoped to this one page only (`app-clube-patrocinadores-page-heading h1`, `≤768px`): `font-size` `42px` → `34px`, `line-height` `52px` → `42px` (same ~1.24 ratio as the original). The value was found empirically via Claude in Chrome by comparing the `h1`'s `scrollWidth` against the container at 320px, 360px, 375px, 390px, 500px, and 768px — fits with margin at every one, including the narrowest real device width.
- The other 7 mobile page-heading titles (`font-size: 42px` / `line-height: 52px`) were left untouched — this was specific to the longest word among the 8 page titles.

## Recent Changes (`/clube/elenco` Desktop — New Grouped "Ver por posições" View, Futsal Only)
- User request, backed by Figma measurements (`elenco_principal_posicao_futsal` frame, node `1480-3543`): the "Ver por posições" tab should show the Futsal roster split into titled groups — Goleiros, Fixos (can be more than 2), Alas - Esq / Dir, Pivôs, in that order, each row a left-aligned (not centered/stretched) horizontal auto-layout, ahead of the pre-existing "Comissão Técnica / Staff" section. Scope explicitly agreed with the user: Futsal only for now, Futebol de Campo untouched.
- Pre-existing-code investigation (not a Figma gap): `RosterPosition` only had soccer-field values (`goleiro`/`defensor`/`meio-campo`/`atacante`), reused for futsal athletes too — meaningless seed data for futsal (there was no `"fixo"`/`"ala"`/`"pivô"` at all), and the "Ver por posições" link just filtered down to a single position (`goleiro`), it never grouped anything.
- `src/data/roster.ts`: `RosterPosition` gained `"fixo" | "ala" | "pivo"`; new `futsalPositions` cycle (`goleiro, fixo, ala, ala, pivo`) replaces the field cycle when generating the 20 futsal athletes (resulting distribution: 4 goalkeepers, 4 fixos, 8 alas, 4 pivots); new `futsalPositionGroups` (order + label per group) and a `groupFutsalAthletesByPosition()` helper.
- New `visualizacao` query param (`todas` default | `posicoes`), parsed in `src/app/clube/elenco/page.tsx` and threaded through to `RosterPageContent`. `RosterFilters.tsx`: the "Ver por posições" link now points to `?modalidade=futsal&visualizacao=posicoes` only when Futsal is the active category — for Futebol de Campo the old behavior (filter to `goleiro`) was left completely intact.
- `RosterPageContent.tsx`: when `category===futsal && view===posicoes`, renders the grouped sections (`groupFutsalAthletesByPosition`) instead of the flat grid; otherwise, 100% unchanged (existing `repeat(5, 256px)` grid).
- New CSS (`globals.css`): `.components-roster-roster-page-content-positionGroups` (column, `width: min(1160px, 100%)` — same container family already used by this page's toolbar/hr/staff section), `-positionGroup h3` (section title), `-positionRow` (`display:flex; flex-wrap:wrap; justify-content:flex-start; gap:40px`). The card itself (`components-roster-athlete-card-card`, fixed `256px`, uses a `clip-path` with absolute-pixel coordinates) was neither shrunk nor stretched — just reflowed via `flex-wrap`, so no row overflows at any width, including the 8-card "Alas" group (wraps to 2 lines on its own, no dedicated breakpoint needed).
- Verified live via Claude in Chrome: cards left-aligned (`firstCardLeft === rowRect.left`) in all 4 groups; no overflow (`scrollWidth === clientWidth`) at 1250px, 1470px, 1600px (the 1200–1480px gap-bug range documented earlier in this changelog) and at 375px mobile; Futsal's "Todas as posições" and the Futebol de Campo view are both pixel-identical to before (flat 20-card grid, no groups).

## Recent Changes (`/clube/elenco` — Real DB Data, "Todas as posições" Removed, Campo Placeholder)
- User request: pull the roster and technical staff from the database (`players` and `staff_members` tables, the same Neon Postgres already backing `news`/`team_info`), remove the "Todas as posições" toggle for good (only "Ver por posições" stays), and — since the `campo` (soccer field) modality has zero registered players — show an "elenco ainda sendo montado" (roster still being assembled) notice instead of an empty list or fake data.
- New module `src/data/players.ts` (identical shape to `news.ts`/`teamInfo.ts`: `neon()` + `cache()` + `[]` fallback on error). `src/data/roster.ts` (the mock module) was left untouched — it still backs the player detail page (`/clube/elenco/[slug]`), which wasn't part of this request; `futsalPositionGroups`/`groupFutsalAthletesByPosition`, added last session only for the mock-based grouping, were removed from `roster.ts` since they're dead now that this page no longer imports from it.
- Real schema inspected directly against Postgres (`information_schema`) before writing any query — **no field was assumed**: `players` has `id, modality, number, position, nickname, social_media, quote, name, birthday, dominant_foot` (no `slug`, `image`, `height`, `weight`, `city`); `staff_members` has `id, name, nickname, function` (no `image`). `modality` is the `player_modality` enum (`campo`/`futsal`) — a live query confirmed **0 rows with `modality='campo'`** and 12 with `futsal`, matching what the user reported.
- `position` in the DB is free-typed text, not an enum — real data is inconsistent (`"Pivo"`, `"pivô"`, `"Pivô"`, `"Goleiro"`, `"Ala"`, `"Alal"` — a typo of "Ala" —, `"Fixo"`, `"Ala/Fixo"` — a compound value). `groupPlayersByPosition()` normalizes via `NFD` + accent stripping + `toLowerCase()` + substring matching (`"gol"→goleiro`, `"piv"→pivo`, `"fix"→fixo`, `"ala"→ala`), taking only the part before `"/"` on a compound value; verified against all 12 real rows with an ad-hoc Node script — all 12 land in the right group, typo and compound value included. A player with an unrecognized position would fall into an `"Outros"` bucket (doesn't happen today, but keeps a future bad data-entry from silently vanishing).
- `RosterFilters.tsx`: the "Todas as posições / Ver por posições" nav was removed entirely — only the two modality tabs (Futsal / Futebol de Campo) remain. `RosterPageContent.tsx` is now `async` and always renders grouped (no more `visualizacao` param); `page.tsx` simplified to only read `modalidade` (new default: `futsal`, since it's the only one with real content today — the old default was `campo`).
- When `groupPlayersByPosition()` returns empty (today, only happens for `modalidade=campo`), the page shows `"Elenco de {modalidade} ainda sendo montado. Em breve, mais novidades por aqui."` instead of any grid — text and category are assembled dynamically, so it also self-corrects if `futsal` ever becomes empty. The "Comissão Técnica / Staff" section doesn't depend on modality (one staff for the whole club) and still renders even on the modality with no players.
- `AthleteCard.tsx` was simplified: since it's only used by this page (confirmed via `grep` — the same-named component in the Home page's `RosterSection.tsx` is a different file/type, unrelated), it now takes the DB-shaped data directly (`{id, name, number, positionLabel}` / `{id, name, role}`) instead of the mock `Athlete | StaffMember` type. **Cards are no longer links** to `/clube/elenco/[slug]` — the DB has no `slug` and none of the detail-page data (real photo, height, weight, quote, related players), so linking would either 404 or require fabricating data about real people; left as a static card until the detail page is also migrated to the DB (not requested in this task).
- `globals.css`: `.components-roster-roster-page-content-toolbar` swapped `justify-content: space-between` (which used to separate the title from the two navs) for `align-items:center; gap:32px` (title and tabs now cluster together on the left, since nothing is left on the right); removed the now-dead `.components-roster-roster-filters-positionGroup` class and the `.filters` wrapper (fixed `878px` width, only made sense with both navs side by side); new `.components-roster-roster-page-content-empty` class for the notice.
- Verified live via Claude in Chrome (real data, no mocks): Futsal shows all 4 groups with the 12 real players correctly distributed (2 goalkeepers, 1 fixo, 5 alas, 4 pivots) and names with no stray whitespace (raw data had `" Name "` on some rows — `.trim()` applied on read); Futebol de Campo shows the notice and still shows the technical staff (2 real members); no overflow (`<body>`'s `scrollWidth === clientWidth`) at 375, 500, 992, 1200, 1250, 1470, and 1600px.

## Recent Changes (`/clube/elenco` — Hover/Press Reveal, Position Dividers, Orange Section Titles)
- User request with 4 reference images: (1) a player card should reveal number + nickname + position only on mouse hover or press (touch); (2) a staff card should reveal nickname + role (with the club crest) the same way; (3)/(4) a thin gray line should separate each position group (measured in Figma: `1160 × 2`, `corner-radius: 2`); and each section title ("Fixos" etc.) was black and needed to be orange.
- Discovered while implementing the reveal: the DB-backed cards weren't selecting `nickname` yet — only `name` (full name) was being pulled. Added `nickname` to the query and type in `src/data/players.ts` (the column already exists on both `players` and `staff_members`, it just wasn't being read). `AthleteCard.tsx` now displays `person.nickname` as the card title (keeping `person.name`, the full name, only in the image's `alt` text for accessibility) — and the staff variant's image gained the club crest icon (`/header/symbol.png`, the same asset already used in the header/player-detail page) next to the name, `variant="staff"` only.
- Also fixed: the card's position label was showing the **group's** plural name ("Goleiros") instead of the player's own singular position ("Goleiro") — `positionGroupOrder` in `players.ts` gained a singular `cardLabel` separate from `label` (plural, used only for the section heading).
- `globals.css` (`.components-roster-athlete-card-*`): `.info` (number/nickname/position) and a new `::after` (dark gradient scrim, `rgba(2,12,20,…)`, the same `--azul-preto` already used across the site) now start at `opacity:0` and only reveal on `:hover`, `:focus-visible`, and `:active` (`:active` covers touch "press") — previously always visible. New `.components-roster-athlete-card-staffIcon` class (28×28px) for the staff-card crest.
- **Bug caught during visual verification**: the new `.components-roster-roster-page-content-positionGroup h3 { color: #ff3203 }` rule (meant for the section heading) used a descendant selector (`positionGroup h3`) that also unintentionally painted the player's NAME `<h3>` inside the card orange (it's a deeper descendant of the same `.positionGroup`) — caught this from a zoomed screenshot showing the name rendered orange instead of white. Fixed by switching to a direct-child selector (`.positionGroup > h3`), which only matches the section heading. Also removed `text-transform: uppercase` from the section heading, since the reference shows "Fixos" (not "FIXOS").
- New divider `.components-roster-roster-page-content-positionDivider` (`height:2px; border-radius:2px; background:#cfcfcf` — the same gray already used on the existing `<hr>` between the roster and the staff section, rather than inventing an unconfirmed new shade) inserted as a flex sibling between each position group (not before the first or after the last — that already has the pre-existing `<hr>` ahead of the staff section); the flex container's existing `gap:48px` already gives symmetric spacing on both sides of the divider with no extra margin CSS needed.
- Verified live via Claude in Chrome with a real mouse hover (not just CSS inspection): the goalkeeper card reveals "12 / Giurizatto / Goleiro" over the darkened scrim; the staff card reveals the crest + "Vandin" + "Técnico"; the default (unhovered) state shows only the photo, matching both reference images.

## Recent Changes (`/clube/elenco/[slug]` — Player Detail Page Rebuilt with Real DB Data, Desktop + Mobile)
- User request with one desktop image (frame `elenco_principal_posicao_futsal`, a "ticket"-shaped 1440×840 card with Apelido/Nome/Data de Nascimento/Pé Dominante/social links/photo/number) plus a Figma design-token dump (colors, typography, spacing) for both desktop and mobile.
- **Before implementing, asked the user 2 blocking questions** (via `AskUserQuestion`, did not guess): (1) the Figma card also shows "N° de Jogos/Assistências/Gols" per player, but the `players` table has no such columns and `games` is just the match schedule, no per-player stats — user's answer: **don't follow that part of the design**, those 3 fields were dropped from the implementation; (2) the detail page currently runs on 100% mock data, disconnected from the real `players` table already used by the listing — user's answer: **yes, migrate to real data**, the listing cards become links again.
- **Found while investigating before touching anything**: `public/squad/player-profile.png`, which the user updated and committed in an earlier message ("go ahead and commit the images too, they're part of the details"), was already, in the old code, an absolutely-positioned background `<img>` sitting behind the card's content (not a CSS `clip-path`) — the user had simply re-exported that same PNG from Figma with the new "ticket" look (notched edges, orange frame, bull/"ACF NA VEIA" already baked into the image itself). That removed any need to rebuild the card's shape via CSS/SVG: repositioning the content (text, photo, number) on top of the new PNG was enough. A new `public/squad/player-profile-mob.png` (381×607) came along too — the mobile version of the same frame.
- `src/data/players.ts`: one consolidated `getAllPlayers()` query (request-cached) now also pulls `birthday`, `dominant_foot`, `quote`, and `social_media` (jsonb) alongside what already existed; `getPlayersByCategory`/`getPlayerBySlug` now filter that same in-memory list (a single DB round-trip per request). New `slug` helper (derived from `nickname`, accents stripped, an id suffix appended on collision) — the DB has no `slug` column. `birthday` is formatted with `getUTCDate/Month/FullYear` (avoids the classic timezone bug that shifts the day by one); `dominant_foot` (enum `direito`/`esquerdo`/`ambidestro`) only needs its first letter capitalized, since the enum values are already the correct Portuguese words.
- `AthleteCard.tsx` is a `<Link href="/clube/elenco/${slug}">` again (it had been static since last task, for lack of a `slug`/real data — now resolved).
- `PlayerDetailContent.tsx` rewritten: new "Perfil do Atleta" header / "Elenco →" link (back to `/clube/elenco?modalidade=${category}`, preserving the right tab); inside the card — "Apelido" (small orange caption) + giant nickname, a 2-column Nome/Data de Nascimento/Pé Dominante list, real social icons (the DB's `social_media`, rendered only when a URL is present, reusing the same `/footer/facebook.png`, `/footer/instagram.png`, `/footer/youtube.png` PNGs already used in the footer), a placeholder photo, and a ghost jersey number (`#99` etc.) over the frame. The quote (`quote`, a real DB field) only shows up if a player actually has one entered — none do today, so it doesn't render, matching the reference image (card → sponsors, nothing in between).
- **Removed** the old "related players" section (a mini-card grid for the same position) and the "ACF SPORTS #NAVEIA" signature footer with its plain-text (non-link) social list — the new Figma image shows neither, and the single "Elenco →" link already covers going back.
- `[slug]/page.tsx`: swapped `getAthleteBySlug` (mock) for `getPlayerBySlug` (real DB); removed `generateStaticParams` — the page went from static (SSG, pre-built from the mock array) to dynamic (`ƒ`, matching the listing), so a newly-registered player already has a working page with no redeploy needed.
- **Cleanup**: with the detail page migrated, `src/data/roster.ts` (the entire fake mock athlete array) and `src/components/roster/PlayerMainCardImage.tsx` (an SVG card generator that, confirmed via `grep`, was never actually rendered anywhere) had no real consumers left — deleted both files plus their orphaned CSS, rather than leaving ~200 dead lines in the repo.
- Responsive: a `<picture>` with `<source media="(max-width:768px)">` swaps to the mobile frame automatically; the mobile layout (positions/font sizes) is my own adaptation from the typography tokens alone (no mobile image was provided) — worth checking against a real Figma mobile screenshot if anything looks off.
- Verified live via Claude in Chrome (real data, player "Cabeludo"/Miguel Ribeiro Dos Santos): desktop visually matches the reference image (Apelido/Nome/Data/Pé Dominante/Instagram/photo/#99 over the new frame); mobile swaps to the `-mob.png` frame automatically with no overflow (`scrollWidth === clientWidth` at 390px); clicking a listing card navigates end-to-end to `/clube/elenco/giurizatto`; the "Elenco →" link goes back to `/clube/elenco?modalidade=futsal`.

## Recent Changes (`/clube/elenco/[slug]` — Precise In-Card Repositioning from Figma's Design Panel)
- User request with 7 images: 6 Figma Design-panel screenshots measuring individual card elements (apelido/nickname, nome/data de nascimento, pé dominante, social links, player photo, jersey number) plus 1 screenshot of the real site showing the card rendering too large — overflowing the page, with the header overlapping the top of the card.
- Extracted the measurements from each screenshot (`Position X/Y`, `Resizing W×H`, `Gap`) and, to place each element, reused the card's own origin recorded in an earlier message (`X:85, Y:546, W:1440, H:840`) to convert Figma's absolute canvas positions into offsets relative to the card (e.g. the player photo at `X:903,Y:657,W:418,H:605` becomes `left:56.8%, top:12.9%, width:29%, height:70.3%` of the card) — sanity-checked this math by confirming the resulting right-margin for the photo (~14.2%) matched what I'd already estimated in an earlier iteration, which gave confidence to proceed.
- **"Pé Dominante is the same measure" (point 3)**: Figma shows different horizontal gaps between the two columns on the "Nome/Data de Nascimento" row (76px) vs. "Pé Dominante/N° de Jogos" row (156px) — that's just an artifact of Hug-width auto-layout (the 2nd column starts at a fixed position, so the gap shifts depending on how long the 1st column's text is). This page already uses a 2-column CSS Grid (`1fr 1fr`), which aligns the 2nd column consistently regardless of content — so instead of copying Figma's two different numbers, only the vertical gap between rows was adjusted to `32px` (was `24px`), which is what the user meant by "same measure."
- **A real bug, not just repositioning**: the player photo (`.playerFigure`) and ghost jersey number (`.number`) had the wrong size — `340×700` (a narrow, overly-tall rectangle) and `font-size:140px`, when Figma measures `418×605` (photo) and a `248×162` text block for "#08" (roughly `90-95px` font-size). Fixed to the correct values — the photo is noticeably less stretched, and the number is much smaller.
- **Root cause of the "card overflowing the page" (point 6)**: `.profileCard` used `width:100%` inside a container up to `1440px` — with `aspect-ratio: 1461/861`, that gave a card up to `~849px` tall, easy to not fit on one screen without a lot of scrolling. Reduced to `width: min(1160px, 100%)` (the same narrower "family" already used by this page's own toolbar and by other site sections — footer, plans, etc.), which also shrinks the height proportionally (`~684px`) — the whole card now fits on one screen on common laptop sizes.
- **Bug caught only by testing in a real browser** (not just reading CSS): switching `.playerFigure` from a fixed `right:200px` to a percentage-based `left:56.8%` (so it scales correctly with the card's new size) broke the existing mobile breakpoint (`≤768px`) — `right:0; width:100%; height:52%` — because now there was a `left` inherited from desktop *and* `width:100%` at the same time, and in that combination CSS ignores `right`. Result: on mobile, the photo was 100% of the card's width but shifted ~57% to the right, spilling well outside the card. Only caught this by measuring the photo's `getBoundingClientRect()` inside a 390px iframe — fixed by adding an explicit `left:0` in the mobile block.
- Verified live via Claude in Chrome: the whole card visible in a single screenshot on desktop (previously needed scrolling); tested with a short nickname ("ICE") and a long one ("GIURIZATTO", 10 letters) without breaking out of the box; no overflow (`scrollWidth === clientWidth`) at 992, 1100, 1200, 1300, 1440, 1600, and 390px (mobile, after the bug fix above).

## Recent Changes (`/clube/elenco/[slug]` Mobile — Internal Card Spacing Fixed from Design-Panel Measurements)
- User request: 5 Figma Design-panel screenshots measuring specifically the **mobile** version of the card — the Apelido/nickname group, the full text block, the gap between one text row and the next, the photo+number group, and the social-links row.
- `Gap: 2` on the Apelido/nickname group (was `margin-top: 4px` in the code — adjusted to `2px`); the identity block's inset from the card edges: `top: 46px` and `left: 40px` on an intrinsic `381×607px` card → converted to `7.6%`/`10.5%` (percentage-based, so it stays correct at any screen width, since the card scales fluidly via `aspect-ratio`).
- **"Spacing between the text items" (point 3)**: this was the most direct measurement of the bunch — selected the "Nome" row itself in Figma and the panel showed `Gap: ⇕ 8` between it and the following "Data de Nascimento" row. Changed the mobile `.profileList` grid's `gap` from `12px` to `8px`.
- **Found while trying to reconcile this gap with the rest of the card**: Figma measures the full list with the design's original 6 rows (including `N° de Jogos/Assistências/Gols`, already dropped earlier since that stat doesn't exist in the DB — see prior changelog entry). That means the "list → social links" spacing Figma shows (computed from each element's absolute position) assumes a 6-row list, not the 3 rows we actually render. Rather than try to recompute that number for a shorter list (fragile, would require fully trusting cross-referenced math between different screenshots), the same "space after the last visible row" logic was kept and the resulting value (`60px`) applied as `margin-top` on the social-links block — the more predictable behavior, and one that doesn't break if the row count changes later.
- Photo + jersey number (point 4): the mobile photo is **not** a full-width banner like I'd previously implemented — Figma shows a smaller cutout (`216×316`, ~57%×52% of the card) tucked against the card's right/bottom edges (`right:10px`/`bottom:72px` → `2.6%`/`11.9%`), the same composition as desktop (photo as a portrait on the right, overlapping the text), just at a smaller scale. Removed the "full-width banner" version I'd built before.
- Social links (point 5): gap between icons adjusted from `14px` to `16px` (a direct Figma measurement, `Gap: 16` on the icon row).
- Verified live via Claude in Chrome, inside a 390px iframe: no overflow (`scrollWidth === clientWidth`); the whole card visible in a single screenshot; the player photo now shows as a portrait on the right overlapping the text starting around "Pé Dominante" (matching the reference), no longer a horizontal banner; spacing between "Nome"/"Data de Nascimento"/"Pé Dominante" visibly tighter and even between all rows.

## Recent Changes (`/clube/elenco/[slug]` Mobile — Jersey Number Rendering Outside the Card)
- User reported (likely testing on a phone through an ngrok tunnel, visible in the dev server logs) that the jersey number was rendering outside the card on mobile, with the Figma "photo + number" group measurement (`W:131.19 H:72`, a `0` bottom-gap marker).
- **Root cause, confirmed by measuring in the browser before changing any CSS** (not just going by the given number): the mobile `.number` still carried a leftover value from before the photo's correct measurements were known — `right:20px; bottom:20px`, a much smaller inset than the photo's own (`bottom:11.9%` ≈ 72px). Since the card has notched corners (the "ticket" shape), that small inset placed the number right on top of the bottom-right corner's diagonal cutout — technically inside the element's rectangular box, but visually outside the card's visible dark surface, spilling past the orange stroke line.
- Fixed by aligning the number to the same bottom "floor" already used by the photo (`bottom: 11.9%`), with `right: 8%` and `font-size` reduced from `56px` to `48px` (so it isn't cramped in that more inset spot).
- Verified live via Claude in Chrome inside a 390px iframe: the number now clearly sits within the card's dark surface, over the photo, above the "ACF NA VEIA" watermark, clear of the corner's orange stroke.

## Recent Changes (`/clube/elenco/[slug]` — User-Made Styling Tweaks to the Player Card)
- "Apelido" label swapped from text (`<p>`) to an image asset (`/squad/apelido.png`); Instagram icon swapped from `/footer/instagram.png` to `/squad/insta-azul.png`; `Nome`/list-value colors (`dt`/`dd`) adjusted via arbitrary Tailwind classes (`text-[#ffffff]`/`text-[#FF7A5C]`). Build (`npm run build`) verified before commit.

## Recent Changes (`/contato` — Top Half Rebuilt from Figma: "Fale com o ACF" + "Onde estamos" + Cards + "Fique à vontade")
- User request with 11 Figma Design/Inspect panel screenshots (measuring `Position X/Y`, `Resizing W×H`, `Gap`) plus a final page mock and the Figma-exported design-tokens YAML (colors, typography, spacing, radius).
- The new Figma design has no contact form ("vamos conversar?") and no tabbed plans section ("Nossos Planos") that previously existed — only "Fale com o ACF", "Onde estamos" with 3 cards (address/channels/hours), a gray divider, and "Fique à vontade" with a WhatsApp CTA; the "Seja um parceiro do ACF" section (3 static cards) was left out for now, to be supplied by the user later. Asked the user what to do with the existing form and plan tabs — confirmed removing both now (`ContactContent.tsx` rewritten from scratch, dropping the plan-tabs state and the form).
- `Nossa sede.`/`Nossos canais.` use `teamInfo.address`/`teamInfo.phone`/`teamInfo.email` (DB-backed via `getTeamInfo()`), keeping the existing dynamic pattern; `Horario de atendimento.` is static copy (no matching column on the `team_info` table).
- Card and WhatsApp-button icons switched from PNGs (`/contact/*.png`) to `lucide-react` (`MapPin`, `Phone`, `Clock`, `MessageCircle`), already a dependency used in `MainMenu.tsx`.
- CSS (`.components-contact-content-*` in `globals.css`) rewritten: container `min(1160px, calc(100% - 40px))` (same pattern already used by the page header and other sections), `40px`/`24px` vertical gaps between blocks matching the Figma panel's markers, a `2px` divider in the design system's `cinza_verde` color (`#cfcfcf`), cards using the double drop-shadow effect token (`-1px -1px 4px` / `1px 1px 4px`, `#A09E9E/0.32`), and a WhatsApp button with `10px` radius and white text (consistent with the previously recorded fix for the plan-card WhatsApp buttons).
- Heading/paragraph font sizes and colors were inferred from the Figma typography token table cross-referenced with the pattern already used elsewhere in the file (couldn't be derived precisely from the Design panel's "Hug" block heights alone) — worth double-checking against an actual Figma screenshot if any text size looks off.
- Build (`npm run build`) validated and checked live via Claude in Chrome at 1600px: structure, copy, icons, and spacing match the provided mock.

## Recent Changes (`/contato` — Added the "Seja um parceiro do ACF" Plan Cards Section, Reusing `/clube/patrocinadores`'s Data/Style)
- User request with 5 Figma Design-panel screenshots (section size `W1600×H1010`, title block position `X:216,Y:80,W:760 Hug,H:96 Hug,Gap:8`, card grid position/gap `X:80,Y:216,W:1440 Hug,H:714 Hug,Gap:40`) and an explicit note that the cards are the same ones already used in "Nossos Planos" on the sponsors page.
- The numbers match two independently centered containers of different widths inside the `1600px`-wide dark band: the title uses the same standard container already used elsewhere on this page, `min(1160px, calc(100% - 40px))` (≈220px inset, close to the measured `216`), while the card grid uses a new, wider container, `min(1440px, calc(100% - 160px))` (exact `80px` inset) — confirmed by visually comparing the provided mock, which shows the title indented further than the first card's left edge.
- Copied the plan data (`plans` — names, prices, WhatsApp messages, benefits) and the card's visuals (dark-red `#661400` title, orange-ring bullet, blue price, no-icon gradient button) directly from `SponsorsPageContent.tsx`/`PlanCard`, rather than re-transcribing the slightly different wording from the new screenshots — the user confirmed these should be "the same" cards, so the already-implemented component is the source of truth, not the new screenshot. Data/CSS were duplicated inside `ContactContent.tsx` (not extracted into a shared component), following the repo's existing pattern of colocating data per component rather than sharing it across pages.
- Unlike the sponsors page's grid (asymmetric `340px 400px 340px` columns with a bigger/featured "master" card), here all 3 cards are the same size (`repeat(3, 1fr)`) — this section's mock shows no card bigger than the others.
- The dark background band uses `var(--azul-preto)` (the same color as this page's "contato." header), with `padding: 80px 0` matching the top/bottom insets measured in Figma.
- Build (`npm run build`) validated and checked live via Claude in Chrome at 1600px: title aligned per the mock, all 3 cards the same height (grid stretch), price/button identical to the existing sponsors-page pattern, no stray whitespace in the transition between the light "Fique à vontade" section and the dark band.

## Recent Changes (Post-QA Fixes — Desktop + Mobile)
- `RosterSection.tsx` (home "elenco" carousel) now receives real players via `getPlayersByCategory("futsal")` (called in `page.tsx`, first 5) instead of the hardcoded `athleteCards` array; the zigzag visual offsets (`athlete2`/`athlete3`/`athlete4` in CSS) now key off the card's position in the carousel rather than the DB `id`. On mobile (`≤768px`, via `matchMedia` inside `useEffect`) the component trims the list to the first 4 players, per request.
- Added `ScrollReveal.tsx` (a client-side wrapper using `IntersectionObserver`, fires once then disconnects) and wrapped it around the home page sections (`newsBand`, `PlansSection`, `RosterSection`, `YoutubeSection`) for a fade-in + translateY animation on scroll; `.components-scroll-reveal-section`/`-visible` classes live in `globals.css`, with a `prefers-reduced-motion` fallback.
- `.components-roster-athlete-card-text` and `-label` (the text/badge shown on hover for cards on `/clube/elenco`) had no declared `font-family` and fell back to the `body`'s default `--font-rubik`; fixed to `--font-outfit`/`--font-montserrat` (the same title+text font pairing used elsewhere on the site).
- Removed the "Gm"/"Gc" columns from the standings table in `CompetitionsContent.tsx` (`/clube/competicoes`); on mobile, the table now fits inside its card without needing horizontal scroll (first column capped at `92px` with `text-overflow: ellipsis`, font size reduced to `12px`).
- The "whatsapp" button in `ContactContent.tsx` swapped the generic lucide `MessageCircle` icon for the real `/contact/whatsapp.png` asset (with `filter: brightness(0) invert(1)` to render it white against the orange gradient).
- The `HeroNews` button (mobile, `≤992px`) got `margin-bottom: 60px` so it no longer sits flush against the bottom of the screen.
- Build (`npm run build`) validated and checked live via Claude in Chrome on desktop (1440px) and mobile (390px).

## Recent Changes (`/clube/competicoes` Mobile — Off-Center Team Crests on the "Próxima Partida" Card)
- Root cause found by measuring `getBoundingClientRect()` on the actual elements in the browser (not just eyeballing the screenshot): the `<span class="teamLogo">` wrapping the crest `<img>` is, technically, just another `<span>` child of `.components-competitions-content-team` — so the mobile rule `.components-competitions-content-team span { display:block; width:100%; ... }` (meant only for the team-name text to truncate with `text-overflow:ellipsis`) was also matching the crest's wrapper span, overriding its base `display:inline-flex` (higher specificity: `.team span` beats `.teamLogo`) and pushing the crest to the left edge of the card instead of centering it.
- Fixed by excluding `teamLogo` from the generic rule (`span:not(.components-competitions-content-teamLogo)`) and adding an explicit `justify-content:center; width:100%` on `.teamLogo` inside the mobile block, so it stays centered even if the base `display:inline-flex` changes later.
- Verified live via Claude in Chrome at 390px, measuring both crest `<img>` centers against the "X" center: symmetric distance (~80.7px) on both sides after the fix (before: ~118.7px on one side, ~42.7px on the other).

## Recent Changes (Smoother Home Scroll Animation + "Players Not Loading" Diagnosis)
- `ScrollReveal.tsx`/`globals.css`: the `opacity`/`transform` transition changed from `700ms ease` (`36px` offset) to `1100ms cubic-bezier(0.22, 1, 0.36, 1)` (a gentle "ease-out-quint" deceleration curve) with the offset reduced to `24px` — a noticeably more gradual effect while scrolling the page.
- User reported `/clube/elenco` wasn't loading players; confirmed via a direct Neon query (outside the app) that the `players` table has **0 rows**, while every other table (`staff_members`, `games`, `competitions`, `team_info`) has data as normal — not a code bug, missing data in the database (user later confirmed the table had gotten corrupted). Since the home page's "elenco" carousel was wired to the same query in the previous round, the home page shows the same symptom (the "elenco." section renders with no cards) until the data is restored — no code change was made for this, the components already read from the correct source and will work again once the table is repopulated.

## Recent Changes (Home `planos.` — Cards Rebuilt from Figma Design-Panel Measurements)
- Rebuilt via the `figma-desktop-parity` skill using screenshots of the Figma Design panel (not eyeballed redlines). Reference frame `1600×774`; PONTUAL card `X:220 Y:194 W:560 H:480`; MASTER card `X:820 Y:172 W:560 H:524` (elevated `22px`, `44px` taller than pontual); `40px` gap between cards — all of it lines up exactly with the existing `.components-plans-section-inner { width: min(1160px, ...) }` container (`(1600-1160)/2 = 220`, `560+40+560 = 1160`). Verified live via `getBoundingClientRect()` at 1600px: all four values match the Figma numbers pixel for pixel.
- Removed the "mensal" card from the home section (the Figma frame only has 2 `planos_home` instances, confirmed by the exported design-token doc); it still exists on `/contato` and `/clube/patrocinadores`, which have their own separate data/CSS and were left untouched. Kept the existing prices (R$180/R$500) and benefit copy as-is — the Figma mock showed different numbers (R$199/R$299) and a slightly different benefits list, but that reads as mockup placeholder content rather than the real values already used consistently on the other pages; confirmed with the user before deciding to keep the real content.
- Both cards now share the same dark `#07243d` background (previously the "featured" card used a light `#e1ebf5` background with dark text, diverging from the new Figma look); `h3` title unified to `32px/48px` Montserrat SemiBold on both cards (previously only the featured card used that size); added a thin `<hr>` divider between the header (title+price) and the benefits list, present in every Figma screenshot but missing from the previous implementation; accent color (MASTER title, checkmark icons, button gradient) switched to `#FF7A5C`/`linear-gradient(168deg, #ff7a5c 0%, #8f1d00 99%)` — the gradient already matched the extracted Figma token exactly, no adjustment needed.
- The "ver mais" button switched from a fixed width (`200px`) to hug-content (`padding:16px 32px`, no `width`), matching the Figma `btn_card_azul` component's `Resizing: Hug` setting.
- **Bug found and fixed while verifying on mobile**: the button used `margin-top:auto` to sit flush at the card's bottom edge — that only works when the card has a fixed height (desktop). In the mobile carousel (`≤768px`) the card uses auto height (`min-height:0`), so `margin-top:auto` had no leftover space to push against and the button ended up touching the last list item. Switched to a fixed `margin-top:24px`; desktop spacing stays identical because what actually pushes the button to the card's bottom there is `.benefits{flex:1}` stretching inside the fixed-height card, not the button's own margin.
- Build (`npm run build`) validated and checked live via Claude in Chrome at 1600px (desktop) and ~390px (mobile carousel, both cards).

## Recent Changes (Root Cause Fix — "Players Not Loading" on Home + `/clube/elenco`)
- After the user re-registered players in the database (the `players` table was empty/corrupted in the previous session), the home page and `/clube/elenco` still showed no players even though the table was populated (11 rows confirmed via a direct Neon query).
- **Root cause**: the `players.modality` column changed from a single scalar value (`"futsal"` or `"campo"`) to a Postgres **array** (`player_modality[]`) — all 11 current players have `{futsal,campo}` (they play both modalities). `src/data/players.ts` still expected `modality` as a single string and compared `player.category === category` (`===` against an array is never `true`), so `getPlayersByCategory("futsal")`/`("campo")` always returned an empty list — hence the home carousel and `/clube/elenco` listing both rendering the "no players" empty state.
- Fixed in `players.ts`: `PlayerRow.modality` and the new `RosterPlayer.categories` field are now `RosterCategory[]`; `getPlayersByCategory` switched from `===` to `.includes(category)`. Kept `RosterPlayer.category` (singular, first modality in the array) only for the "Elenco →" link in `PlayerDetailContent.tsx`, which needs a single value to build the `?modalidade=...` URL — that component wasn't changed.
- Verified with `npx tsc --noEmit` (no errors) and live via Claude in Chrome: `/clube/elenco?modalidade=futsal` and `?modalidade=campo` both show all 11 players (expected, since every current player has both modalities), and the home page's "elenco." carousel shows 5 real players again.

## Recent Changes (`/clube/patrocinadores` "Nossos Planos" — Rebuilt to Match Home's Visual System)
- Same exact measurements as the home redesign (via `figma-desktop-parity`): `560×480` (pontual) and `560×524` (master, elevated `22px`) cards, `40px` gap — matching the `min(1160px, ...)` container the section already used. Verified live via `getBoundingClientRect()`: `560×480`/`560×524`/`40px` gap/`22px` elevation, identical to Figma.
- Removed the "mensal" plan card (Figma only shows 2 cards here too) — confirmed with the user beforehand since this is the real sales page (unlike home, which is just a teaser); the mensal plan no longer appears on this page.
- `SponsorsPageContent.tsx`: `PlanCard` restructured to header (title+price) → divider → list → button, matching the home pattern; light card background (`#fdfdfd`) switched to the same dark `#07243d` as home; the old empty-circle list marker switched to an orange `✓` checkmark; button labels changed from a fixed "enviar direct no whatsapp" (all 3 cards) to `"aderir"` (pontual) / `"ver mais"` (master) with an arrow icon — confirmed with the user beforehand, keeping the per-plan WhatsApp `href` with its pre-filled message (only the visible label changed, not the behavior).
- **CSS specificity bug found and fixed**: the featured button's gradient (`.featuredPlanButton`, a single class) wasn't showing — the button rendered gray even with the class applied. Cause: the base rule `.components-sponsors-page-content-planCard a` (class+element compound selector, specificity 0,1,1) outweighed `.featuredPlanButton` alone (specificity 0,1,0), so it always won regardless of source order. Fixed by switching to `.components-sponsors-page-content-featuredPlanCard a` (same 0,1,1 specificity as the base rule, resolved by source order) — the same bug class already seen once before in `.components-competitions-content-teamLogo`.
- Card grid migrated from `grid-template-columns: 340px 400px 340px` (asymmetric 3-column) to `display:flex` with 2 `560px` cards; `≤1200px`/`≤768px` breakpoints rewritten to follow the same horizontal scroll-snap carousel pattern already used on home.
- Build (`npm run build`, `npx tsc --noEmit`) validated and checked live via Claude in Chrome on desktop (1600px, measured) and mobile (~500px, both cards in the carousel).

## Recent Changes (Mobile: Plan Cards Switched from Horizontal Carousel to Stacked)
- User request after seeing the carousel live. Removed `overflow-x:auto`/`scroll-snap-type:x mandatory`/`flex-direction:row` from the `≤768px` block on both components (`.components-plans-section-cards` and `.components-sponsors-page-content-planGrid`) — the `≤1200px` block already stacked the cards in a column (`flex-direction:column`) with `gap:40px` inherited from the base rule, so it was enough to stop overriding to `row` on mobile; added `gap:24px` (tighter than desktop's `40px`) and removed the `margin-right`/`scroll-snap-align`/`:last-child` rules that only made sense for the carousel.
- Verified live via Claude in Chrome at ~606px: both cards on each page now stack one below the other, no horizontal swipe needed.
