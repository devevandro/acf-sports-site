# ACF Site Figma.

Next.js site for ACF Sports, built from Figma design context and local public assets.

## Tech Stack

- Next.js 15 with App Router
- React 19
- TypeScript 5
- Tailwind CSS 4

## Development

Install dependencies and run the local server:

```bash
npm install
npm run dev
```

Build the production bundle:

```bash
npm run build
```

## Project Notes

- Pages live under `src/app`.
- Shared sections and UI live under `src/components`.
- Roster and news seed data live under `src/data`.
- Site media is stored in `public`, grouped by page or section.
- `docs/figma/README.md` records the Figma extraction mapping.

- **Figma Site Alignment & Responsive Breakpoints**:
  - Standardized responsive breakpoints across the project: `576px` (celulares grandes), `768px` (tablets), `992px` (laptops), and `1200px` (desktops).
  - Aligned competitions, contact, history, and player profile sections with Figma design nodes (`node-id=2396-21910`, `2392-9111`, `2394-9599`, `2394-20847`).
  - Replaced temporary Figma image URLs with committed local assets under `public/history`, `public/squad`, `public/header`, and `public/contact`.
  - Added interactive competition filtering, match detail modal behavior, sponsorship plan tabs, and contact form confirmation state.
  - Migrated roster and player profile navigation links to Next.js `<Link>`.
  - Refactored `HeroNews.tsx` carousel: 12-second auto-play timer, active thumbnail progress bar animation, inactive thumbnail grayscale filter, full screen (100%) width container, and viewport height optimization for scroll-free viewing.
  - Aligned sponsorship reasons content section left margin to match the page header container title ("patrocinadores.").
  - Updated the top header to rotate committed GIF assets from `public/gif` and adjusted the player profile figure/frame layout to use the shared local player image.
  - Standardized internal code identifiers to English for page component names, menu state keys, roster helpers, sponsorship plan keys, and roster query parameter constants while preserving Portuguese UI copy and routes.
  - Validated production build (`npm run build`).
  - Refined home page interactions for the hero slide fade, main menu dropdown active states, news CTA layout, roster hover details, sponsor marquee, and sponsorship plan card feedback.

- **Mobile Navigation & Brand Refresh**:
  - Added a mobile hamburger menu to `MainMenu.tsx`: a toggle button and collapsible full-width panel with a row-style accordion for the "clube" submenu, plus WhatsApp/Instagram quick-action icons visible only below the `768px` breakpoint.
  - Updated the primary brand orange (`--laranja` and related accent colors) from `#f56345`/`#cc4529` to `#ff3203` across `globals.css`, `ContactContent.tsx`, and `PlayerMainCardImage.tsx`.
  - Switched `globals.css` `font-family` declarations to reference the CSS custom properties (`--font-montserrat`, `--font-outfit`, `--font-rubik`, `--font-roboto`, `--font-poppins`) exposed by `next/font/google` in `layout.tsx`, replacing hardcoded font names.
  - Replaced hero carousel and related news images with new local assets (`public/carousel/image-01.jpg`, `public/carousel/image-02.jpg`).

- **Carousel Image Update**:
  - Replaced `public/carousel/image-01.jpg` and `image-02.jpg` with `image-01.png`/`image-02.png`, updating references in `HeroNews.tsx` and `src/data/news.ts` accordingly. Files were saved with capitalized names (`Image-01.png`/`Image-02.png`) and renamed to lowercase to avoid breaking on Vercel's case-sensitive Linux build environment (macOS's filesystem is case-insensitive, so the mismatch wasn't visible locally).

- **Favicon Fix**:
  - Moved `favicon.ico` from `src/favicon.ico` to `src/app/favicon.ico` — Next.js App Router only picks up the favicon automatically from `app/favicon.ico` (or `public/favicon.ico`); it was previously outside `app/` and silently ignored.
  - Shortened the page `<title>` metadata in `layout.tsx` to "ACF Sports | Site Oficial".

- **CI/CD**:
  - Added GitHub Actions workflows (`.github/workflows/prev-deploy.yaml`, `.github/workflows/prod-deploy.yaml`) that build and deploy to Vercel: preview deployments on push to `stage`, production deployments on push to `main`.
  - Fixed `prev-deploy.yaml`: removed the unsupported `--preview` flag from the `vercel build` and `vercel deploy` steps (Vercel CLI 59.x has no such flag; preview is the default target when `--prod` is omitted), which was failing the workflow with `unknown or unexpected option: --preview`.
  - Added a step to `prev-deploy.yaml` that aliases every `stage` preview deployment to a fixed domain, `stage.acfsports.com.br`, via `vercel alias set` — needed because the Vercel project has no Git repository connected, so the dashboard can't offer branch-based domain assignment. Requires a CNAME record for `stage` pointing to `cname.vercel-dns.com` on the DNS provider (Cloudflare).
  - Registered `stage.acfsports.com.br` as a formal project domain on Vercel (`vercel domains add`) so it's exempt from SSO deployment protection like the production domain — a bare `vercel alias set` alias alone doesn't qualify for that exemption and was prompting a Vercel login on first access.

- **News: Neon Postgres Integration**:
  - `src/data/news.ts` no longer holds static mock data — it now queries the `news` table in the shared Neon Postgres database (via `@neondatabase/serverless`, lazily initialized in `src/db/index.ts`) that the `acf-site-manager` dashboard (`dashboard.acfsports.com.br`) writes to. `getAllNews()` is wrapped in React's `cache()` and falls back to `[]` on a DB error so pages degrade gracefully instead of crashing.
  - The `acf-site` Vercel project's `DATABASE_URL` env var points at the same Neon database `acf-site-manager` uses (read access); locally it lives in `.env` (gitignored).
  - `NewsItem` shape changed to match the DB columns: `id`, `tag`, `title`, `subtitle`, `content` (rich HTML from the dashboard's editor, rendered via `dangerouslySetInnerHTML`), `author`, `image`, `createdAt`. The old mock-only fields (`slug`, `category`, `description`, `caption`, `body[]`, `quote`, and the `layered`/`mascot` image variants) are gone.
  - Detail route renamed from `src/app/noticias/[slug]` to `src/app/noticias/[id]` since the DB has no slug column — links now use the row's `id` (UUID). `generateStaticParams` was removed in favor of ISR (`export const revalidate = 60` on `/`, `/noticias`, and `/noticias/[id]`) since content is edited live via the external dashboard.
  - `HeroNews.tsx` no longer has its own hardcoded `slides` array — it's now a client component that receives `news: NewsItem[]` as a prop, fetched server-side in `src/app/page.tsx` via `getFeaturedNews(3)`. `NewsGrid.tsx` and `NewsArchive.tsx` became async server components fetching `getLatestNews(6)` / `getAllNews()` directly.

- **Carousel da Home: Filtro por Highlight e Notícia Fixada**:
  - `NewsItem` ganhou o campo `highlight: boolean` (da coluna `highlight` da tabela `news`). `HeroNews.tsx` agora só exibe no carousel da home notícias com `highlight === true`, e sempre força a notícia de ID `cd288794-2014-4f37-8163-cb5082cd0b47` para a posição fixa do carousel, independente do seu próprio valor de `highlight`. `src/app/page.tsx` agora passa `getAllNews()` no lugar do antigo helper `getFeaturedNews(3)` (removido).
  - `getLatestNews()` (o grid "notícias" da home, `NewsGrid.tsx`) agora sempre exclui essa mesma notícia fixada, então ela só aparece no carousel. O arquivo completo `/noticias` (`NewsArchive.tsx`, via `getAllNews()`) não é afetado.

- **Carousel da Home: Notícia Fixada Movida para a Posição 3**:
  - O ponto de inserção da notícia fixada no `HeroNews.tsx` mudou do índice 1 (posição 2) para o índice 2 (posição 3).

- **Grid de Notícias da Home: Layout Corrigido com Menos de 3 Itens**:
  - `.components-news-grid-grid` usava `grid-template-columns: repeat(3, 330px)` fixo, o que reservava espaço de layout para 3 itens mesmo quando havia menos disponíveis (ex.: com a notícia fixada excluída do grid, sobram só 2 dos 6 itens buscados), deixando o layout desbalanceado.
  - `NewsGrid.tsx` agora calcula `gridColumns = Math.min(newsItems.length, 3)` e passa via custom property CSS `--news-grid-columns`; `globals.css` usa `repeat(var(--news-grid-columns, 3), 330px)` no desktop e `repeat(min(var(--news-grid-columns, 2), 2), 330px)` no breakpoint de 1200px, então o grid sempre reflete a quantidade real de notícias.

- **Painel "jogos" da Home: Conectado à Tabela `games`**:
  - Adicionado `src/data/games.ts`, uma camada de acesso a dados para a tabela `games` (`id`, `competition_id` → join com `competitions.title`, `opponent`, `result`, `date`, `location`), seguindo o mesmo padrão de `src/data/news.ts` (`cache()` + fallback silencioso em erro). Uma partida é considerada **finalizada** quando `result` não é vazio nem `"-"` (o valor padrão que o formulário de jogos do dashboard usa para partidas futuras), e **próxima** caso contrário.
  - `getLatestFinishedGame()` retorna a partida finalizada com a `date` mais recente; `getNextUpcomingGame()` retorna a próxima partida com a `date` mais próxima. Ambas retornam `null` quando não há partida correspondente.
  - `GamesPanel.tsx` virou um componente de servidor assíncrono que busca as duas via `Promise.all` e só renderiza cada bloco ("partida finalizada" / "próxima partida") quando a partida correspondente existe (antes eram exemplos estáticos fixos no código).

- **Painel "jogos" da Home: Logo do Adversário**:
  - A tabela `games` não tem coluna própria de logo. Os brasões dos adversários ficam dentro do JSON `competitions.table` (cada item tem `team` e, opcionalmente, `symbol`), então `getAllGames()` agora também busca `c."table" AS competition_table` e resolve `opponentLogo` casando `team === opponent` (exato, com trim).
  - `GamesPanel.tsx` usa `game.opponentLogo` quando existe, caindo para o brasão do próprio clube (`/header/symbol.png`) quando o adversário não tem `symbol` cadastrado na tabela da competição ou quando a partida não tem competição vinculada.
  - Confirmado com dados reais na tabela `games`: a próxima partida mais próxima ("Cyber Futsal / Arena Cassimiro", 03/09/2026) já renderiza o brasão real hospedado no Vercel Blob.

- **Painel "jogos" da Home: Text-Overflow em Nomes Extensos**:
  - Nomes longos de adversário/clube podiam estourar a largura fixa de 95px do `.components-games-panel-team`, pois o `span` tinha `white-space: nowrap` sem truncamento, e o bloco do time é um item flex cujo `min-width: auto` padrão deixa o conteúdo mínimo (o texto inteiro) vencer a largura explícita.
  - Corrigido no `globals.css`: adicionado `min-width: 0` em `.components-games-panel-team` (permite encolher de fato para 95px) e `display: block; width: 100%; overflow: hidden; text-overflow: ellipsis;` em `.components-games-panel-team span`, valendo tanto para "partida finalizada" quanto "próxima partida" (estilos compartilhados).
  - `MatchCard` em `GamesPanel.tsx` agora também define `title` com o nome completo em cada span de time, para ficar disponível ao passar o mouse mesmo com o texto truncado.

- **Painel "jogos" da Home: Nome Correto do Clube**:
  - `GamesPanel.tsx` tinha "ACF Sport Club" fixo como nome do time da casa — resquício do mock estático original, divergente do nome real do clube usado no restante do código (`ACF Sports/Vila Mercado`, ex.: `CLUB_NAME` em `src/data/news.ts` e os itens `team` dentro de `competitions.table`).
  - Adicionada a constante `CLUB_NAME = "ACF Sports/Vila Mercado"` em `GamesPanel.tsx`, usada no nome do time da casa nos dois cards.

- **Painel "jogos" da Home: Text-Overflow na Faixa de Data/Competição**:
  - `.components-games-panel-matchDate` (a faixa laranja/branca com a data, ex.: "02/09/2026 - COPA SESC FUTSAL ADULTO / Ginásio Pedro Mariucci") tinha `white-space: nowrap` sem truncamento; textos longos de competição/local eram simplesmente cortados pelo `overflow: hidden` do card pai, no meio da palavra e sem reticências.
  - Adicionado `overflow: hidden; text-overflow: ellipsis;` em `.components-games-panel-matchDate` no `globals.css`.

- **Painel "jogos" da Home: Local Movido para Dentro do Card**:
  - `formatGameDate()` (`src/data/games.ts`) não inclui mais o `location` na string da faixa de data/competição no topo do card — só data + competição. Nova função `formatGameTime()` extrai só o horário (`HH:mm`) de `game.date`, usada na página de competições.
  - `MatchCard` em `GamesPanel.tsx` ganhou a prop `location` e agora renderiza o local do jogo dentro do card, centralizado, logo abaixo da linha dos times/placar (antes ficava embutido na faixa do topo).
  - `globals.css`: `.components-games-panel-matchBody` virou um container flex-column (a antiga disposição em linha dos times/placar foi extraída para `.components-games-panel-matchTeams`); nova classe `.components-games-panel-matchLocation` estiliza o texto do local; a altura fixa dos cards (`.components-games-panel-matchCard` / `.components-games-panel-upcomingCard`) aumentou de 116px/136px para 140px/160px para acomodar a linha extra.

- **Página de Notícias: Notícia Fixada Ausente do Arquivo Completo**:
  - `NewsArchive.tsx` (`/noticias`) agora filtra `PINNED_CAROUSEL_NEWS_ID` da lista, igual ao que `getLatestNews()` já fazia para o grid da home — a notícia fixada só aparece no carousel da home, nunca junto das demais no arquivo.

- **Página de Notícias: Paginação Condicional**:
  - A navegação de paginação (números de página + setas) em `NewsArchive.tsx` só é renderizada quando há mais de 6 notícias (após excluir a fixada); os números de página agora são gerados dinamicamente (`Math.ceil(newsItems.length / 6)`) em vez do array fixo `[1, 2, 3, 4, 5, 6]`.

- **Página de Competições: Card "Próxima Partida" Conectado ao Banco**:
  - `src/app/clube/competicoes/page.tsx` virou um Server Component assíncrono que busca `getNextUpcomingGame()` (mesma fonte usada pelo painel "jogos" da home) e passa os dados formatados como prop `nextGame` para `CompetitionsContent`; ganhou `export const revalidate = 60`, igual à home.
  - `CompetitionsContent.tsx` não usa mais o mock estático `nextMatchData` — monta o card e o modal de detalhes a partir da prop `nextGame` (times, competição, data, hora, local), e mostra "Nenhuma partida agendada no momento." quando não há próxima partida no banco. A lista "Partidas Anteriores" abaixo permanece com dados mock (fora do escopo desta mudança).

- **Rodapé: Contato e Redes Sociais Conectados à Tabela `team_info`**:
  - Adicionado `src/data/teamInfo.ts`, seguindo o mesmo padrão (`cache()` + fallback em erro) de `games.ts`/`news.ts`, buscando `facebook`, `instagram`, `youtube`, `phone`, `email`, `address` e `symbol` da tabela `team_info` (linha única).
  - `SiteFooter.tsx` virou um componente de servidor assíncrono que usa `getTeamInfo()` para preencher os links de redes sociais, telefone, e-mail e endereço, no lugar dos valores fixos anteriores. Campos vazios no banco (ex.: `email`) caem no fallback estático anterior.

- **Página de Contato: Conectada à Tabela `team_info`**:
  - `src/app/contato/page.tsx` virou um Server Component assíncrono (`export const revalidate = 60`) que busca `getTeamInfo()` e passa como prop `teamInfo` para `ContactContent`.
  - `ContactContent.tsx` (client component) recebe `teamInfo: TeamInfo` e monta `contactItems` (telefone, e-mail, endereço) e os links de redes sociais (Instagram/Facebook/YouTube) a partir dela, no lugar dos valores fixos anteriores. O campo "Atendimento" continua estático — não existe coluna correspondente em `team_info`. O link do WhatsApp agora é derivado do `teamInfo.phone` (nova função `toWhatsappNumber()`: extrai só os dígitos e prefixa `55` quando o número tem 11 dígitos, ou seja, sem código de país).

- **Carousel da Home: Notícia Fixada Sem Overlay de Texto**:
  - Quando o slide ativo do carousel é a notícia fixada (`PINNED_CAROUSEL_NEWS_ID`, usada como banner/anúncio — ex.: a notícia "Patrocinadores"), `HeroNews.tsx` não renderiza mais o `storyCard` (categoria, título e resumo sobrepostos à imagem), deixando só a imagem de fundo visível. Para as demais notícias do carousel o comportamento não muda.
  - `MatchCard` em `GamesPanel.tsx` agora também define `title` com o texto completo da faixa de data, mesmo padrão já usado nos nomes dos times.

- **Carousel da Home: Notícia Fixada Sempre na 4ª Posição**:
  - `buildCarouselSlides()` em `HeroNews.tsx` inseria a notícia fixada (`PINNED_CAROUSEL_NEWS_ID`) na 3ª posição (`Math.min(2, slides.length)`); ajustado para `Math.min(3, slides.length)`, garantindo que ela sempre ocupe a 4ª posição do carousel quando houver slides suficientes.

- **Painel "tabelas" da Home: Conectado à Tabela `competitions`**:
  - Adicionado `src/data/competitions.ts` (padrão `cache()` + fallback em erro, igual a `games.ts`/`teamInfo.ts`), com `getHomeCompetitions()` buscando em `competitions` apenas as linhas com `home_page = true`, ordenadas por `updated_at` desc; a coluna jsonb `table` é normalizada e ordenada por posição.
  - `StandingsPanel.tsx` virou um Server Component assíncrono que busca `getHomeCompetitions()` e repassa para o novo `StandingsPanelClient.tsx` (client component), que renderiza a tabela de classificação real (posição, clube, pontos, jogos, saldo de gols, vitórias, derrotas) e mostra "Nenhuma tabela disponível no momento." quando nenhuma competição está marcada para a home. Quando há mais de uma competição com `home_page = true`, o botão com `ChevronDown` (antes decorativo) agora abre um dropdown funcional para trocar entre elas — mesmo padrão visual do dropdown em `CompetitionsContent.tsx`.
  - `globals.css`: `.components-standings-panel-tableCard` passou a `position: relative; overflow: visible` (antes `overflow: hidden`) para não cortar o dropdown; novas classes `.components-standings-panel-dropdownMenu`, `.components-standings-panel-dropdownItem` e `.components-standings-panel-empty`.

- **Painel "tabelas" da Home: Tabela Quebrada com Nomes Reais de Clube**:
  - Com dados reais (ex.: "Cyber Futsal/Arena Cassimiro"), a tabela colapsava: `table-layout` (padrão `auto`) redistribuía a largura das colunas com base no conteúdo, e a coluna do clube (`white-space: nowrap`, sem truncamento) forçava as colunas numéricas (Pts/Jgs/Sgs/Vit/De) a espremerem e sobrepor o texto — inclusive no cabeçalho.
  - Corrigido em `globals.css`: `.components-standings-panel-table` ganhou `table-layout: fixed`, com `.components-standings-panel-clubColumn` fixada em `40%` e as demais colunas em `12%` cada (soma 100%); `.components-standings-panel-clubColumn` ganhou `overflow: hidden; text-overflow: ellipsis` para truncar nomes longos em vez de estourar a célula.
  - A altura fixa do card (`316px`, que sobrava vazia para competições com poucos times) virou altura automática; a tabela agora fica dentro de `.components-standings-panel-tableScroll` (nova classe, `max-height: 280px; overflow-y: auto`) com cabeçalho `position: sticky`, então competições com muitos times (ex.: 8) ganham rolagem interna em vez de esticar o card.
  - `StandingsPanelClient.tsx` passou a envolver a `<table>` nesse novo wrapper e a adicionar `title={entry.team}` na célula do clube, para o nome completo aparecer ao passar o mouse quando truncado.

- **Página de Competições: Tabelas de Classificação Conectadas ao Banco**:
  - Adicionado `getAllCompetitions()` em `src/data/competitions.ts`, buscando todas as linhas de `competitions` ordenadas por `home_page DESC, updated_at DESC` — a competição marcada com `home_page = true` sempre vem primeiro no array (e, portanto, pré-selecionada na tabela).
  - `src/app/clube/competicoes/page.tsx` agora busca `getAllCompetitions()` junto com `getNextUpcomingGame()` e passa o resultado como prop `competitions` para `CompetitionsContent`.
  - `CompetitionsContent.tsx` não usa mais os mocks estáticos `competitionOptions`/`standingsData` — o dropdown de competições e a tabela de classificação são montados a partir da prop `competitions` (mesmo shape `HomeCompetition`/`StandingEntry` usado no painel da home); o dropdown fica desabilitado quando há só uma competição, e é exibido "Nenhuma tabela disponível no momento." quando o banco não retorna nenhuma.

- **Página de Competições: "Partidas Anteriores" Conectada ao Banco**:
  - Adicionado `getPreviousGames()` em `src/data/games.ts`, reaproveitando o mesmo filtro `isFinished` já usado por `getLatestFinishedGame()` (jogo é considerado finalizado quando `result` não é vazio nem `"-"`), retornando todos os jogos finalizados ordenados do mais recente para o mais antigo (em vez de só o último).
  - `src/app/clube/competicoes/page.tsx` agora também busca `getPreviousGames()` e monta a prop `previousMatches` (tipo `PreviousMatchData`, exportado por `CompetitionsContent.tsx`) com o mesmo padrão de formatação já usado para `nextGame` (`formatGameDate`/`formatGameTime`, logo do adversário via `competition.table`).
  - `CompetitionsContent.tsx` removeu o mock estático `previousMatchesData` — a lista "Partidas Anteriores" é montada a partir da prop `previousMatches` e só aparece quando há pelo menos um jogo finalizado cadastrado no banco; caso contrário mostra "Nenhuma partida anterior registrada no momento." (mesmo padrão das outras seções). Como a tabela `games` não tem um placar estruturado por lado (só a coluna livre `result`, ex.: usada como "3 x 1" no painel "jogos" da home), o tipo `MatchDetail` trocou os campos `homeTeam.score`/`awayTeam.score` (números fixos do mock) por um único campo `result: string`, exibido tal como vem do banco tanto no card quanto no modal de detalhes. Os campos `highlights`/`referee` do mock (sem coluna correspondente no banco) foram removidos, junto com o bloco "Observações da Partida" no modal que os exibia.

- **Carousel da Home: Copy dos Slides Redesenhada para Bater com a Referência do Figma**:
  - `HeroNews.tsx`: os slides normais agora mostram só o título da notícia (categoria/resumo removidos), alinhado à esquerda na base do stage, em uma caixa com largura limitada (`max-width: min(640px, calc(100% - 480px))` para nunca colidir com a faixa de miniaturas), sobre um novo gradiente `.components-hero-news-scrim` na base para garantir legibilidade sobre qualquer imagem de fundo.
  - O slide fixado (`PINNED_CAROUSEL_NEWS_ID`, o banner de patrocinadores) agora usa o próprio título vindo do banco (ex.: "seja um de nossos patrocinadores.") empilhado acima de um novo botão `.components-hero-news-ctaButton` ("Ver Planos ↗") que leva para `/clube/patrocinadores`, com o mesmo gradiente laranja já usado no site (`linear-gradient(165deg, #ff3203 0%, #8f301d 99%)`) e raio em formato de pílula.
  - Corrigida uma regressão anterior em que o título usava `font-size: 4rem` (64px) centralizado ocupando quase toda a largura do stage, o que dominava visualmente a imagem de fundo; o título agora é 32px/38px (alinhado à esquerda), reduzindo ainda mais nos breakpoints `768px`/`576px`.

- **Corpo da Notícia: Links Internos Agora Têm Cor e Hover**:
  - Adicionado `.components-news-detail-content a` no `globals.css`: links inseridos no HTML rico de `news.content` (renderizado via `dangerouslySetInnerHTML` em `NewsDetail.tsx`) agora aparecem na cor laranja da marca (`var(--laranja)`, `#ff3203`) com sublinhado, e escurecem para `#8f301d` no hover, com transição suave de cor. Antes esses links não tinham cor nem destaque próprios, herdando o estilo padrão do parágrafo ao redor.

- **Seção "planos" da Home: Cores Corrigidas Conforme o Design System do Figma**:
  - Comparado `PlansSection.tsx`/`globals.css` com um design system exportado do Figma para o frame "planos"; a maior parte das cores e tipografia já batia exatamente. Corrigidas duas divergências confirmadas: a cor do título e do preço no card em destaque ("master") era `#7a210f`, corrigida para `#661400` (token `cor do acf sport/L-18` do design system); o gradiente do botão do card em destaque usava o gradiente padrão do site (`#ff3203 → #8f301d`), corrigido para o gradiente específico deste componente no design system (`#ff7a5c → #8f1d00`).

- **Lote de Correções (`improvements.md`)**:
  - **Notícias relacionadas / paginação**: `getRelatedNews()` (`src/data/news.ts`) agora exclui `PINNED_CAROUSEL_NEWS_ID` além da notícia atual, então o banner de patrocinadores nunca aparece em "Mais notícias sobre o ACF". `NewsDetail.tsx` pagina essa lista de 4 em 4 e só mostra a navegação de páginas quando há mais de 4 itens elegíveis.
  - **Paginação funcional de verdade**: tanto a paginação de relacionadas do `NewsDetail.tsx` quanto a do `NewsArchive.tsx` (`/noticias`) eram decorativas (números fixos, todos os links apontando pra `/noticias` com a página 1 sempre marcada como ativa). Agora ambas leem uma prop `page` (vinda do `searchParams` da rota), fatiam a lista de itens de acordo, geram links reais `?page=N`, e escondem as setas de próxima/última página ao chegar no fim. `/noticias` e `/noticias/[id]` passaram a ser rotas dinâmicas por causa disso (dependem de `searchParams`).
  - **Logo do header conectado ao `team_info`**: adicionado `src/components/SiteHeader.tsx`, um server component assíncrono que busca `getTeamInfo()` e renderiza o `MainMenu` com uma nova prop `logoSrc` (`MainMenu.tsx`, com fallback pro asset estático atual). Troquei `<MainMenu />` por `<SiteHeader />` (e o import correspondente) nas 9 páginas que renderizam o header.
  - **Faixa de patrocinadores escondida por flag**: adicionado `SPONSORS_STRIP_ENABLED = false` dentro do próprio `SponsorsStrip.tsx`, que agora retorna `null` quando desabilitado — esconde a faixa/carrossel de logos de patrocinadores em todas as páginas sem apagar nada do código. A página dedicada `/clube/patrocinadores` não é afetada.
  - **Correção de espaçamento na seção "elenco"**: `.components-roster-section-section` tinha `min-height: 1048px` fixo (também duplicado como estilo inline em `RosterSection.tsx`), deixando um vão vazio grande abaixo do botão "ver elenco completo" sempre que o conteúdo real era mais baixo que essa altura. Removido o `min-height` fixo (a regra CSS, seu override no breakpoint `768px`, e o estilo inline) para a seção se ajustar ao conteúdo; o padding inferior de `.components-roster-section-inner` ajustado para `40px` no desktop / `32px` no mobile, batendo com o token de espaçamento `lg` do design system.
  - **Correção do gradiente na seção YouTube**: `.components-youtube-section-button` ("inscreva-se no canal do youtube") usava o gradiente padrão do site (`#ff3203 → #8f301d`); corrigido para `#ff7a5c → #8f1d00`, o gradiente exato documentado pra esse botão no design system exportado do Figma (tipografia, demais cores e a sombra do card já batiam com precisão).

- **Atualização de Assets**:
  - Substituídos `public/footer/acf-footer-logo.png`, `public/footer/sovereign-footer.png` e `public/youtube-section/youtube-bull.png` por exports atualizados (sem mudanças de código).

- **Notícias: Corrigido Fuso Horário na Exibição da Data**:
  - `formatNewsDate()` (`src/data/news.ts`) formatava a data com `Intl.DateTimeFormat("pt-BR")` sem especificar `timeZone`, então o resultado dependia do fuso do ambiente de execução. Em produção (runtime em UTC), notícias criadas à noite no horário de Brasília apareciam com a data do dia seguinte (UTC), divergindo da data real armazenada no banco. Corrigido fixando `timeZone: "America/Sao_Paulo"` nas duas chamadas de formatação.

- **Página "Nossa História": Conectada à Tabela `team_history`**:
  - Adicionado `src/data/teamHistory.ts` (`getTeamHistory()`), no mesmo padrão de `teamInfo.ts`: busca a linha mais recente de `team_history` (`title`, `content`, `symbol`, `mascot`, `content_image`, `mascot_images`, `created_at`), com fallback estático apenas para `title`/`content`/`content_image` (o núcleo da página).
  - `src/app/clube/historia/page.tsx` virou um Server Component assíncrono (`export const revalidate = 60`) que busca `getTeamHistory()` e passa como prop `history` para `HistoryContent.tsx`, que deixou de ter texto/imagens hardcoded — `content`/`mascot`/`symbol` são HTML vindo do banco, renderizado via `dangerouslySetInnerHTML` (mesmo padrão de `NewsDetail.tsx`).
  - As seções "símbolos" e "mascote" agora só renderizam quando há dado correspondente no banco (`history.symbol` não vazio; `history.mascot` não vazio ou `history.mascotImages` com pelo menos um item) — sem fallback estático, para não mostrar conteúdo de exemplo quando o CMS ainda não preencheu esses campos.
  - O texto do artigo (`.components-history-content-articleText`) teve o `text-indent` removido e a tipografia ajustada para bater com o corpo de texto de notícias (`.components-news-detail-body`: `24px`/`40px`, cor `#4e4e4e`, alinhado à esquerda).

- **Página de Competições: Legenda de Classificação/Rebaixamento Removida**:
  - Removido o bloco de legenda ("Classificados para próxima fase" / "Rebaixados para a segunda divisão") abaixo da tabela de classificação em `CompetitionsContent.tsx`, a pedido do usuário.

- **Logo do Clube nos Cards de Partida Vinda do Banco**:
  - `TeamBadge` (`CompetitionsContent.tsx`) ganhou a prop `home`, aplicando um destaque visual (fundo circular escuro) apenas no brasão do time da casa. `CompetitionsContent` e `GamesPanel.tsx` agora recebem/buscam `teamInfo.symbol` (via `getTeamInfo()`) e usam esse valor como logo do ACF nos cards de "Próxima Partida"/"Partidas Anteriores"/painel "jogos", com fallback para `/header/symbol.png` quando o banco não tem o campo preenchido.

- **Jogos: Horário Separado da Data**:
  - `src/data/games.ts` passou a selecionar também a coluna `time` da tabela `games` (campo `GameItem.time`, `string | null`), separada de `date`. A ordenação por data mais recente/próxima (`gameDateTime`) agora compara strings `"YYYY-MM-DD HH:MM"` diretamente, em vez de converter para `Date`/timestamp — evita ambiguidade de fuso horário na comparação.

- **Página de Patrocinadores: Cards de Plano Redesenhados**:
  - `SponsorsPageContent.tsx` ganhou as flags `SHOW_PARTNERS`/`SHOW_ONE_OFFS` (ambas `false`) para esconder os grupos "Parceiros" e "Pontuais" sem apagar os dados.
  - `PlanCard` agora separa o preço em valor e período (`plan.price.split(" / ")`), renderizados em `<span>`s distintos (`.components-sponsors-page-content-planPriceAmount`/`...PricePeriod`), e ganhou um divisor (`.components-sponsors-page-content-planDivider`) abaixo do título. O botão do WhatsApp deixou de usar as classes utilitárias do Tailwind e passou a ter estilo próprio no `globals.css`, com o ícone posicionado absolutamente.

- **Lote de Correções Responsivas (Header, Home Mobile, Telas Grandes)**:
  - **Submenu "Clube" invisível no mobile**: a regra `:hover`/`:focus-within` do dropdown (herdada do comportamento desktop) tinha especificidade maior que a versão mobile e continuava aplicando `transform: translateX(-50%)` no painel, empurrando os itens ("História", "Elenco", "Competições") pra fora da tela — por isso apareciam como linhas em branco ao abrir o menu no celular. Corrigido zerando `transform` nessas regras dentro do breakpoint `768px` em `globals.css`.
  - `TopCf.tsx`: removida a rotação automática entre os dois GIFs do topo; exibe sempre o primeiro item do array (`headerGifs[0]`).
  - **Home mobile — "jogos"**: a seção "tabelas" (classificação) agora fica com `display: none` abaixo de `768px`.
  - **Home mobile — "planos"**: cards convertidos em carrossel horizontal com scroll-snap (`overflow-x: auto; scroll-snap-type: x mandatory`) somente no breakpoint `768px`; desktop/tablet inalterados.
  - **Home mobile — "elenco"**: trocado de linha única com `overflow-x` (mostrava só 1 card e uma fatia do próximo) para grid de 2 colunas no breakpoint `768px`, exibindo sempre dois atletas por vez.
  - **Home mobile — "YouTube"**: `.components-youtube-section-section` tinha `height` fixo com `overflow: hidden`; no mobile o card interno ficava mais alto que essa altura e cortava o bloco final ("Rede ACF Soberano"). Adicionado um breakpoint `768px` com `height: auto` e o card em fluxo normal (`position: relative`) em vez de absoluto.
  - **Footer mobile**: coluna "Informações" (`.components-site-footer-column:first-child`) e o endereço (`.components-site-footer-address`/`.components-site-footer-addressDivider`, novas classes em `SiteFooter.tsx`) agora ficam ocultos abaixo de `768px`; telefone e e-mail continuam visíveis.
  - **Alinhamento/tamanho em telas grandes (ex.: 2560×842)**: `.components-main-menu-menu` usava `padding-inline: clamp(24px, 13.75vw, 220px)`, uma fórmula baseada em viewport sem relação com o container de `1440px` usado no resto do site — em telas largas o header ficava esticado e desalinhado com o conteúdo abaixo dele. Trocado para `max(24px, calc((100% - 1440px) / 2))`, que centraliza e limita o header exatamente como as demais seções. A mesma fórmula foi aplicada ao texto (`.components-hero-news-storyCard`) e às miniaturas (`.components-hero-news-thumbnails`) do carousel de notícias da home, que tinham inset fixo de `32px` a partir da borda da tela.
  - Validado com `npm run build` e com testes visuais via Chrome headless (Puppeteer) em várias larguras (390px, 2560px) simulando toque real no menu mobile, já que a extensão Claude in Chrome não conectou durante a sessão.

- **Segundo Lote de Melhorias Mobile (revisão manual pós-deploy)**:
  - **Home — "elenco" virou carrossel de verdade**: `RosterSection.tsx` agora agrupa os 5 atletas em slides de 2 (client component com `useRef`/`useState`), usando `scroll-snap-type: x mandatory` no breakpoint `768px` com bolinhas de navegação clicáveis (`.components-roster-section-dots`); no desktop/tablet o wrapper de slide vira `display: contents` e o layout original (5 cards numa linha) fica intacto.
  - **Home — botão "ver mais notícias" centralizado e alinhado com a tabela**: `.components-news-grid-action` trocou `justify-content: flex-end` + `padding-right: 116px` fixo por `justify-content: center`; `.app-page-contentRow` passou a `align-items: stretch` e `.components-news-grid-section` virou `display: flex; flex-direction: column` com `margin-top: auto` no botão, alinhando seu rodapé com a base da coluna lateral (jogos + tabelas). A tabela "tabelas." (`.components-standings-panel-tableCard`) ganhou padding interno (`padding: 0 12px`) para o conteúdo não colar nas bordas.
  - **Página de Competições: tabela sem borda**: `.components-competitions-content-tableBlock` (sem nenhum estilo próprio antes) ganhou borda, cantos arredondados e fundo cinza, no mesmo padrão visual da tabela "tabelas." da home.
  - **Página de Competições: totalmente sem responsividade mobile**: não existia nenhum breakpoint `768px` para `CompetitionsContent`; adicionado um bloco completo centralizando "Próxima Partida"/cards de "Partidas Anteriores" (que quebravam com colunas fixas em pixels) e mantendo a tabela rolável horizontalmente dentro da nova borda.
  - **Footer mobile: espaçamento excessivo no topo**: `.components-site-footer-columns` usava `padding-top: calc(64px + var(--main-menu-height))` (`160px` no total) no breakpoint `768px`, criando um vão grande antes de "MÍDIAS SOCIAIS"; reduzido para `40px` fixo (a compensação pela altura do header fixo já é feita separadamente via `scroll-margin-top`, não precisa duplicar no padding visual).
  - **Tamanhos de fonte mobile ajustados por referência visual**: título do destaque (`.components-hero-news-title`) e "notícias." (`.components-news-grid-title`) para `32px`; títulos do card do YouTube (`.components-youtube-section-intro h2`/`.components-youtube-section-ctaBlock h3`) para `24px` e parágrafos para `16px`; "Soberano TV"/telefone/e-mail no rodapé (`.components-site-footer-acfTv`/`...menu a`/`...contact p`) para `16px`.
  - **Painel "jogos" da home no mobile**: símbolos dos times (`.components-games-panel-team img`) e o nome do time (`.components-games-panel-team span`) aumentados (`48px→56/64px`, fonte `14px`); altura dos cards (`.components-games-panel-matchCard`/`...upcomingCard`) ajustada para não cortar o texto de local do jogo.
  - **Nome do time nas notícias menos berrante**: `.components-news-detail-clubName` trocou o laranja puro (`--laranja`, `#ff3203`) em negrito por um tom mais suave (`#e0673f`) sem negrito.
  - **Página de Patrocinadores: cards de plano em carrossel no mobile**: `.components-sponsors-page-content-planGrid` vira `grid-auto-flow: column` com scroll-snap somente no breakpoint `768px` (antes empilhava verticalmente); desktop/tablet sem mudanças.
  - **Página de Contato: formulário movido para o final no mobile**: `ContactContent.tsx` reestruturado — o bloco do formulário saiu de dentro da coluna esquerda e virou irmão direto de `.components-contact-content-planPanel`; `.components-contact-content-inner` usa `grid-template-areas` (`"left plan" "form plan"` no desktop, `"left" "plan" "form"` empilhado no mobile) para reordenar visualmente sem depender da ordem do DOM.
  - **Página de Contato: cards de plano em carrossel no mobile**: adicionado `.components-contact-content-planCarousel` (todos os 3 planos, scroll-snap) exibido só abaixo de `768px`; as abas + card único existentes (`.components-contact-content-planTabs`/`...planCardDesktop`) continuam sendo o comportamento em telas maiores.
  - **Bug de overflow horizontal corrigido (Contato e Competições mobile)**: itens de grid (`.components-contact-content-leftColumn`/`...planPanel`/`...formBlock`, `.components-competitions-content-previous`/`...tableBlock`) não tinham `min-width: 0`; o texto do card de endereço (não quebrando por causa do tamanho mínimo automático de item de grid) forçava a coluna inteira a ficar mais larga que a tela no mobile, criando barra de rolagem horizontal. Corrigido explicitando `min-width: 0` nesses itens.
  - Validado com `npm run build` e testes visuais reais via Claude in Chrome em `390px`/`500px` de largura (elenco, competições, contato, patrocinadores, notícia, footer).

- **Terceiro Lote de Melhorias Mobile (revisão manual pós-deploy)**:
  - **Título do carousel da home reduzido**: `.components-hero-news-title` em mobile (breakpoints `992px`/`576px`) reduzido de `32px` para `20px`, ajuste solicitado após ver o resultado do lote anterior em dispositivo real.
  - **Ícone com cor errada no botão "Ver Planos"**: `HeroNews.tsx` usava o caractere Unicode `↗` solto (`<span>↗</span>`), que o iOS renderiza com a fonte emoji colorida em vez de herdar a cor do texto do botão. Trocado pelo ícone `ArrowUpRight` do `lucide-react`, mesmo padrão usado nos demais botões do site.
  - **Botão "inscreva-se no canal do youtube" quebrando feio no mobile**: `.components-youtube-section-button` tinha `height: 56px` fixo sem padding extra; o texto longo quebrava em duas linhas e estourava a altura fixa, deixando o ícone desalinhado. Adicionado `height: auto; min-height: 52px` com padding e fonte reduzida (`14px`) no breakpoint `768px`.
  - **Header com padding interno grande no mobile**: `.components-main-menu-menu` no breakpoint `768px` reduzido de `padding-inline: 20px` (com `16px` top/bottom herdado do desktop) para `padding: 8px 20px`; logo reduzida de `52×68px` para `44×58px`.
  - **"ver mais" do painel "jogos" (home) realinhado**: o link estava dentro do `<header>` ao lado do título "jogos."; movido para fora, virando um item irmão renderizado depois de `.components-games-panel-blocks` (`GamesPanel.tsx`) — no desktop continua posicionado absolutamente no canto superior direito (igual antes), no mobile (`768px`) passa a ficar estático, alinhado à direita, no rodapé do painel. Cor mantida em `var(--cinza-medio)` (`#959595`), que já era o valor usado.
  - **Página de Competições: título e cards de time quebrados**: título trocado de "tabelas & jogos" para "competições" em `src/app/clube/competicoes/page.tsx`. A `.components-competitions-content-matchTeams` no mobile usava `flex-wrap: wrap`, que quebrava a badge do time e o "X" para linhas separadas com nomes de time longos "vazando" para fora do card. Trocado para `flex-wrap: nowrap` com `.components-competitions-content-team` limitado (`max-width: 120px`, `flex: 1 1 0`) e `text-overflow: ellipsis` no nome do time, mantendo tudo em uma única linha.
  - **Consolidação de regras de CSS conflitantes na página de Competições**: existia um segundo bloco `@media` mais antigo (não visto no lote anterior) redefinindo `.matchTeams`/`.previousCard` mais adiante no `globals.css`, que — por vir depois no arquivo — sobrescrevia parte do bloco mobile mais novo (e mantinha `.cardSeparator`/`.previousMeta` com `display: none` herdado de uma regra de tablet `≤1200px`). Removido o bloco `768px` duplicado e isolada a regra de ocultação desses elementos para o intervalo `769px–1200px` (só tablet), evitando que ela afete o mobile.
  - **Espaçamento excessivo no topo da página de Contato**: `.app-contato-page-heading` no mobile reduzido de `min-height: 144px` para `110px`; `.components-contact-content-section` com `padding-top` reduzido de `56px` para `32px`; gap entre o título e o card de informações (`.components-contact-content-leftColumn`) reduzido de `56px` para `28px`.
  - **Card externo duplicado no carrossel de planos do Contato**: `.components-contact-content-planPanel` no mobile tinha fundo branco, borda e sombra próprios, envolvendo os cards do carrossel (que já têm o mesmo tratamento visual individualmente) — resultado era um "card dentro de card". Removido o fundo/borda/sombra do painel no breakpoint `768px`, mantendo apenas o título e o carrossel.
  - Validado com `npm run build` e testes visuais reais via Claude in Chrome em `420px`/`500px` de largura (home, competições, contato).

- **Atualização de Assets: GIF do Header**:
  - Substituído `public/gif/acf-na-veia.gif` por `public/gif/cf_gif.gif` como primeiro item de `headerGifs` em `TopCf.tsx`; `public/gif/25-anos.gif` atualizado com uma nova versão exportada. `public/gif/header.gif` (não referenciado em nenhum componente) removido.

- **Correções de Modo Desktop (a partir de `improvements.md`)**:
  - **Cards de plano do Patrocinadores com alturas desiguais**: `.components-sponsors-page-content-planGrid` usava `align-items: start`, então o card MASTER (texto mais curto) ficava mais baixo que PONTUAL/MENSAL e o preço/botão desalinhavam. Trocado para `align-items: stretch`.
  - **Página de Contato: painel "Nossos Planos" desalinhado com o card de informações**: o bloco de título "CONTATE-NOS" estava aninhado dentro da coluna esquerda, então o painel de planos (que ocupa as duas linhas do grid) alinhava o topo com o título, não com o card — ficando mais alto que o esperado. Título movido para fora da coluna esquerda como item próprio do grid (`grid-template-areas` com linha "title" no topo).
  - **Tabela de classificação da página Competições vazando da borda direita**: `.components-competitions-content-tableBlock` não tinha `min-width: 0`; como o `<table>` interno tem `min-width: 500px` não encolhível, o card crescia além da largura da sua coluna no grid. Corrigido com `min-width: 0`.
  - **Largura do conteúdo desalinhada com o menu**: as faixas de título ("CLUBE / competições.", "SOCIAL / contato.", etc., 8 páginas) e o conteúdo da página Competições usavam `width: min(1160px, ...)`, mais estreito que o menu (~1440px). Alterado para `min(1440px, ...)`, replicando o padrão já usado na home. Escopo intencionalmente limitado a essas faixas + Competições; outras seções com grids de colunas fixas (rodapé, cards de elenco) não foram alteradas para não abrir vãos vazios.
  - **Título do carousel da home em 3 linhas no desktop**: `.components-hero-news-title` ganhou `-webkit-line-clamp: 2` a partir de `993px` (desktop), sem alterar o comportamento mobile.
  - **Espaçamento excessivo no topo do rodapé**: `.components-site-footer-columns` tinha `padding-top: calc(120px + var(--main-menu-height))` (~216px) por engano, duplicando a compensação de altura do header fixo (já feita via `scroll-margin-top`). Reduzido para `120px`.
  - **Botão "ENVIAR DIRECT NO WHATSAPP" totalmente fora do padrão do Figma**: removido o ícone do WhatsApp, `border-radius` trocado de formato pílula para `10px`, e texto ajustado para uma linha (`Patrocinadores` e `Contato`).
  - **Bug de cascata CSS causando texto cinza em vez de branco no botão**: `a { color: inherit; }` estava declarado **fora** de `@layer components` (que envolve o resto do `globals.css`); por spec de CSS Cascade Layers, regra sem layer sempre vence regra com layer independente da especificidade, então esse reset genérico anulava silenciosamente qualquer `color` customizado em links no site inteiro. Corrigido movendo os resets de `a`/`button` para dentro de `@layer components`.
  - Validado com `npm run build` e testes visuais via Claude in Chrome em `1440px` (todas as páginas afetadas) e `390px` (checagem de regressão mobile).

- **Hover dos cards de planos (Home/Patrocinadores) e espaçamento do Contato (a partir de `improvements.md`)**:
  - **Cards de planos de Patrocinadores ganharam o movimento de hover da home**: a primeira leitura do pedido saiu invertida (uniformizou alturas e removeu o salto no hover da Home). Intenção correta: os cards de "planos" da Home (`.components-plans-section-card`) mantêm as alturas originais (diferentes entre PONTUAL/MENSAL `440px` e o card MASTER `...featuredCard` `516px`) e o salto no `:hover` (`translateY(-10px)`) sem alteração; os cards de Patrocinadores (`.components-sponsors-page-content-planCard`) ganharam o mesmo `transform: translateY(-10px)` com transição de sombra no `:hover`, para se moverem igual aos da Home.
  - **Espaço excessivo no topo do Contato, acima de "CONTATE-NOS"**: `.components-contact-content-section` com `padding-top` reduzido de `64px` para `32px` no desktop (o breakpoint `768px` já estava em `32px`).
  - **Painel "Nossos Planos" do Contato revertido para o alinhamento original**: uma primeira tentativa esticava `.components-contact-content-planPanel` (`display: flex; justify-content: center; align-self: stretch;`) para preencher a mesma altura da coluna esquerda, fechando o vão vazio abaixo do card — mas o card centralizado ao lado do formulário, bem mais alto, ficou pior visualmente do que o vão original, segundo o feedback. Revertido para a caixa com altura baseada apenas no próprio conteúdo, como estava antes.
  - Validado com `npm run build` e testes visuais via Claude in Chrome em `1440px` na Home (hover dos planos), Patrocinadores (hover) e Contato.

- **Card MASTER de Patrocinadores maior que os das pontas, igual à Home (a partir de `improvements.md`)**:
  - O plano `master` em `SponsorsPageContent.tsx` ganhou a flag `featured: true`, aplicada como `.components-sponsors-page-content-featuredPlanCard` no card.
  - **A primeira tentativa (`0.86fr 1fr 0.86fr`, `align-items: stretch`→`center`, `min-height: 566px`/`666px`) saiu ao contrário**: o texto dos benefícios de PONTUAL/MENSAL é longo (diferente das frases curtas da Home), então estreitar essas colunas fez o texto quebrar em mais linhas, e a altura *determinada pelo conteúdo* desses cards (até `764px`, medido) acabou maior que o piso de `666px` do MASTER — o oposto do pretendido. `align-items: center`/`stretch` no grid só dimensiona itens *dentro* de uma linha já alta o suficiente; não encolhe um card abaixo do que o próprio texto exige.
  - **Correção**: `.components-sponsors-page-content-planGrid` agora usa `grid-template-columns: 340px 400px 340px` fixo (larguras absolutas idênticas às da Home, com `justify-content: center` adicionado já que colunas fixas não preenchem mais a linha automaticamente) em vez de frações `fr`. Para garantir que a altura *real* renderizada de PONTUAL/MENSAL fique de forma confiável abaixo da do MASTER, a tipografia da lista de benefícios foi ajustada (`.components-sponsors-page-content-planCard li` de `20px/28px` para `18px/26px`; `gap` do `ul` de `32px` para `24px`) — medido: isso reduz a altura natural dos cards das pontas de ~708–764px para ~628px na largura de `340px`. O `min-height` de `.components-sponsors-page-content-featuredPlanCard` subiu de `666px` para `720px`, para ficar claramente mais alto (720 vs. 628, ~15%) e mais largo (400 vs. 340, ~18%) que os cards das pontas, reproduzindo a proporção da Home. Verificado com medições reais via `getBoundingClientRect()` no navegador, não só visualmente.
  - Restrito ao desktop: o breakpoint `≤1200px` já existente empilha o grid em uma coluna e reseta `min-height`/`width`/`padding` do `.planCard` com a mesma especificidade mais adiante na cascata (e o breakpoint `768px` já tinha seu próprio `font-size` menor para `li`), então mobile/tablet não são afetados.
  - Validado com `npm run build` e testes visuais via Claude in Chrome em Patrocinadores, comparando com a Home.

- **Diagnóstico de paridade com o Figma no desktop (relatado pelo designer do layout)**:
  - Comparando com dados reais do painel "Design" do Figma (sem Dev Mode/MCP disponível), quase todos os valores de espaçamento/largura da seção de notícias já batiam com o design (largura de conteúdo 1440px, cards 330px, gaps 40px/80px, sidebar 290px). O defeito real: `.components-news-grid-copy h3`/`p` não tinham limite de linhas, então títulos reais (mais longos que o texto de exemplo do Figma) quebravam em 3 linhas em vez das 2 linhas do design (card de 356px), esticando o card para 402px e desalinhando as linhas do grid. Corrigido com `-webkit-line-clamp: 2` (+ `display: -webkit-box`, `-webkit-box-orient: vertical`, `overflow: hidden`) em `h3` e `p`. Mesmo defeito encontrado e corrigido em `.components-news-archive-copy h2`/`p` (página `/noticias`). Cards de Patrocinadores e Elenco foram verificados e já tratam conteúdo de tamanho variável corretamente (sem alteração necessária).
  - **Bug separado, encontrado durante a investigação**: o dev server do Next.js às vezes passa a servir `_next/static/css/app/layout.css` como 404 depois de várias edições em `globals.css` (a pasta `.next/static/css/app/` fica vazia), fazendo a página renderizar sem nenhum estilo — fácil de confundir com um bug de layout real. Correção: `pkill -f "next dev" && rm -rf .next && npm run dev`. Documentado na skill `.claude/skills/figma-desktop-parity/SKILL.md`, junto com o fluxo completo de comparação com o Figma sem Dev Mode (prints do painel Design + Alt-hover).
  - Ainda não verificado: `components-standings-panel-tableCard`, `components-youtube-section-card`, `components-sponsors-strip-logoCard`, `components-contact-content-*Card`, `components-history-content-symbolCard`.
  - Validado com `npm run build` e medições reais via `getBoundingClientRect()` no Claude in Chrome, na largura de referência de 1600px (Home, `/noticias`, Patrocinadores, Elenco).

- **Conteúdo colado nas bordas entre 1200px–1480px de largura (relatado numa tela real de MacBook, 1470px)**:
  - Várias fileiras de cards têm largura fixa somando exatamente 1440px (o valor máximo do design), sem nenhum degrau intermediário entre o breakpoint de `1200px` e esse máximo. Os containers `min(1440px, calc(100% - 40px))` encolhem para uma margem fixa de `20px` de cada lado abaixo de `1480px` de tela — então qualquer fileira cujo total fixo seja exatamente `1440px` estoura o próprio container nessa faixa, ficando cortada rente à borda da tela pelo `overflow: hidden` da seção (sem barra de rolagem, sem aviso visual do corte).
  - Auditados todos os containers `min(1440px, ...)`/`min(1160px, ...)` e todas as fileiras/grids de largura fixa do `globals.css`. A maioria já é segura (usa unidade flexível, ou o total fixo já bate exatamente com um breakpoint existente: faixas de título, grid da Competições, planos da Home/Patrocinadores, grid do arquivo de notícias). Três pontos realmente tinham o problema:
    - **Carrossel "elenco." da Home** (`.components-roster-section-athlete`, 5×256px + 4×40px = 1440px): card trocado de `width/height` fixos para `width: min(256px, calc((100% - 160px) / 5)); aspect-ratio: 256/448`. Seguro porque o card é um `<svg viewBox="0 0 256 448">` — o desenho interno escala proporcionalmente com o tamanho renderizado, sem distorcer.
    - **Grid "notícias." da Home** (`.components-news-grid-grid`, 3×330px + 2×40px = 1070px): colunas trocadas para uma fórmula `min(330px, calc(...))` que encolhe junto com o espaço disponível; card e imagem passaram de `width: 330px` para `width: 100%` pra acompanhar.
    - **Listagem completa do elenco** (`/clube/elenco`, `.components-roster-roster-page-content-grid`, 5×256px + 4×40px = 1440px): **não** foi encolhido como os outros dois — o card usa `clip-path: path(...)` com coordenadas absolutas em pixel, que não escalam ao redimensionar o elemento (diferente do `viewBox` de um SVG); encolher o card distorceria o recorte hexagonal. Corrigido por breakpoint: adicionado `@media (max-width: 1480px)` caindo pra 4 colunas (1144px, seguro). Isso também corrigiu um **bug de CSS morto pré-existente e não relacionado**: dois blocos `@media (max-width: 1200px)` diferentes redefiniam as colunas desse grid (`repeat(4, 256px)` e, logo em seguida, `repeat(3, 256px)`) — mesma especificidade, mesma condição, então o primeiro nunca era aplicado de fato. Removida a duplicata.
    - **Padding do header** (`.components-main-menu-menu`) usava um piso de `24px`, diferente do piso de `20px` dos containers de conteúdo — um desalinhamento real de 4px entre a logo/menu e as seções abaixo, em qualquer tela abaixo de `1480px`. Unificado para `20px` nos dois.
  - Validado com `npm run build` e checagens reais de overflow (`scrollWidth` vs `clientWidth`) via Claude in Chrome em `1200px`, `1250px`, `1300px`, `1470px` (a largura real reportada), `1480px` e `1600px`, na Home e em `/clube/elenco`.

- **Header realinhado ao container de 1160px, confirmado pelo designer**:
  - A correção acima alinhou o header ao container de 1440px (mesmo das seções "notícias"/"elenco"). O designer revisou um print da seção "planos." (`.components-plans-section-inner`, `width: min(1160px, calc(100% - 40px))`, título encostado na borda do container, sem margem extra) e confirmou: **o header deve seguir esse container mais estreito de 1160px — as seções de conteúdo mais largas (notícias, elenco etc.) estão corretas como estão e não deveriam ser alteradas.**
  - `.components-main-menu-menu` mudou de `padding: 16px max(20px, calc((100% - 1440px) / 2))` para `padding: 16px max(20px, calc((100% - 1160px) / 2))`. Isso deixa o header intencionalmente mais estreito que as seções de 1440px abaixo dele (ex: em 1600px de tela, header com 220px de margem vs. 80px do conteúdo de notícias/elenco) — essa assimetria é o visual correto confirmado, não um desalinhamento a corrigir.
  - Verificado ao vivo: `.components-main-menu-logoLink` e `.components-plans-section-inner` agora têm exatamente os mesmos valores de `left`/`right` (`getBoundingClientRect()`) em todas as larguras testadas.

- **Footer realinhado ao header, mesmo container de 1160px**:
  - Pedido do designer: o footer deveria fechar a home nas mesmas bordas do header (já alinhado a 1160px). `.components-site-footer-columns` usava `max-width: 1600px; padding: 120px 220px 0` — um padding fixo de 220px que só batia com o header exatamente em 1600px de tela e, abaixo disso, desalinhava. Como as 3 colunas fixas + gaps também somam exatamente 1160px, esse container também estava sofrendo o mesmo bug de overflow silencioso (cortado pelo `overflow:hidden`) entre 1200-1600px — achado de bônus ao fazer essa mudança.
  - Trocado para `width: min(1160px, calc(100% - 40px)); margin: 0 auto; padding-top: 120px`, a mesma fórmula do header/`.components-plans-section-inner`. O botão "voltar ao topo" (`right`) também ajustado para `max(20px, calc((100% - 1160px) / 2))`. Os breakpoints existentes de `≤1200px` e `≤768px` não foram alterados.
  - Verificado ao vivo: logo/último link do menu e colunas/botão do footer com os mesmos valores de `left`/`right` em 1470px e 1600px; sem overflow no grid do footer.

- **Faixas de título de todas as páginas e conteúdo de Competições realinhados a 1160px; Contato já estava correto**:
  - QA do usuário encontrou mais dois desalinhamentos em relação ao header (já em 1160px): a faixa de título de todas as 8 páginas (Competições, Elenco listagem, Elenco detalhe do jogador, História, Patrocinadores, Contato, Notícias listagem, Notícias detalhe) ainda usava `width: min(1440px, calc(100% - 40px))`, e o conteúdo de Competições (`.components-competitions-content-inner`) também. Ambos trocados para `min(1160px, calc(100% - 40px))`.
  - **Contato não precisou de mudança** — `.components-contact-content-inner` já usava `min(1160px, ...)` antes de qualquer alteração desta sessão; só parecia desalinhado porque o header ainda mirava 1440px na época. Com o header em 1160px, Contato já bateu sozinho.
  - Antes de estreitar o container de Competições, auditado o layout interno de largura fixa quanto ao mesmo risco de overflow das correções anteriores: `.components-competitions-content-contentGrid` é `560px 1fr` (primeira coluna rígida, segunda flexível) e a tabela de classificação tem `min-width: 500px` (já corrigida numa sessão anterior contra vazamento da própria coluna). `560 + 80px de gap + 500 = 1140px`, cabe nos novos 1160px com só 20px de sobra — apertado, mas confirmado seguro empiricamente (sem overflow) em 1600px, 1200px e 850px.
  - Validado com `npm run build` e checagens reais de `getBoundingClientRect()`/overflow via Claude in Chrome em `/clube/competicoes` e `/contato`.

- **Checagem de regressão mobile (depois de todas as correções de paridade desktop acima)**:
  - Verificado overflow (`scrollWidth`/`clientWidth`) em ~500px de largura (o mínimo possível de redimensionar nesse setup de automação — não deu pra testar 375px exato, mas 500px já cobre todo o tier mobile `≤768px`) em todas as páginas/seções mexidas hoje: Home, `/clube/elenco`, `/clube/competicoes`, `/contato`, `/clube/historia`, `/clube/patrocinadores`, `/noticias`, `/noticias/[id]`, `/clube/elenco/[slug]`. Nenhum overflow horizontal de página, e nenhuma quebra nova rastreável às mudanças de hoje — todo `@media (max-width: 768px)` tocado pelo trabalho de hoje já substitui totalmente os valores desktop alterados.
  - **Dois problemas pré-existentes encontrados, confirmados sem relação com o trabalho de hoje** (via `git diff`/`git show HEAD` — nenhum commit desta sessão tocou esse código):
    1. `.components-site-footer-columns` em `≤768px`: o `grid-template-columns` computado resolve para `400px` em vez do `1fr` que o CSS pede, causando ~70px de overflow horizontal dentro do footer (contido pelo `overflow:hidden` da seção, não gera scrollbar de página, mas o footer mobile de coluna única não fica na largura correta). Investigado a fundo: descartado bug de cascata de `@layer` (as três regras candidatas estão confirmadamente na mesma layer, na ordem certa, e a regra de `≤768px` vence num elemento de teste isolado, até dentro do footer real); descartada a armadilha clássica do CSS Grid de tamanho mínimo intrínseco (medido cada filho, o maior é 190px, longe de 400px); descartado zoom/transform/iframe. A causa raiz não foi encontrada num tempo razoável.
    2. Faixa de título de `/clube/patrocinadores` em `≤768px`: o título "patrocinadores." (a palavra mais longa entre os 8 títulos de página) estoura a própria faixa em ~15px (`345px` renderizado vs `330px` do container) na fonte mobile de 42px — provavelmente só essa palavra específica sendo longa demais; os outros 7 títulos cabem bem na mesma largura/fonte.

- **Ícones do header (hambúrguer/Instagram) recuados no mobile, seguindo o mesmo princípio do desktop**:
  - QA do usuário: em `≤768px`, o botão hambúrguer e a linha de ícones (WhatsApp/Instagram) ficavam quase colados nas bordas (`padding: 8px 20px`), enquanto todo o conteúdo mobile se centraliza com `width: min(330px, calc(100% - 32px))` — em qualquer tela acima de 362px, essa margem do conteúdo é maior que os 20px fixos do header. Mesmo princípio do alinhamento header/1160px feito no desktop, agora pro mobile.
  - `.components-main-menu-menu` em `≤768px` mudou de `padding: 8px 20px` para `padding: 8px max(16px, calc((100% - 330px) / 2))` — algebricamente a mesma fórmula de margem que o padrão `min(330px, calc(100% - 32px))` do conteúdo produz.
  - Verificado ao vivo em 500px: hambúrguer e conteúdo (`.components-news-archive-grid`/`.components-site-footer-columns`) com o mesmo `left` (78px) e a linha de ícones batendo no mesmo `right` (408px) do conteúdo. Menu mobile abre/fecha normalmente.

- **Título da notícia em destaque (HeroNews) desktop alargado**:
  - Pedido do usuário: `.components-hero-news-storyCard` (o card de título sobre o card destacado, `HeroNews.tsx:56`) estava com `max-width: min(640px, calc(100% - 480px))`, um teto fixo de 640px que se aplica a qualquer tela acima de ~1120px — em monitores largos (1440px+) o título quebrava em 2 linhas ou era truncado pelo `-webkit-line-clamp: 2` mesmo havendo bastante espaço livre entre o texto e as miniaturas (`.components-hero-news-thumbnails`) à direita.
  - Alterado apenas o teto fixo, de `640px` para `760px` (`max-width: min(760px, calc(100% - 480px))`); a reserva de `480px` para as miniaturas foi mantida igual, então o comportamento em telas estreitas (993–1120px, onde essa reserva já era o fator limitante) não muda.
  - Verificado ao vivo via Claude in Chrome (medição de `getBoundingClientRect()` do card vs. das miniaturas): em ~1100px de largura o espaçamento fica idêntico ao original (nenhuma regressão); com o teto de 760px testado isoladamente, a folga entre o card e as miniaturas continua ampla (>200px) em telas largas. Confirmado também um comportamento pré-existente e não alterado por esta mudança: com 3 miniaturas, `.components-hero-news-storyCard` e `.components-hero-news-thumbnails` já se sobrepõem levemente (bounding box, sem overlap visual real hoje) em larguras entre ~993–1120px — a reserva fixa de 480px não escala com a quantidade de notícias em destaque; fora do escopo deste pedido.

- **Footer mobile: causa raiz do overflow de `≤768px` encontrada e corrigida (achado documentado numa sessão anterior, sem causa raiz na época)**:
  - O relato anterior media `.components-site-footer-columns` com `grid-template-columns` computado em `400px` em vez do `1fr` do CSS, ~70px de overflow interno — já tinha descartado bug de cascata `@layer` e a armadilha clássica de tamanho mínimo intrínseco do Grid.
  - Causa raiz real: `SiteFooter.tsx`, a seção "mídias sociais" (segunda coluna) carrega a classe `w-100` (`className="components-site-footer-column text-acf-gray w-100"`) — um resquício de convenção Bootstrap (largura 100%) que não faz nada nesse sentido aqui. Neste projeto Tailwind CSS v4, uma utilidade numérica sem escala definida (`w-100`) é resolvida pela escala dinâmica de espaçamento do v4 (`n × 0.25rem`), então `w-100` virou `width: 25rem` = **400px literais** — o mesmo número exato do bug relatado. Esse item, com largura própria de 400px, forçava a única trilha `1fr` do grid mobile a crescer para acomodá-lo (grid blowout via `min-width: auto` padrão dos itens de grid), estourando o container de 330px em ~70px.
  - `w-100` não era usado em nenhum outro lugar do código (`grep -rn "w-100\b" src` só retornava essa linha) e não tinha função no design mobile nem desktop — na verdade, ele já forçava a mesma coluna a ficar em 400px também no desktop, estourando a trilha de 176px do grid desktop de forma silenciosa (nunca reportado, mas confirmado e corrigido pelo mesmo fix).
  - Fix: removida a classe `w-100`, mantendo `text-acf-gray`. Verificado ao vivo via Claude in Chrome (iframe com largura controlada, contornando a limitação de redimensionar a janela real neste setup): em 500px, `grid-template-columns` computado passou a `330px` (igual ao container, sem overflow, `scrollWidth === clientWidth`); em 1440px (desktop), a segunda coluna agora renderiza exatamente nos `176px` da trilha do grid (antes: 400px, estourando).

- **Patrocinadores mobile: título "patrocinadores." estourava a faixa em `≤768px` (achado documentado numa sessão anterior)**:
  - Causa: única palavra sem espaço (mais o span "." colorido), `white-space: normal` não consegue quebrar uma palavra sem hífen — a fonte Montserrat 700 a 42px mede ~345px, mas o container mobile é `min(330px, calc(100% - 32px))` (chega a 288px em telas de 320px de largura real).
  - Fix aplicado apenas nesta página (`app-clube-patrocinadores-page-heading h1`, `≤768px`): `font-size` de `42px`→`34px` e `line-height` de `52px`→`42px` (mesma proporção ~1,24 do original). Valor escolhido empiricamente via Claude in Chrome testando o `scrollWidth` do `h1` contra o container em 320px, 360px, 375px, 390px, 500px e 768px — cabe com folga em todos, inclusive no menor caso real (320px de largura de tela).
  - As outras 7 faixas de título mobile (`font-size: 42px` / `line-height: 52px`) não foram tocadas — o problema era específico da palavra mais longa entre as 8.

- **`/clube/elenco` desktop: nova visualização "Ver por posições" agrupada, apenas para Futsal**:
  - Pedido do usuário, com medidas do Figma (frame `elenco_principal_posicao_futsal`, node `1480-3543`): a aba "Ver por posições" deve mostrar o elenco de Futsal dividido em grupos com título próprio — Goleiros, Fixos (pode ter mais de 2), Alas - Esq / Dir, Pivôs, nessa ordem, cada fileira em auto-layout horizontal alinhada à esquerda (não centralizada, não esticada), antes da seção "Comissão Técnica / Staff" que já existia. Escopo combinado com o usuário: só Futsal por enquanto, Futebol de Campo não foi tocado.
  - Investigação prévia (código, não Figma): `RosterPosition` só tinha valores de futebol de campo (`goleiro`/`defensor`/`meio-campo`/`atacante`) reaproveitados também para os atletas de futsal — dado de seed sem sentido para futsal (nunca existia "fixo"/"ala"/"pivô"), e o link "Ver por posições" apenas filtrava para uma única posição (`goleiro`), não agrupava nada.
  - `src/data/roster.ts`: `RosterPosition` ganhou `"fixo" | "ala" | "pivo"`; novo ciclo `futsalPositions` (`goleiro, fixo, ala, ala, pivo`) substituindo o ciclo de campo na geração dos 20 atletas de futsal (distribuição resultante: 4 goleiros, 4 fixos, 8 alas, 4 pivôs); novo `futsalPositionGroups` (ordem + rótulo de cada grupo) e helper `groupFutsalAthletesByPosition()`.
  - Novo query param `visualizacao` (`todas` padrão | `posicoes`), lido em `src/app/clube/elenco/page.tsx` e repassado a `RosterPageContent`. `RosterFilters.tsx`: o link "Ver por posições" agora aponta para `?modalidade=futsal&visualizacao=posicoes` apenas quando a categoria ativa é Futsal — para Futebol de Campo o comportamento antigo (filtra por `goleiro`) foi mantido intacto, sem tocar nesse fluxo.
  - `RosterPageContent.tsx`: quando `categoria===futsal && visualizacao===posicoes`, renderiza os grupos (`groupFutsalAthletesByPosition`) em vez do grid plano; senão, comportamento 100% inalterado (grid `repeat(5, 256px)` existente).
  - CSS novo (`globals.css`): `.components-roster-roster-page-content-positionGroups` (coluna, largura `min(1160px, 100%)`, mesma família de container já usada pela toolbar/hr/staff desta página), `-positionGroup h3` (título da seção) e `-positionRow` (`display:flex; flex-wrap:wrap; justify-content:flex-start; gap:40px`) — o card (`components-roster-athlete-card-card`, 256px fixo, usa `clip-path` com coordenadas absolutas) não foi encolhido/esticado, só reflow por `flex-wrap`, então nenhuma fileira faz overflow em nenhuma largura, incluindo o grupo "Alas" com 8 cards (quebra em 2 linhas sozinho, sem precisar de breakpoint dedicado).
  - Verificado ao vivo via Claude in Chrome: cards alinhados à esquerda (`firstCardLeft === rowRect.left`) em todos os 4 grupos; sem overflow (`scrollWidth === clientWidth`) em 1250px, 1470px, 1600px (faixa do bug de gap 1200–1480px descrito no início deste changelog) nem em 375px mobile; "Todas as posições" (Futsal) e a visualização de Futebol de Campo continuam exatamente como antes (grid plano de 20 cards, sem grupos).

- **`/clube/elenco`: dados reais do banco (`players`/`staff_members`), filtro "Todas as posições" removido, aviso para Futebol de Campo**:
  - Pedido do usuário: puxar o elenco e a comissão técnica do banco de dados (tabelas `players` e `staff_members`, mesmo Neon Postgres já usado por `news`/`team_info`), remover de vez o filtro "Todas as posições" (só resta "Ver por posições") e, como a modalidade `campo` ainda não tem nenhum jogador cadastrado, mostrar um aviso de "elenco ainda sendo montado" em vez de uma lista vazia ou de dados fake.
  - Novo módulo `src/data/players.ts` (padrão idêntico a `news.ts`/`teamInfo.ts`: `neon()` + `cache()` + fallback `[]` em caso de erro). `src/data/roster.ts` (mock) foi mantido intocado — ainda serve a página de detalhe do jogador (`/clube/elenco/[slug]`), que não fazia parte deste pedido; `futsalPositionGroups`/`groupFutsalAthletesByPosition`, adicionados na sessão anterior só para a versão mock do agrupamento, foram removidos de `roster.ts` por ficarem mortos após esta troca.
  - Schema real inspecionado diretamente no Postgres (via `information_schema`) antes de escrever qualquer query — **nenhum campo foi assumido**: `players` tem `id, modality, number, position, nickname, social_media, quote, name, birthday, dominant_foot` (sem `slug`, `image`, `height`, `weight`, `city`); `staff_members` tem `id, name, nickname, function` (sem `image`). `modality` é o enum `player_modality` (`campo`/`futsal`) — consulta real confirmou **0 linhas com `modality='campo'`** e 12 com `futsal`, batendo com o que o usuário avisou.
  - `position` no banco é texto livre digitado à mão, não um enum — dado real tem inconsistência (`"Pivo"`, `"pivô"`, `"Pivô"`, `"Goleiro"`, `"Ala"`, `"Alal"` — typo de "Ala" —, `"Fixo"`, `"Ala/Fixo"` — posição composta). `groupPlayersByPosition()` normaliza via `NFD` + remoção de acento + `toLowerCase()` + casamento por substring (`"gol"→goleiro`, `"piv"→pivo`, `"fix"→fixo`, `"ala"→ala`) e usa só o primeiro valor antes de `"/"` num campo composto; testado contra as 12 linhas reais da tabela (script Node ad-hoc), as 12 caem certinho num dos 4 grupos, incluindo o typo e o valor composto. Um jogador com posição não reconhecida cairia num grupo `"Outros"` (não acontece hoje, mas evita sumir silenciosamente um cadastro futuro mal digitado).
  - `RosterFilters.tsx`: removida por completo a `nav` "Todas as posições / Ver por posições" — só restam as duas abas de modalidade (Futsal / Futebol de Campo). `RosterPageContent.tsx` passou a ser `async` e sempre renderiza agrupado (sem mais o parâmetro `visualizacao`); `page.tsx` simplificado para só ler `modalidade` (novo default: `futsal`, já que é a única com conteúdo real hoje — antes o default era `campo`).
  - Quando `groupPlayersByPosition()` retorna vazio (hoje, só acontece com `modalidade=campo`), a página mostra `"Elenco de {modalidade} ainda sendo montado. Em breve, mais novidades por aqui."` em vez de qualquer grid — texto e categoria montados dinamicamente, então também funciona sozinho se `futsal` ficar vazio no futuro. A seção "Comissão Técnica / Staff" não depende da modalidade (é a mesma equipe para o clube todo) e continua aparecendo mesmo na aba sem jogadores.
  - `AthleteCard.tsx` foi simplificado: como só é usado por esta página (confirmado via `grep`, o componente de mesmo nome em `RosterSection.tsx` da Home é outro arquivo/tipo, sem relação), passou a aceitar diretamente o formato vindo do banco (`{id, name, number, positionLabel}` / `{id, name, role}`) em vez do tipo mock `Athlete | StaffMember`. **Os cards deixaram de ser links** para `/clube/elenco/[slug]` — o banco não tem `slug` nem os dados da página de detalhe (foto real, altura, peso, frase, "related players"), então linkar geraria 404 ou obrigaria a inventar dados de jogadores reais; ficou como cartão estático até a página de detalhe também ser migrada para o banco (não pedido nesta tarefa).
  - `globals.css`: `.components-roster-roster-page-content-toolbar` trocou `justify-content: space-between` (que separava o título das duas navs) por `align-items:center; gap:32px` (título e abas agora ficam agrupados à esquerda, já que não sobrou nenhuma nav à direita); removida toda a classe morta `.components-roster-roster-filters-positionGroup` e o wrapper `.filters` (width fixo de 878px, só fazia sentido com as duas navs lado a lado); nova classe `.components-roster-roster-page-content-empty` para a mensagem de aviso.
  - Verificado ao vivo via Claude in Chrome (dados reais, sem mocks): Futsal mostra os 4 grupos com os 12 jogadores reais corretamente distribuídos (2 goleiros, 1 fixo, 5 alas, 4 pivôs) e nomes já sem espaços em branco extras (dado bruto tinha `" Nome "` em alguns registros — `.trim()` aplicado na leitura); Futebol de Campo mostra a mensagem de aviso e ainda assim a comissão técnica (2 membros reais); sem overflow (`scrollWidth === clientWidth` do `<body>`) em 375, 500, 992, 1200, 1250, 1470 e 1600px.

- **`/clube/elenco`: cards revelam apelido/posição só no hover/toque, linha divisória entre posições e título das seções em laranja**:
  - Pedido do usuário com 4 imagens de referência: (1) card de jogador deve mostrar número + apelido + posição só ao passar o mouse ou pressionar (toque); (2) card da comissão técnica deve mostrar apelido + função (com o brasão do clube) do mesmo jeito; (3)/(4) uma linha cinza fina deve separar cada grupo de posição (medida no Figma: `1160 × 2`, `corner-radius: 2`); e o título de cada seção ("Fixos" etc.) estava preto e devia ser laranja.
  - Descoberta ao implementar o hover: os cards do banco não têm `nickname` selecionado ainda — só `name` (nome completo) estava sendo puxado. Adicionado `nickname` à query e ao tipo em `src/data/players.ts` (a coluna já existe em `players` e `staff_members`, só não estava sendo lida); `AthleteCard.tsx` agora mostra `person.nickname` no título do card (mantendo `person.name`, o nome completo, só no `alt` da imagem para acessibilidade) — e o `<img alt>` da comissão técnica ganhou um ícone do brasão (`/header/symbol.png`, mesmo asset já usado no header/detalhe do jogador) ao lado do nome, só para `variant="staff"`.
  - Também corrigido: o rótulo de posição do card mostrava o nome do **grupo** no plural ("Goleiros") em vez da posição do jogador no singular ("Goleiro") — `positionGroupOrder` em `players.ts` ganhou um `cardLabel` singular separado do `label` (plural, usado só no título da seção).
  - `globals.css` (`.components-roster-athlete-card-*`): `.info` (número/apelido/posição) e um novo `::after` (gradiente escuro `rgba(2,12,20,…)`, mesma cor `--azul-preto` já usada no resto do site) agora começam com `opacity:0` e só aparecem em `:hover`, `:focus-visible` e `:active` (`:active` cobre o "pressionar" em touch) — antes ficavam sempre visíveis. Nova classe `.components-roster-athlete-card-staffIcon` (28×28px) para o brasão nos cards de staff.
  - **Bug pego na validação visual**: o novo `.components-roster-roster-page-content-positionGroup h3 { color: #ff3203 }` (para o título da seção) tinha um seletor descendente (`positionGroup h3`) que sem querer também pintava de laranja o `<h3>` do NOME do jogador dentro do card (que também é filho, mais profundo, do mesmo `.positionGroup`) — vi isso claramente numa captura de tela com zoom (nome aparecendo laranja em vez de branco). Corrigido trocando para filho direto (`.positionGroup > h3`), que pega só o título da seção. Removido também o `text-transform: uppercase` do título da seção, já que a referência mostra "Fixos" (não "FIXOS").
  - Nova linha divisória `.components-roster-roster-page-content-positionDivider` (`height:2px; border-radius:2px; background:#cfcfcf` — mesma cor já usada no `<hr>` existente entre o elenco e a comissão técnica, evitando inventar um tom novo não confirmado) inserida como irmã flex entre cada grupo de posição (não antes do primeiro nem depois do último — esse já tinha o `<hr>` existente antes da comissão técnica); o `gap:48px` do container flex já dá o espaçamento simétrico dos dois lados da linha automaticamente, sem CSS extra de margem.
  - Verificado ao vivo via Claude in Chrome com hover real do mouse (não só inspeção de CSS): card de goleiro revela "12 / Giurizatto / Goleiro" com fundo escurecido; card de staff revela o brasão + "Vandin" + "Técnico"; estado padrão (sem hover) mostra só a foto, igual às referências.

- **`/clube/elenco/[slug]`: página de detalhe do jogador refeita do zero com dados reais do banco (desktop + mobile)**:
  - Pedido do usuário com uma imagem do desktop (frame `elenco_principal_posicao_futsal`, um card "ticket" 1440×840 com Apelido/Nome/Data de Nascimento/Pé Dominante/redes sociais/foto/número) e um dump de design tokens (cores, tipografia, spacing) do Figma para desktop e mobile.
  - **Antes de implementar, perguntei ao usuário 2 coisas bloqueantes** (via `AskUserQuestion`, não adivinhei): (1) o card do Figma também mostra "N° de Jogos/Assistências/Gols" por jogador, mas a tabela `players` não tem essas colunas e a tabela `games` é só a agenda de partidas, sem estatística por jogador — resposta do usuário: **não seguir esse ponto do design**, esses 3 campos foram removidos da implementação; (2) a página de detalhe hoje usa 100% dados mockados, desconectados da tabela `players` real já usada na listagem — resposta do usuário: **sim, migrar para dados reais**, os cards da listagem voltam a ser links.
  - **Achado ao investigar antes de mexer**: o asset `public/squad/player-profile.png` que o usuário atualizou e comitou numa mensagem anterior ("pode commitar as imagens, fazem parte dos detalhes") já era, no código antigo, um `<img>` de fundo absolutamente posicionado atrás do conteúdo do card (não um `clip-path` CSS) — o usuário só reexportou esse PNG do Figma com o novo desenho "ticket" (bordas com entalhes, moldura laranja, boi/"ACF NA VEIA" já desenhados dentro da própria imagem). Isso eliminou a necessidade de recriar a forma do card via CSS/SVG: bastou reposicionar o conteúdo (texto, foto, número) por cima do PNG novo. Também veio um `public/squad/player-profile-mob.png` novo (381×607), a versão mobile do mesmo frame.
  - `src/data/players.ts`: nova query única `getAllPlayers()` (cacheada por request) trazendo `birthday`, `dominant_foot`, `quote` e `social_media` (jsonb) além do que já existia; `getPlayersByCategory`/`getPlayerBySlug` agora filtram essa mesma lista em memória (um só round-trip ao banco por request). Novo helper de `slug` (a partir do `nickname`, sem acento, com sufixo do id em caso de colisão) — o banco não tem coluna `slug`. `birthday` formatado com `getUTCDate/Month/FullYear` (evita o típico bug de fuso horário virando o dia errado); `dominant_foot` (enum `direito`/`esquerdo`/`ambidestro`) só recebe a primeira letra maiúscula, já que os valores do enum já são exatamente as palavras certas em português.
  - `AthleteCard.tsx` voltou a ser um `<Link href="/clube/elenco/${slug}">` (estava estático desde a tarefa anterior, por falta de `slug`/dados reais — resolvido agora).
  - `PlayerDetailContent.tsx` reescrito: novo cabeçalho "Perfil do Atleta" / link "Elenco →" (volta para `/clube/elenco?modalidade=${categoria}`, mantendo a aba certa); dentro do card — "Apelido" (rótulo pequeno laranja) + apelido gigante, lista Nome/Data de Nascimento/Pé Dominante em 2 colunas, ícones de rede social reais (`social_media` do banco, só renderiza se a URL existir, reaproveitando os PNGs `/footer/facebook.png`, `/footer/instagram.png`, `/footer/youtube.png` já usados no rodapé), foto placeholder e número fantasma (`#99` etc.) sobre o frame. Citação (`quote`, campo real do banco) só aparece se o jogador tiver uma cadastrada — hoje nenhum tem, então não aparece, batendo com a imagem de referência (card → patrocinadores, sem nada entre os dois).
  - **Removida** a seção antiga de "jogadores relacionados" (grid de mini-cards da mesma posição) e o rodapé de assinatura "ACF SPORTS #NAVEIA" com lista de redes em texto puro (não eram links reais) — a nova imagem do Figma não mostra nenhum dos dois, e o link único "Elenco →" já cobre a navegação de volta.
  - `[slug]/page.tsx`: trocado `getAthleteBySlug` (mock) por `getPlayerBySlug` (banco real); removido `generateStaticParams` — a página passou de estática (SSG, pré-gerada no build a partir do array mock) para dinâmica (`ƒ`, igual à listagem), então um jogador novo cadastrado no banco já tem página funcionando sem precisar de novo deploy.
  - **Limpeza**: com a página de detalhe migrada, `src/data/roster.ts` (todo o array mock de atletas fake) e `src/components/roster/PlayerMainCardImage.tsx` (um gerador de card em SVG que nunca chegou a ser usado em lugar nenhum, confirmado via `grep`) ficaram sem nenhum consumidor real — removidos os dois arquivos e o CSS órfão correspondente, em vez de deixar ~200 linhas mortas no repositório.
  - Responsivo: `<picture>` com `<source media="(max-width:768px)">` troca para o frame mobile automaticamente; layout mobile (posições/tamanhos de fonte) é uma adaptação minha a partir só dos tokens de tipografia entregues (sem uma imagem do mobile em si) — vale conferir com uma captura de tela real do Figma mobile se algo destoar.
  - Verificado ao vivo via Claude in Chrome (dados reais, jogador "Cabeludo"/Miguel Ribeiro Dos Santos): desktop bate visualmente com a imagem de referência (Apelido/Nome/Data/Pé Dominante/Instagram/foto/#99 sobre o frame novo); mobile troca para o frame `-mob.png` automaticamente e não estoura (`scrollWidth === clientWidth` em 390px); clique num card da listagem navega até `/clube/elenco/giurizatto` de ponta a ponta; link "Elenco →" volta para `/clube/elenco?modalidade=futsal`.

- **`/clube/elenco/[slug]`: reposicionamento fino do conteúdo dentro do card, seguindo medidas exatas do painel Design do Figma**:
  - Pedido do usuário com 7 imagens (6 capturas do painel Design do Figma medindo cada elemento do card — apelido, nome/data de nascimento, pé dominante, redes sociais, foto do jogador, número da camisa — mais 1 captura do site real mostrando o card grande demais, estourando a página, com o header sobrepondo o topo do card).
  - Extraí as medidas de cada captura (`Position X/Y`, `Resizing W×H`, `Gap`) e, para posicionar cada elemento, usei a mesma origem do card já registrada numa mensagem anterior (`X:85, Y:546, W:1440, H:840`) para converter as posições absolutas do Figma em deslocamentos relativos ao card (ex: a foto do jogador em `X:903,Y:657,W:418,H:605` vira `left:56.8%, top:12.9%, width:29%, height:70.3%` do card) — validei essa conta batendo a margem direita resultante da foto (~14.2%) com o valor que eu já tinha estimado numa iteração anterior, então segui com confiança.
  - **"Pé Dominante é a mesma medida" (item 3)**: o Figma mostra gaps horizontais diferentes entre as duas colunas nas linhas "Nome/Data de Nascimento" (76px) e "Pé Dominante/N° de Jogos" (156px) — isso é só um artefato de auto-layout com largura "Hug" (a 2ª coluna começa numa posição fixa, então o gap muda conforme o texto da 1ª coluna é mais curto ou mais longo). Já uso CSS Grid de 2 colunas (`1fr 1fr`), que alinha a 2ª coluna de forma consistente independente do conteúdo — então não copiei os dois números do Figma, só ajustei o espaçamento vertical entre linhas para `32px` (era `24px`), que é o que o usuário quis dizer com "mesma medida".
  - **Achado real, não só reposicionamento**: a foto do jogador (`.playerFigure`) e o número fantasma (`.number`) estavam com o tamanho errado — `340×700` (retângulo estreito e alto) e `font-size:140px`, quando o Figma mede `418×605` (foto) e um bloco de texto `248×162` para "#08" (algo em torno de `90-95px` de fonte). Corrigidos para os valores certos — a foto fica bem mais proporcional (menos esticada) e o número, bem menor.
  - **A causa raiz do "card estourando a página" (item 6)**: `.profileCard` usava `width:100%` dentro de um container de até `1440px` — a `aspect-ratio: 1461/861` então dava uma altura de até `~849px` só pro card, fácil de não caber inteiro numa tela comum sem rolar bastante. Reduzido para `width: min(1160px, 100%)` (a mesma "família" mais estreita já usada pela barra de ferramentas desta página e por outras seções do site — footer, planos, etc.), o que também encolhe a altura proporcionalmente (`~684px`) — o card inteiro passou a caber numa única tela em telas de laptop comuns.
  - **Bug pego só depois de testar no navegador de verdade** (não só olhando CSS): ao trocar `.playerFigure` de `right:200px` fixo para `left:56.8%` (percentual, pra escalar corretamente com o novo tamanho do card), o breakpoint mobile (`≤768px`) que já existia — `right:0; width:100%; height:52%` — parou de funcionar, porque agora havia um `left` herdado do desktop *e* um `width:100%` ao mesmo tempo, e nessa combinação o CSS ignora o `right`. Resultado: no mobile, a foto ficava 100% da largura do card mas deslocada ~57% pra direita, vazando bem pra fora do card. Só vi isso medindo `getBoundingClientRect()` da foto dentro de um iframe de 390px — corrigido adicionando `left:0` explícito no bloco mobile.
  - Verificado ao vivo via Claude in Chrome: card inteiro visível numa única captura de tela em desktop (antes precisava rolar), testado com apelido curto ("ICE") e longo ("GIURIZATTO", 10 letras) sem quebrar/vazar da caixa; sem overflow (`scrollWidth === clientWidth`) em 992, 1100, 1200, 1300, 1440, 1600 e 390px (mobile, após a correção do bug acima).

- **`/clube/elenco/[slug]` mobile: espaçamentos internos do card corrigidos com medidas do painel Design (5 imagens)**:
  - Pedido do usuário: 5 capturas do painel Design do Figma medindo especificamente a versão **mobile** do card — grupo Apelido/Apelido, bloco de texto completo, o gap entre uma linha de texto e outra, o grupo foto+número, e a fileira de redes sociais.
  - `Gap: 2` no grupo Apelido/nome (era `margin-top: 4px` no meu código — ajustado para `2px`); inset do bloco de identidade a partir das bordas do card: `top: 46px` e `left: 40px` de um card com `381×607px` intrínsecos → convertidos para `7.6%`/`10.5%` (percentual, pra continuar certo em qualquer largura de tela, já que o card usa `aspect-ratio` fluido).
  - **"Espaçamento entre os itens de texto" (item 3)**: essa foi a medida mais direta de todas — selecionei a própria linha "Nome" no Figma e o painel mostrou `Gap: ⇕ 8` entre ela e a linha "Data de Nascimento" seguinte. Troquei o `gap` do grid do `.profileList` mobile de `12px` para `8px`.
  - **Achado ao tentar validar as medidas de gap com o restante do card**: o Figma mede a lista completa com as 6 linhas originais do design (incluindo `N° de Jogos/Assistências/Gols`, que já tínhamos decidido não implementar por não existir essa estatística no banco — ver changelog anterior). Isso significa que o espaçamento "lista → redes sociais" que o Figma mostra (calculado a partir da posição absoluta de cada elemento) pressupõe uma lista de 6 linhas, não as 3 que de fato renderizamos. Em vez de tentar recalcular esse número pra uma lista mais curta (frágil, dependeria de eu confiar 100% em contas cruzadas entre capturas de tela diferentes), mantive a mesma lógica de "espaço depois da última linha visível" e apliquei o valor final calculado (`60px`) como `margin-top` do bloco de redes sociais, que é o comportamento mais previsível e não quebra se o número de linhas mudar no futuro.
  - Foto + número do jogador (item 4): a versão mobile da foto **não** é um banner de largura total como eu tinha implementado — o Figma mostra um recorte menor (`216×316`, ~57%×52% do card) encostado nas bordas direita/inferior do card (`right:10px`/`bottom:72px` → `2.6%`/`11.9%`), a mesma composição do desktop (foto como retrato à direita, sobrepondo o texto), só que numa escala menor. Removida a versão "banner full-width" que eu tinha colocado antes.
  - Redes sociais (item 5): gap entre os ícones ajustado de `14px` para `16px` (medida direta do Figma, `Gap: 16` na fileira de ícones).
  - Verificado ao vivo via Claude in Chrome, num iframe de 390px: sem overflow (`scrollWidth === clientWidth`); card inteiro visível numa única captura; foto do jogador aparece como retrato à direita sobrepondo o texto a partir de "Pé Dominante" (igual à referência), não mais como faixa horizontal; espaçamento entre "Nome"/"Data de Nascimento"/"Pé Dominante" visivelmente mais compacto e igual entre todas as linhas.

- **`/clube/elenco/[slug]` mobile: número da camisa "para fora do card"**:
  - Usuário reportou (provavelmente testando pelo celular via túnel ngrok, visível nos logs do dev server) que o número ficava fora do card no mobile, com a medida do grupo "foto + número" do Figma (`W:131.19 H:72`, marcador `0` no gap inferior).
  - **Causa raiz, confirmada medindo no navegador antes de mudar qualquer CSS** (não só olhando o número dado): o `.number` no mobile ainda usava um resquício de antes de eu ter as medidas certas da foto — `right:20px; bottom:20px`, um inset bem menor que o da foto (`bottom:11.9%` ≈ 72px). Como o card tem cantos cortados (formato "ticket"), esse inset pequeno colocava o número exatamente em cima do recorte diagonal do canto inferior direito — tecnicamente dentro da caixa retangular do elemento, mas visualmente para fora da área escura visível do card (passando por cima do traço laranja).
  - Corrigido alinhando o número ao mesmo "chão" já usado pela foto (`bottom: 11.9%`), com `right: 8%` e `font-size` reduzido de `56px` para `48px` (pra não ficar apertado nesse espaço mais recuado).
  - Verificado ao vivo via Claude in Chrome num iframe de 390px: número aparece claramente dentro da área escura do card, sobre a foto, acima da marca d'água "ACF NA VEIA", sem tocar o traço laranja do canto.

- **`/clube/elenco/[slug]`: ajustes de estilo feitos diretamente pelo usuário no card do jogador**:
  - Label "Apelido" trocado de texto (`<p>`) para um asset de imagem (`/squad/apelido.png`); ícone do Instagram trocado de `/footer/instagram.png` para `/squad/insta-azul.png`; cores de `Nome`/valores da lista (`dt`/`dd`) ajustadas via classes Tailwind arbitrárias (`text-[#ffffff]`/`text-[#FF7A5C]`). Build validado (`npm run build`) antes do commit.

- **`/contato`: refeita a metade superior da página com base no Figma (topo "Fale com o ACF" + "Onde estamos" + cards + "Fique à vontade")**:
  - Pedido do usuário com 11 capturas do painel Design/Inspect do Figma (medindo `Position X/Y`, `Resizing W×H`, `Gap`) mais um mock final da página e o YAML de design tokens exportado do Figma (cores, tipografia, espaçamento, radius).
  - O novo Figma não tem formulário de contato ("vamos conversar?") nem a seção de planos com abas ("Nossos Planos") que existiam antes — só "Fale com o ACF", "Onde estamos" com 3 cards (sede/canais/horário), um divisor cinza, e "Fique à vontade" com CTA de WhatsApp; a seção "Seja um parceiro do ACF" (3 cards fixos) ficou de fora por enquanto, a ser passada pelo usuário depois. Perguntado ao usuário o que fazer com o formulário e as abas de planos — confirmado remover os dois agora (`ContactContent.tsx` reescrito do zero, sem o state de planos/tabs e sem o form).
  - `Nossa sede.`/`Nossos canais.` usam `teamInfo.address`/`teamInfo.phone`/`teamInfo.email` (dados do banco via `getTeamInfo()`), mantendo o padrão dinâmico já existente; `Horario de atendimento.` é texto estático (não existe coluna pra isso na tabela `team_info`).
  - Ícones dos cards e do botão de WhatsApp trocados de PNGs (`/contact/*.png`) para `lucide-react` (`MapPin`, `Phone`, `Clock`, `MessageCircle`), já usado em `MainMenu.tsx`.
  - CSS (`.components-contact-content-*` em `globals.css`) reescrito: container `min(1160px, calc(100% - 40px))` (mesmo padrão já usado no header da página e em outras seções), gaps verticais de `40px`/`24px` entre blocos batendo com os marcadores do painel Figma, divisor `2px` na cor `cinza_verde` (`#cfcfcf`) do design system, cards com a sombra dupla do token de efeito (`-1px -1px 4px` / `1px 1px 4px`, `#A09E9E/0.32`), botão de WhatsApp com radius `10px` e texto branco (consistente com a correção já registrada em memória pro botão de WhatsApp dos planos).
  - Tamanhos de fonte e cores dos títulos/parágrafos foram inferidos a partir da tabela de tokens tipográficos do Figma cruzada com o padrão já usado no resto do arquivo (não dava pra derivar com precisão só da altura "Hug" dos blocos no painel Design) — vale conferir contra uma captura de tela real do Figma se algo destoar no tamanho do texto.
  - Build validado (`npm run build`) e conferido ao vivo via Claude in Chrome em 1600px: estrutura, textos, ícones e espaçamento batendo com o mock fornecido.

- **`/contato`: adicionada a seção "Seja um parceiro do ACF" (cards de planos), reaproveitando os mesmos dados/estilo de `/clube/patrocinadores`**:
  - Pedido do usuário com 5 capturas do painel Design do Figma (tamanho da seção `W1600×H1010`, posição do bloco de título `X:216,Y:80,W:760 Hug,H:96 Hug,Gap:8`, posição/gap da grade de cards `X:80,Y:216,W:1440 Hug,H:714 Hug,Gap:40`) e a instrução explícita de que os cards são os mesmos já usados em "Nossos Planos" na página de patrocinadores.
  - Os números batem com dois containers centralizados de larguras diferentes dentro da faixa escura de `1600px`: o título usa o mesmo container padrão `min(1160px, calc(100% - 40px))` já usado no resto da página (inset ≈220px, próximo do `216` medido), enquanto a grade de cards usa um container mais largo, novo, `min(1440px, calc(100% - 160px))` (inset exato de `80px`) — confirmado comparando visualmente o mock fornecido, que mostra o título mais recuado que a borda esquerda do primeiro card.
  - Copiei os dados dos planos (`plans` — nomes, preços, mensagens de WhatsApp, benefícios) e o visual do card (título vermelho-escuro `#661400`, marcador de lista com anel laranja, preço em azul, botão em gradiente sem ícone) diretamente de `SponsorsPageContent.tsx`/`PlanCard`, em vez de retranscrever o texto ligeiramente diferente das capturas de tela — o usuário confirmou que devem ser "os mesmos" cards, então a fonte da verdade é o componente já implementado, não a nova captura. Os dados/CSS foram duplicados dentro de `ContactContent.tsx` (não extraídos para um componente compartilhado), seguindo o padrão já existente no repositório de colocar dados por componente em vez de compartilhar entre páginas.
  - Diferente da grade de patrocinadores (que usa colunas assimétricas `340px 400px 340px` com o card "master" maior/destacado), aqui os 3 cards são do mesmo tamanho (`repeat(3, 1fr)`) — o mock desta seção não mostra nenhum card maior que os outros.
  - Faixa de fundo escura usa `var(--azul-preto)` (a mesma cor do header "contato." desta página), com `padding: 80px 0` batendo com os insets superior/inferior medidos no Figma.
  - Build validado (`npm run build`) e conferido ao vivo via Claude in Chrome em 1600px: título alinhado conforme o mock, 3 cards com mesma altura (stretch do grid), preço/botão idênticos ao padrão já usado em patrocinadores, sem espaço em branco indevido na transição entre a seção clara ("Fique à vontade") e a faixa escura.

- **Rodada de correções pós-QA (desktop + mobile)**:
  - `RosterSection.tsx` (carousel de "elenco" da home) passou a receber jogadores reais via `getPlayersByCategory("futsal")` (chamado em `page.tsx`, primeiros 5) em vez do array `athleteCards` hardcoded; os offsets visuais em zigue-zague (`athlete2`/`athlete3`/`athlete4` no CSS) agora usam a posição do card no carrossel, não mais o `id` do banco. No mobile (`≤768px`, via `matchMedia` em `useEffect`) o componente corta a lista para os 4 primeiros jogadores, conforme pedido.
  - Criado `ScrollReveal.tsx` (wrapper client-side com `IntersectionObserver`, dispara uma vez e desconecta) e aplicado às seções da home (`newsBand`, `PlansSection`, `RosterSection`, `YoutubeSection`) para a animação de fade-in + translateY ao rolar a página; classes `.components-scroll-reveal-section`/`-visible` em `globals.css`, com fallback `prefers-reduced-motion`.
  - `.components-roster-athlete-card-text` e `-label` (texto/badge exibidos no hover dos cards em `/clube/elenco`) não tinham `font-family` declarada e caíam no `--font-rubik` padrão do `body`; corrigido para `--font-outfit`/`--font-montserrat` (mesmo par tipografia usado no resto do site para título+texto).
  - Removidas as colunas "Gm"/"Gc" da tabela de classificação em `CompetitionsContent.tsx` (`/clube/competicoes`); no mobile, a tabela agora cabe no card sem exigir scroll horizontal (largura da 1ª coluna limitada a `92px` com `text-overflow: ellipsis`, fonte reduzida para `12px`).
  - Botão "whatsapp" de `ContactContent.tsx` trocou o ícone genérico `MessageCircle` (lucide) pelo asset real `/contact/whatsapp.png` (aplicado `filter: brightness(0) invert(1)` para renderizar em branco sobre o gradiente laranja).
  - Botão do `HeroNews` (mobile, `≤992px`) ganhou `margin-bottom: 60px` para não colar no fim da tela.
  - Build validado (`npm run build`) e conferido ao vivo via Claude in Chrome em desktop (1440px) e mobile (390px).

- **`/clube/competicoes` mobile: símbolos dos times descentralizados no card "Próxima Partida"**:
  - Causa raiz encontrada medindo `getBoundingClientRect()` dos elementos no navegador (não só olhando a captura): o `<span class="teamLogo">` que envolve o `<img>` do escudo é, tecnicamente, mais um `<span>` filho de `.components-competitions-content-team` — então a regra mobile `.components-competitions-content-team span { display:block; width:100%; ... }` (pensada só pro nome do time truncar com `text-overflow:ellipsis`) também pegava o wrapper do escudo, sobrescrevendo o `display:inline-flex` da base (maior especificidade: `.team span` vence `.teamLogo`) e jogando o escudo pra ponta esquerda do card em vez de centralizado.
  - Corrigido excluindo o `teamLogo` da regra genérica (`span:not(.components-competitions-content-teamLogo)`) e adicionando `justify-content:center; width:100%` explícito em `.teamLogo` no bloco mobile, pra centralizar mesmo se o `display:inline-flex` da base voltar a valer.
  - Verificado ao vivo via Claude in Chrome em 390px, medindo o centro dos dois `<img>` de escudo contra o centro do "X": distância simétrica (~80.7px) dos dois lados após a correção (antes: ~118.7px de um lado, ~42.7px do outro).

- **Animação de scroll da home mais suave + diagnóstico "jogadores não carregam"**:
  - `ScrollReveal.tsx`/`globals.css`: transição de `opacity`/`transform` trocada de `700ms ease` (deslocamento de `36px`) para `1100ms cubic-bezier(0.22, 1, 0.36, 1)` (curva de desaceleração suave, estilo "ease-out-quint") com deslocamento reduzido para `24px` — efeito bem mais gradual ao rolar a página.
  - Usuário reportou que `/clube/elenco` não carrega jogadores; confirmado via query direta no Neon (fora da aplicação) que a tabela `players` está com **0 registros**, enquanto as demais tabelas (`staff_members`, `games`, `competitions`, `team_info`) têm dados normalmente — não é bug de código, é dado ausente no banco (usuário confirmou depois que a tabela estava corrompida). Como o carrossel de "elenco" da home passou a usar essa mesma query na rodada anterior, a home apresenta o mesmo sintoma (seção "elenco." renderiza sem nenhum card) até os dados serem restaurados — nenhuma mudança de código foi feita para isso, os componentes já usam a fonte de dados correta e voltam a funcionar assim que a tabela for repovoada.

- **Home `planos.`: refeitos os cards a partir de capturas do painel Design do Figma (medidas via `figma-desktop-parity`)**:
  - Frame de referência `1600×774`; card PONTUAL `X:220 Y:194 W:560 H:480`; card MASTER `X:820 Y:172 W:560 H:524` (elevado `22px` e `44px` mais alto que o pontual); gap entre cards `40px` — todos batendo exatamente com `.components-plans-section-inner { width: min(1160px, ...) }` já existente (`(1600-1160)/2 = 220`, `560+40+560 = 1160`). Verificado ao vivo via `getBoundingClientRect()` em 1600px: os 4 valores conferem com o Figma pixel a pixel.
  - Removido o card "mensal" da home (o Figma só tem 2 instâncias de `planos_home`, confirmado no export de design tokens); ele continua existindo em `/contato` e `/clube/patrocinadores`, que têm dados/CSS próprios e não foram tocados. Preços (R$180/R$500) e texto dos benefícios mantidos como estavam — o Figma mostrava valores diferentes (R$199/R$299) e uma lista de benefícios ligeiramente diferente, mas isso batia com um mockup, não com os dados reais já usados nas outras páginas; confirmado com o usuário antes de decidir manter o conteúdo real.
  - Ambos os cards passaram a usar o mesmo fundo escuro `#07243d` (antes o card "featured" usava um fundo claro `#e1ebf5`/texto escuro, divergindo do Figma); título `h3` unificado em `32px/48px` Montserrat SemiBold pros dois cards (antes só o featured usava esse tamanho); adicionado um `<hr>` divisor fino entre o cabeçalho (título+preço) e a lista de benefícios, presente em todas as capturas do Figma mas ausente na implementação anterior; cor de destaque (título do MASTER, ícones de check, gradiente do botão) trocada para `#FF7A5C`/`linear-gradient(168deg, #ff7a5c 0%, #8f1d00 99%)` — o gradiente já batia exatamente com o token extraído do Figma, sem precisar de ajuste.
  - Botão "ver mais" trocado de largura fixa (`200px`) para hug-content (`padding:16px 32px`, sem `width`), batendo com o componente `btn_card_azul` do Figma (`Resizing: Hug`).
  - **Bug encontrado e corrigido durante a verificação em mobile**: o botão usava `margin-top:auto` pra ficar colado no rodapé do card — funciona só quando o card tem altura fixa (desktop). No carrossel mobile (`≤768px`), o card usa altura automática (`min-height:0`), então `margin-top:auto` não tinha espaço sobrando pra empurrar e o botão ficava colado no último item da lista. Trocado para `margin-top:24px` fixo; no desktop o espaçamento continua idêntico porque quem empurra o botão pro fundo do card ali é o `.benefits{flex:1}` esticando dentro da altura fixa, não a margem do botão.
  - Build validado (`npm run build`) e conferido ao vivo via Claude in Chrome em 1600px (desktop) e ~390px (carrossel mobile, os dois cards).

- **Causa raiz do "jogadores não carregam" (home + `/clube/elenco`) encontrada e corrigida**:
  - Depois que o usuário recadastrou os jogadores no banco (sessão anterior tinha a tabela `players` zerada/corrompida), a home e o elenco continuavam sem mostrar nenhum jogador mesmo com a tabela populada (11 registros confirmados via query direta no Neon).
  - **Causa raiz**: a coluna `players.modality` mudou de um valor único (`"futsal"` ou `"campo"`) para um **array** Postgres (`player_modality[]`) — todos os 11 jogadores atuais têm `{futsal,campo}` (jogam nas duas modalidades). O código em `src/data/players.ts` ainda esperava `modality` como string única e comparava `player.category === category` (`===` contra um array nunca é `true`), então `getPlayersByCategory("futsal")`/`("campo")` sempre retornava lista vazia — daí o carrossel da home e a listagem de `/clube/elenco` renderizarem o estado "nenhum jogador".
  - Corrigido em `players.ts`: `PlayerRow.modality` e o novo campo `RosterPlayer.categories` agora são `RosterCategory[]`; `getPlayersByCategory` passou a usar `.includes(category)` em vez de `===`. Mantido `RosterPlayer.category` (singular, primeira modalidade do array) só para o link "Elenco →" em `PlayerDetailContent.tsx`, que precisa de um único valor pra montar a URL `?modalidade=...` — não foi alterado.
  - Verificado com `npx tsc --noEmit` (sem erros) e ao vivo via Claude in Chrome: `/clube/elenco?modalidade=futsal` e `?modalidade=campo` mostram os 11 jogadores (esperado, já que todos têm as duas modalidades), e a home volta a mostrar 5 jogadores reais no carrossel de "elenco.".

- **`/clube/patrocinadores`, seção "Nossos Planos": refeita com o mesmo sistema visual da home, a partir de medidas do painel Design do Figma**:
  - Mesmas medidas exatas da home (`figma-desktop-parity`): cards `560×480` (pontual) e `560×524` (master, elevado `22px`), gap `40px` — batendo com o container `min(1160px, ...)` que a seção já usava. Verificado ao vivo via `getBoundingClientRect()`: `560×480`/`560×524`/gap `40`/elevação `22`, idêntico ao Figma.
  - Removido o card "mensal" (Figma só mostra 2 cards aqui também) — decisão confirmada com o usuário antes, já que esta é a página de vendas real (diferente da home, que é só um teaser); o plano mensal deixou de existir nesta página.
  - `SponsorsPageContent.tsx`: `PlanCard` reestruturado pra header (título+preço) → divisor → lista → botão, igual ao padrão da home; card claro (`#fdfdfd`) trocado por fundo escuro `#07243d` igual à home; marcador da lista trocado do círculo vazio antigo para o checkmark `✓` laranja; textos dos botões trocados de "enviar direct no whatsapp" (fixo nos 3 cards) para `"aderir"` (pontual) / `"ver mais"` (master) com ícone de seta — confirmado com o usuário antes, mantendo o `href` de WhatsApp com mensagem pré-preenchida por plano (só mudou o rótulo visível, não a função).
  - **Bug de especificidade CSS encontrado e corrigido**: o gradiente do botão featured (`.featuredPlanButton`, uma classe única) não aparecia — o botão renderizava cinza mesmo com a classe aplicada. Causa: a regra base `.components-sponsors-page-content-planCard a` (seletor composto classe+elemento, especificidade 0,1,1) tinha mais peso que `.featuredPlanButton` sozinha (especificidade 0,1,0), então sempre vencia independente da ordem no arquivo. Corrigido trocando pra `.components-sponsors-page-content-featuredPlanCard a` (mesma especificidade 0,1,1 da regra base, decidido por ordem no arquivo) — mesmo padrão de bug já visto antes em `.components-competitions-content-teamLogo`.
  - Grid dos cards migrado de `grid-template-columns: 340px 400px 340px` (3 colunas assimétricas) para `display:flex` com 2 cards de `560px`; breakpoints `≤1200px`/`≤768px` reescritos seguindo o mesmo padrão de carrossel horizontal com scroll-snap já usado na home.
  - Build validado (`npm run build`, `npx tsc --noEmit`) e conferido ao vivo via Claude in Chrome em desktop (1600px, medido) e mobile (~500px, os dois cards no carrossel).

- **Mobile: cards de planos (home `planos.` e `/clube/patrocinadores` "Nossos Planos") trocados de carrossel horizontal para empilhamento vertical**:
  - Pedido do usuário depois de ver o carrossel em produção. Removido `overflow-x:auto`/`scroll-snap-type:x mandatory`/`flex-direction:row` do bloco `≤768px` nos dois componentes (`.components-plans-section-cards` e `.components-sponsors-page-content-planGrid`) — como o bloco `≤1200px` já deixava os cards em coluna (`flex-direction:column`) com `gap:40px` herdado da regra base, bastou parar de sobrescrever pra `row` no mobile; adicionado `gap:24px` (mais compacto que o `40px` do desktop) e removidas as regras de `margin-right`/`scroll-snap-align`/`:last-child` que só faziam sentido no carrossel.
  - Verificado ao vivo via Claude in Chrome em ~606px: os dois cards de cada página aparecem um abaixo do outro, sem necessidade de swipe horizontal.

- **Lote de ajustes pontuais (hover dos cards de jogador, ícones dos planos, `/contato`, posições de campo, elenco aleatório, overflow da tabela)**:
  - **Hover dos cards em `/clube/elenco`**: aplicada a tipografia do novo design system (título Montserrat Medium 24/32, texto da posição Outfit Regular 20/28 com tracking 1.5%), badge do número trocado de pílula translúcida pra retângulo arredondado (`8px`) `#484747`/texto branco. Animação de hover deixada mais suave: o card agora eleva (`translateY(-6px)`) com sombra mais forte, a imagem dá um zoom mais perceptível (1.06) e o overlay escuro ganhou um `backdrop-filter: blur(3px)` — tudo em `320-380ms` com curva `cubic-bezier(0.22,1,0.36,1)` (era `180-200ms ease`).
  - **Ícones dos cards de planos (home + `/clube/patrocinadores`)**: o checkmark `✓` antes de cada benefício foi trocado pelos SVGs `public/sponsor/bull-gray-icon.svg` (card pontual) e `public/sponsor/bull-orange-icon.svg` (card master) — confirmado com o usuário que "mensal" citado no pedido era o card master (não existe mais um card "mensal" separado nessas duas páginas).
  - **`/contato`**: texto "É só chamar a gente pelo whatsapp." agora uppercase (body-md do design system é "upper"); botão do WhatsApp com `border-radius:8px` e gradiente atualizado pra `#ff7a5c → #8f1d00` (era `#ff3203 → #8f301d`); os 3 cards (Nossa sede/Nossos canais/Horário) ganharam um badge circular (`50px` radius) atrás do ícone, e o texto do corpo subiu de `14px/22px` pra `16px/24px` Outfit — batendo com os tokens `body-3` extraídos do Figma.
  - **Posições de futebol de campo em `/clube/elenco`**: adicionado um segundo conjunto de grupos de posição (Goleiros, Zagueiros, Laterais, Volantes, Meias, Pontas, Centroavantes) usado só na aba "Futebol de Campo"; `groupPlayersByPosition` agora recebe a `category` e escolhe o conjunto certo. **Achado durante a implementação**: o schema do banco mudou de uma coluna `position` única pra duas colunas separadas, `position_futsal` e `position_campo` — a query antiga (`SELECT ... position ...`) começou a falhar com `NeonDbError: column "position" does not exist`, quebrando a home E o `/clube/elenco` inteiros. Corrigido lendo as duas colunas novas; como `position_campo` na prática ainda só tem valores em vocabulário de futsal (Goleiro/Fixo/Ala/Pivô, dado não recadastrado ainda), a normalização mapeia esses valores pro equivalente de campo mais próximo (Fixo→Volante, Ala→Ponta, Pivô→Centroavante) até que posições reais de campo sejam cadastradas.
  - **Elenco aleatório na home**: `pickRandom()` (Fisher-Yates) substituiu o `slice(0, 5)` fixo em `page.tsx`, sorteando 5 jogadores futsal a cada renderização. Como a home usa `revalidate=60`, o conjunto muda a cada renovação do cache (até 1x por minuto), não a cada request individual — suficiente pra "jogadores diferentes a cada acesso" na prática, mas vale saber que não é 100% por requisição.
  - **Tabela de `/clube/competicoes`**: `text-overflow: ellipsis` adicionado à coluna "Clube" (`overflow:hidden`, `max-width:165px`, já tinha `white-space:nowrap`) — nomes longos como "ACF Sports/Vila Mercado" agora truncam em vez de estourar a coluna.
  - Build (`npm run build`, `npx tsc --noEmit`) validado e conferido ao vivo via Claude in Chrome em todas as páginas afetadas.

- **Filtros futsal/futebol de campo em `/clube/elenco` passam a usar `position_futsal`/`position_campo` direto, não mais `modality`**:
  - Pedido do usuário. `getPlayersByCategory` filtrava por `player.categories.includes(category)`, derivado da coluna `modality` — mas essa coluna mudou de `{campo,futsal}` pra só `{futsal}` em todos os jogadores (achado ao reconferir o banco), o que faria a aba "Futebol de Campo" ficar vazia mesmo com jogadores tendo `position_campo` preenchido. Trocado o filtro pra checar diretamente se `position_campo`/`position_futsal` está preenchido (`!== ""`) pro respectivo jogador, sem depender de `modality`.
  - `RosterPlayer.categories` (array, só usado nesse filtro) removido por ficar sem uso; `RosterPlayer.category` (singular, usado no link "Elenco →" do perfil do jogador) agora deriva de `positionFutsal ? "futsal" : "campo"` em vez do array de modalidade. Coluna `modality` removida do `SELECT` e do tipo `PlayerRow` por não ser mais lida em nenhum lugar.
  - Também notado: o LZ já tem um valor real e distinto em `position_campo` (`"Centroavante"`, diferente do `position_futsal` `"Pivô"`) — confirmado ao vivo que ele aparece no grupo "Centroavantes" com o rótulo "Centroavante" no hover, enquanto os demais jogadores (que ainda têm `position_campo` igual ao `position_futsal`) continuam caindo no fallback de mapeamento futsal→campo.
  - Build (`npm run build`, `npx tsc --noEmit`) validado; conferido ao vivo que as duas abas seguem mostrando os 11 jogadores (todos têm os dois campos de posição preenchidos hoje).

- **Correção do item anterior: filtro futsal/campo volta a usar `modality`, não `position_futsal`/`position_campo`**:
  - Usuário corrigiu o pedido anterior: a aba "Futebol de Campo" só deve listar jogadores cujo `modality` inclua `campo` (ou seja, `{futsal,campo}`); se o jogador só tem `{futsal}`, ele não aparece lá — a aba deve mostrar a mensagem "Elenco de futebol de campo ainda sendo montado. Em breve, mais novidades por aqui." (mensagem de estado vazio que já existia em `RosterPageContent.tsx`, só precisava o filtro retornar lista vazia).
  - Revertido `getPlayersByCategory` pra filtrar por `player.categories.includes(category)` de novo; `modality` voltou ao `SELECT` e ao tipo `PlayerRow`, e `RosterPlayer.categories`/`category` voltaram a derivar do array de modalidade (não mais de `positionFutsal`/`positionCampo`).
  - `position_futsal`/`position_campo` continuam sendo usados exatamente como antes só pra agrupar por posição (`groupPlayersByPosition`) dentro de cada aba já filtrada — essa parte não mudou.
  - Como todos os 11 jogadores estão hoje com `modality: {futsal}` (achado na sessão anterior), a aba "Futebol de Campo" fica vazia até que jogadores sejam marcados com `campo` também — verificado ao vivo: `/clube/elenco?modalidade=campo` mostra a mensagem de "ainda sendo montado", `?modalidade=futsal` continua mostrando os 11, e a home continua normal.

- **`/contato` "Seja um parceiro do ACF" refeita com 2 cards; dados dos planos unificados no site inteiro; `/clube/patrocinadores` "Patrocinador Master" agora lê a tabela `sponsors` do banco**:
  - **Nova fonte única de dados**: `src/data/sponsorPlans.ts` exporta `sponsorPlans` (pontual/master — nome, `priceAmount`, `pricePeriod`, benefícios, mensagem de WhatsApp, `featured`) e `SPONSOR_WHATSAPP_NUMBER`. `src/components/SponsorPlanCard.tsx` renderiza um card a partir de um `SponsorPlan`. `PlansSection.tsx` (home), `SponsorsPageContent.tsx` (`/clube/patrocinadores`) e `ContactContent.tsx` (`/contato`) agora importam desse único arquivo em vez de cada um manter seu próprio array de planos.
  - **Preços unificados nos valores do mockup do Figma (R$199 pontual / R$299 master) nas três páginas** — a home mantinha R$180/R$500 de propósito (ver entrada "Home `planos.`" acima), e `/contato`/`/clube/patrocinadores` tinham um terceiro conjunto de valores; confirmado explicitamente com o usuário nesta sessão padronizar tudo pelos números do Figma em vez de manter 2-3 preços diferentes pro mesmo plano espalhados pelo site. Texto dos benefícios também unificado pelo texto do Figma (o master ganhou um 6º benefício, "link no site e logo na home", que não existia nas listas antigas de home/patrocinadores).
  - **`/contato` "Seja um parceiro do ACF"**: antes tinha 3 cards fixos próprios (pontual/master/mensal, card claro `#fdfdfd`, marcador circular vazado, uma única mensagem fixa de WhatsApp por card) num grid `grid-template-columns: repeat(3, ...)` — substituída pelo mesmo componente/visual de 2 cards (`SponsorPlanCard`) já usado em `/clube/patrocinadores` e na home (card escuro `#07243d`, marcadores com ícone de touro, `560px`/`524px` no featured), com confirmação explícita do usuário pra também tirar o plano "mensal" desta página. CSS renomeado de `.components-sponsors-page-content-plan*` pra um prefixo compartilhado `.components-sponsor-plan-card-*` (componente/arquivo próprio, seguindo a convenção de nomenclatura de CSS por caminho do projeto) e retirado das antigas regras `.partnerCard*` do `ContactContent`, que foram apagadas; `.partnerBand`/`.partnerIntro`/`.partnerGrid` (wrapper específico da página: título, parágrafo de intro, grid flex) foram mantidos e restilizados pra bater com o Figma (faixa sólida `var(--azul-preto)`, título uppercase). O mobile (`≤768px`) já empilhava em 1 coluna antes dessa mudança e continua empilhando, agora através da regra responsiva do próprio card compartilhado.
  - **"Patrocinador Master" em `/clube/patrocinadores` agora vem do banco**: novo `src/data/sponsors.ts` (`getMasterSponsors`, envolvido em `cache()` igual a `players.ts`) consulta `sponsors` filtrando `type = 'sponsor'` (o enum `type` da tabela `sponsors` é `sponsor`/`partner` — `sponsor` mapeia pra este grupo "Patrocinador Master", `partner` mapearia pro grupo "Parceiros" que continua desativado/fixo por flag, já que só "Patrocinador Master" estava no escopo pedido). Substituído o array fixo `masterLogos`; `SponsorsPageContent` agora é uma `async function` (mesmo padrão já usado em `NewsGrid`) renderizando a imagem real de cada linha do banco (envolvida num `<a href={sponsor.url}>` quando a linha tem link) em vez de um card só com texto. Adicionado CSS `.masterLogoCard`/`.masterLogoCard img` (`object-fit: contain` + padding, diferente do `object-fit: cover` dos outros cards de logo) já que logos reais enviados não podem ser cortados. O grupo não renderiza nada se a consulta retornar 0 linhas (consistente com o resto da página, que já esconde grupos vazios via `SHOW_PARTNERS`/`SHOW_ONE_OFFS`).
  - Verificado ao vivo via Claude in Chrome: `/contato` em desktop (1600px) e mobile (390px, cards empilhados pontual-depois-master) batendo com o frame do Figma; `/clube/patrocinadores` e a home mostrando o preço unificado R$199/R$299; as 2 imagens reais de patrocinadores do banco (tabela `sponsors`, `type='sponsor'`) só falharam ao carregar dentro desta sessão específica de navegador automatizado (`fetch`/`<img>` a partir do contexto da página deu `Failed to fetch`, enquanto `curl` e a navegação direta pra mesma URL carregaram a imagem normalmente) — não é um problema de código, mais provavelmente é o próprio sandboxing da extensão Claude in Chrome bloqueando um fetch de imagem cross-origin recém-embutido; vale conferir manualmente num navegador normal.
  - Build (`npm run build`) validado.

- **Alinhamento do "Perfil do Atleta"/"Elenco →", ordenação das tabelas de classificação (pontos + saldo de gols) e estilo do scroll das tabelas**:
  - **`/clube/elenco/[slug]` "Perfil do Atleta" / "Elenco →" fora da identação do resto do site**: `.components-roster-player-detail-content-toolbar` não tinha largura própria, então herdava o `1440px` inteiro do `.components-roster-player-detail-content-inner` — todo o resto do site (inclusive o `.profileCard` dessa mesma página, já em `width: min(1160px, 100%)`) usa a coluna de conteúdo padrão de `1160px`. Corrigido dando ao toolbar `width: min(1160px, 100%); margin: 0 auto 40px;`, o mesmo padrão que `.components-roster-roster-page-content-toolbar` (a listagem `/clube/elenco`) já usa pra se estreitar dentro do próprio `.inner` de `1440px`.
  - **Ordenação das tabelas de classificação estava errada** (`src/data/competitions.ts`): `sortByPosition` ordenava pelo valor `position` salvo no JSON `table` do banco (digitado manualmente por time), não pelo resultado real — um time com 3 pontos podia aparecer abaixo de dois times com 0 ponto se quem cadastrou os dados digitou o campo `position` fora de ordem. Substituído por `rankStandings`, que ordena por `points` decrescente, com empate desempatado por `goalDifference` decrescente, e sobrescreve `position` com o rank calculado (1, 2, 3...) — então o índice `01`/`02`/`03` mostrado (usado tanto por `StandingsPanelClient.tsx` na home quanto por `CompetitionsContent.tsx` em `/clube/competicoes`, ambos consumindo `getHomeCompetitions`/`getAllCompetitions` → o mesmo `mapRow` compartilhado) sempre vem calculado, nunca do cadastro manual do banco. O `isRelegated = posNum > 6` do `CompetitionsContent.tsx` continua funcionando sem alteração, já que agora lê o rank calculado corretamente.
  - **Scroll horizontal feio embaixo da tabela em `/clube/competicoes`**: `.components-competitions-content-tableWrap` (`overflow-x: auto`, necessário porque a tabela tem `min-width: 500px`) não tinha nenhum estilo de scrollbar, então caía na barra cinza/branca grossa padrão do navegador, destoando do card cinza-claro (`#e7e7e7`) ao redor. Adicionado um scroll fino temático (`scrollbar-width: thin` + `scrollbar-color` pro Firefox, `::-webkit-scrollbar*` pro Chrome/Safari) usando `var(--cinza-claro)` na barra sobre trilho transparente, `#777777` no hover — o mesmo tratamento foi aplicado em `.components-standings-panel-tableScroll` (o scroll vertical do widget da home, `overflow-y: auto`) por consistência, mesmo só o horizontal tendo sido reportado.
  - Verificado ao vivo via Claude in Chrome: `/clube/elenco/galiano` com o toolbar agora alinhado com o título do cabeçalho e o card de perfil; `/clube/competicoes` e o widget "tabelas." da home mostrando "Cyber Futsal/Arena..." (3 pts) como `01`, acima dos dois times com 0 ponto; o scroll horizontal da tabela de competições renderizando como uma barra cinza fina em vez da barra padrão do sistema.
  - Build (`npm run build`) validado.
  - Build (`npm run build`, `npx tsc --noEmit`) validado.

- **Fotos reais no elenco (home + `/clube/elenco`), fonte "News Cycle" nos números de camisa, e "Perfil do Atleta" com posições de campo/futsal**:
  - **Fotos reais dos jogadores**: `RosterPlayer`/`RosterPlayerCard`/`RosterStaffMember` (`src/data/players.ts`) ganharam o campo `image` (lido da coluna `image` das tabelas `players`/`staff_members`). O carrossel "elenco." da home (`RosterSection.tsx`/`page.tsx`) e os cards de `/clube/elenco` (`AthleteCard.tsx`) agora usam `athlete.image`/`person.image` como `src`, com fallback pro placeholder estático (`/squad/player.png`/`/squad/player-placeholder.png`) quando o banco não tem foto cadastrada para aquele jogador.
  - **Fonte "News Cycle"**: adicionada em `layout.tsx` (`next/font/google`, variável `--font-news-cycle`) e aplicada nos números de camisa — badge do card em `/clube/elenco` (`AthleteCard`), número no card do "Perfil do Atleta" e no card SVG do carrossel da home — no lugar da Montserrat usada antes; peso ajustado de `800` para `700` para bater com a fonte nova.
  - **"Perfil do Atleta" (`/clube/elenco/[slug]`) ganhou os campos "Posição Futebol" e "Posição Futsal"**: `PlayerDetailContent.tsx` agora renderiza `player.positionCampo`/`player.positionFutsal` (formatados via `positionLabelFor`, a mesma função já usada nos cards do elenco) como duas novas linhas da lista de informações, cada uma só aparecendo quando o dado existe.
  - **Reordenação a pedido do usuário**: "Data de Nascimento" passou a vir logo depois de "Nome" (antes vinha depois de "Modalidade") — ordem final: Nome, Data de Nascimento, Modalidade, Pé Dominante, Posição Futebol, Posição Futsal.
  - **Bug: ícones de redes sociais cortados/além da borda do card**: as 2 linhas novas aumentaram a altura da lista de informações, e o `.profileCard` usa `aspect-ratio` fixo + `overflow: hidden` (pra bater com a imagem de fundo decorativa) — então em telas menores a lista de ícones sociais passava a nascer *depois* do limite inferior do card, ficando invisível (cortada) ou visualmente colada na borda ilustrada. Confirmado com medições reais (`getBoundingClientRect()`) via Claude in Chrome em várias larguras (500 a 1440px): sem correção, o card cortava os ícones em mobile e estourava por até ~14px em torno de 850-900px. Corrigido compactando espaçamento/tipografia de `.profileList`/`.socialsList` (gap, margens, tamanho de fonte) em três pontos — regra base, o breakpoint `≤1200px` (que antes não tinha nenhuma compactação, o pior caso) e o `≤768px` — restaurando margem positiva (>15px) em todas as larguras testadas (500, 769, 800, 850, 900, 992, 1200, 1440px).
  - **Imagem "apelido" (`nick-name.png`) ganhou classe própria** (`.components-roster-player-detail-content-nickNameBadge`, tamanho fixo `244px` no desktop) e reduzida para `120px` no breakpoint mobile, a pedido do usuário, para liberar espaço vertical extra dentro do card.
  - **`/contato`**: o texto "É só chamar a gente pelo whatsapp." virou um `<h3>` (era `<p>`), sem o ponto final; o fundo da faixa "Seja um parceiro do ACF" (`.partnerBand`) passou a usar a cor sólida `#051829` em vez do token `var(--azul-preto)`.
  - Build (`npm run build`) validado.

- **Número do "Perfil do Atleta": ícone `#` posicionado ao lado do número e cor da borda ajustada conforme o Figma**:
  - O usuário já tinha adicionado `<img src="/squad/hash_tag.svg">` dentro do `<p>` do número (`PlayerDetailContent.tsx`), mas o ícone renderizava minúsculo/mal alinhado, sem estilo próprio. `.components-roster-player-detail-content-number` virou `inline-flex` (`align-items: center`, `gap: 0.06em`) e ganhou uma regra `.components-roster-player-detail-content-number img` dedicada, dimensionada em `em` (acompanha o `font-size` do número em cada breakpoint: 130px desktop, 64px `≤1200px`, 48px `≤768px`).
  - **Ajuste de tamanho do ícone**: a primeira tentativa dimensionava o ícone pela largura (`width: 0.56em`), o que resultava numa altura ~68% menor que a dos dígitos (medido via `canvas.measureText` no navegador: dígitos "24" a 130px têm `actualBoundingBoxAscent` ≈ 97.4px ≈ 0.75em). Comparando com a referência do Figma (print do usuário, onde o `#` tem praticamente a mesma altura dos números), trocado para dimensionar pela altura (`height: 0.74em; width: auto`), deixando o ícone com porte visual equivalente aos dígitos.
  - Cor da borda dos dígitos (`-webkit-text-stroke`) trocada de `rgba(245, 245, 245, 0.5)` para `#A6CFF5` (a pedido do usuário — coincide com a cor de preenchimento do próprio `hash_tag.svg`, deixando "#24" visualmente coeso).
  - Verificado ao vivo via Claude in Chrome em 1440px e 1100px (font-size 130px/64px); build (`npm run build`) validado.

- **Número do "Perfil do Atleta": revertido de imagem (`hash_tag.svg`) para o `#` feito por texto/CSS**:
  - O usuário achou o resultado do ícone `hash_tag.svg` (item anterior) grande/pesado demais comparado à referência do Figma. `PlayerDetailContent.tsx` voltou a renderizar `#{formatNumber(player.number)}` como texto simples dentro do `<p>` (em vez do `<img src="/squad/hash_tag.svg">` + texto), então o `#` usa a mesma fonte/tamanho/`-webkit-text-stroke` outline dos dígitos automaticamente, sem precisar de dimensionamento manual por breakpoint.
  - `globals.css`: removida a regra `.components-roster-player-detail-content-number img` (não usada mais) e o `display:inline-flex`/`gap` de `.components-roster-player-detail-content-number` (não necessário pra texto simples); a cor `#A6CFF5` do `-webkit-text-stroke` foi mantida.
  - Build (`npm run build`) validado.

- **"Perfil do Atleta": `#` menor que o número, ícones de rede social reposicionados, e correções de mobile (sobreposição foto/texto, imagem de apelido, número)**:
  - **`#` menor que os dígitos**: o `#` e o número dividiam a mesma tag `<p>`/classe, então ambos ficavam no mesmo `font-size` (130px), maiores que o esperado (referência do Figma mostra o `#` nitidamente menor que os dígitos). `PlayerDetailContent.tsx` agora envolve só o `#` num `<span className="components-roster-player-detail-content-hashtag">`; a nova classe usa `font-size: 0.5em` (metade do número, escalando junto em qualquer breakpoint) e `-webkit-text-stroke: 1.5px` (mais fino, proporcional ao tamanho menor).
  - **Ícones de rede social reposicionados**: antes ficavam dentro da lista de informações (`.identity`), presos logo abaixo do último campo de texto, com um vão grande de espaço vazio até a base do card — divergindo da referência do Figma, onde os ícones formam uma faixa própria perto da base do card, na mesma altura do número. `PlayerDetailContent.tsx`: o bloco `.socialsList` saiu de dentro de `.identity` e virou irmão direto de `.profileCard`; `globals.css`: `.socialsList` passou a `position: absolute` ancorado em `left: 8.3%; bottom: 16.8%` (desktop) — a mesma faixa inferior já usada por `.number` — em vez de `margin-top` relativo ao texto acima. Esse reposicionamento também resolve, como efeito colateral, um bug em que os ícones ficavam cortados/invisíveis em certas larguras (a lista de informações cresceu com os 2 novos campos de posição, empurrando os ícones para além do `overflow: hidden` do card).
  - **Mobile: sobreposição de texto atrás da foto do jogador**: a coluna de texto (`.identity`, `width: 60%`) e a caixa da foto (`.playerFigure`, `width: 56.7%`) se sobrepunham por ~30% da largura do card no breakpoint `≤768px` — inofensivo com a foto placeholder antiga (mais neutra), mas muito visível com a foto real (`jogador.jpg`, opaca) cobrindo texto como "Modalidade"/"Pé Dominante"/"Posição Futebol". Corrigido reduzindo `.identity` para `width: 44%` e `.playerFigure` para `width: 34%; right: 4%`, abrindo uma folga real de ~13px entre as duas colunas (verificado com `getBoundingClientRect()` via Claude in Chrome, inclusive com nomes longos que quebram em 2 linhas).
  - **Mobile: imagem "apelido" (`nick-name.png`) removida** — a pedido do usuário, `.components-roster-player-detail-content-identity > img { display: none; }` no breakpoint `≤768px` (mantida no desktop).
  - **Mobile: número mais para dentro** — `.number` no breakpoint `≤768px`: `right` de `8%` para `20%`, afastando o "#" + dígitos da borda direita do card.
  - **Nota sobre viewport de teste**: `.components-roster-player-detail-content-inner` usa `width: min(330px, calc(100% - 32px))`, então a partir de ~362px de viewport o card já trava em 330px — testar em qualquer largura ≥362px (ex.: 500px no Chrome desktop) reproduz fielmente o mesmo layout de um iPhone 15 (393px).

- **Card do elenco (`/clube/elenco`): número do estado hover/pressionado trocado de badge sólido para contorno, cores do nome/posição ajustadas ao design system**:
  - **Número (`.components-roster-athlete-card-label`)**: era um badge com fundo sólido `#484747` + texto `#f5f5f5` em `12px`. A pedido do usuário, trocado pelo mesmo tratamento visual já usado no número do "Perfil do Atleta" (`.components-roster-player-detail-content-number`) — `News Cycle`/`Montserrat` `700` itálico, `color: transparent` + `-webkit-text-stroke`, sem fundo/borda/padding — só que na cor `#F5F5F5` (o detalhe usa `#A6CFF5`). Tamanho fixado em `64px` para o número ocupar visualmente a mesma altura do bloco nome+posição, como na referência do Figma. `AthleteCard.tsx` ganhou um `formatNumber()` local (mesma lógica do `PlayerDetailContent.tsx`: preenche com zero à esquerda) já que o número deixou de ter um badge que disfarçava dígitos únicos.
  - **Nome (`.components-roster-athlete-card-title`)**: cor ajustada de `#f4f5f7` para `#F5F5F5` (token exato do design system); tipografia (Montserrat Medium 24/32) já batia com a referência, sem mudança.
  - **Posição (`.components-roster-athlete-card-text`)**: cor ajustada de `#a9b3c1` para `#777777` (token `cinza_mais_escuro` do design system); tipografia (Outfit Regular 20/28, tracking 1.5%) já batia com a referência, sem mudança.
  - **Desktop/mobile**: o card não tem breakpoints próprios — `.components-roster-roster-page-content-grid` mantém a coluna fixa em `256px` em qualquer largura (só o número de colunas muda), então a mudança se aplica igual nas duas telas; verificado ao vivo via Claude in Chrome em desktop (hover) e em 390px (mesmo card, mesma tipografia).
  - Build (`npm run build`) validado.

- **Card do elenco (`/clube/elenco`): overlay do hover/pressionado trocado de escurecer+desfocar a foto para tingir de laranja, sem desfoque**:
  - A pedido do usuário, `.components-roster-athlete-card-card::after` (o overlay que sobe atrás do nome/posição/número no hover) trocou o gradiente `rgba(2, 12, 20, ...)` (azul quase preto) por `rgba(255, 50, 3, ...)` — o mesmo laranja principal da aplicação (`--laranja: #ff3203`) — mantendo os mesmos estágios de opacidade (`0.94 → 0.6 → 0`) pra continuar dando contraste pro texto.
  - Removido o `backdrop-filter: blur(3px)` que era aplicado no hover/focus/active (e a respectiva transição) — a foto do jogador agora permanece nítida, só a cor do overlay muda.
  - Build (`npm run build`) validado; verificado ao vivo via Claude in Chrome.

- **Card do elenco (`/clube/elenco`): corrigida a foto vazando pra fora da moldura no hover (zoom `scale(1.06)`)**:
  - **Causa raiz**: `.components-roster-athlete-card-image` (a `<img>`) tinha o próprio `clip-path` (a forma octogonal) E o `transform: scale(1.06)` do zoom de hover na mesma tag. `clip-path` recorta a caixa do elemento antes do `transform` ser aplicado, então o `transform: scale` amplia a forma **já recortada** como uma unidade rígida — o octógono inteiro cresce 6% a partir do próprio centro, e as pontas/cantos passam a ultrapassar o recorte octogonal do `.card` pai por uma fresta de subpixel, revelando ali um pedaço da própria foto (fundo do estúdio, mais claro) por trás da moldura preta — visível como uma lasca cinza-clara no canto, mais perceptível agora que o overlay ficou laranja vibrante em vez de escuro.
  - **Correção**: a `<img>` passou a ficar dentro de um `<div className="components-roster-athlete-card-imageWrap">` (`AthleteCard.tsx`). O wrapper ganhou o `position: absolute`, `inset: var(--border-gap)`, o `clip-path` octogonal e `overflow: hidden` fixos (nunca são transformados); a `<img>` interna ficou só com `width/height: 100%; object-fit: cover` e o `transform: scale(1.06)` do hover — como o recorte agora vive num elemento estático, o zoom da imagem fica sempre contido dentro da forma, sem vazar pra moldura.
  - Verificado ao vivo via Claude in Chrome, com zoom digital no canto superior direito do card em hover (onde a lasca aparecia) — canto limpo, sem vazamento. Build (`npm run build`) validado.

- **Cards do elenco na home (seção "elenco.", `RosterSection.tsx`): nome/número redesenhados conforme o novo design system, borda do hover trocada e corrigido corte da moldura no topo em telas ≤1200px**:
  - **Nome (`.components-roster-section-athleteName`)**: fonte trocada de Montserrat 40px pra Outfit Medium `52.39px`/`line-height:57px`, `letter-spacing:8%` e um contorno sutil (`-webkit-text-stroke: 1.31px rgba(255,50,3,0.13)`, laranja `#FF3203` a 13% de opacidade) — conforme a especificação de tipografia do Figma. Posição ajustada de `top:20px; left:22px` pra `top:16px; left:16px` (X16/Y16 no Figma).
  - **Número (`.components-roster-section-athleteNumber`)**: fonte trocada de News Cycle/Montserrat Bold pra **Oleo Script** (nova fonte adicionada em `layout.tsx` via `next/font/google`, variável `--font-oleo-script`), Regular `40px`/`line-height:48px`, cor do texto pro `#020C14` (`azul_preto`, era `#01121f`). A caixa clara de fundo virou tamanho fixo `56×80px`, posicionada em `top:0; right:9px` (X191/Y0 no Figma, card de 256px de largura) em vez de `min-width:52px` com padding solto, e ganhou `border-radius: 0 0 0 16px` (só o canto inferior-esquerdo arredondado, "mixed" no Figma) em vez do `4px` uniforme.
  - **Cor da borda no hover/pressionado**: `.components-roster-section-athlete:hover .athleteStroke` trocou de branco (`#f5f5f5`) pra `#F5B4A6`, a pedido do usuário (a borda "de repouso", vermelha `#b83e25`, não mudou).
  - **Bug corrigido: moldura cortada no topo em ≤1200px**: causa raiz — `.components-roster-section-athletes` ganha `overflow-x: auto` nesse breakpoint, e pela regra do spec CSS ("se um eixo de overflow não é `visible` e o outro é, o outro vira `auto`"), isso força `overflow-y` a virar `auto` também, mesmo sem declarar. Os cards usam `transform: translateY(...)` negativo pro efeito de "escada" (`-16px`/`-32px` nas posições 2/3/4) e mais `-6px` extra no hover/active — na faixa ≤1200px (tablet) o deslocamento chega a `-38px`, e no mobile (≤768px, onde o "-16/-32" é resetado) sobra só o `-6px` do hover; em ambos os casos, o topo do card levantado ultrapassava a borda de padding do container e era cortado pelo `overflow:auto` recém-ativado. Corrigido dando um `padding-top: 40px` ao `.components-roster-section-athletes` dentro do `@media (max-width: 1200px)` — espaço suficiente pro pior caso (`-38px`) nas duas faixas de largura.
  - Verificado ao vivo via Claude in Chrome em 1440px (hover normal), 900px (tablet, card com `-38px` no hover) e 390px (mobile, hover/toque) — moldura completa em todos, sem corte. Build (`npm run build`) validado.

- **Cards do elenco na home: animação de hover/pressionado suavizada ("de dentro para fora"); Perfil do Atleta mobile: card esticado verticalmente pra separar melhor os componentes**:
  - **Animação "de dentro para fora"**: `.components-roster-section-athleteInfo` (o overlay com nome + número que aparece no hover) ganhou `transform: scale(0.8)` em repouso e `transform-origin: center`, animando pra `scale(1)` junto com o fade de opacidade no hover/`:active` — em vez de só um fade simples, o nome e o número agora "crescem" suavemente a partir do centro do card. Duração da transição aumentada de `320ms` pra `460ms` (nome+número e também o contorno laranja/salmão do card, `.athleteStroke`) pra ficar perceptivelmente mais suave, mantendo a curva `cubic-bezier(0.22,1,0.36,1)` já usada no resto do site.
  - **"Perfil do Atleta" mobile (`player-profile-mob.png`)**: o `aspect-ratio` do `.components-roster-player-detail-content-profileCard` no breakpoint `≤768px` aumentou de `381/607` (a proporção nativa exata da imagem, herdada 1:1 sem distorção) pra `381/720` — como o frame usa `object-fit:fill` e todo o conteúdo sobreposto (identidade, lista de infos, ícones sociais, foto do jogador, número) é posicionado em `%` relativos ao card, esticar a altura do card estica a imagem de fundo verticalmente e espalha proporcionalmente todos os elementos, sem precisar reposicionar nada individualmente. Corrige uma sobreposição real: antes o ícone do Instagram ficava colado no "#" do número e a foto do jogador cobria a última linha da lista ("Posição Futsal"); depois do ajuste, os componentes ficam bem separados. Desktop não foi tocado (o `aspect-ratio: 1461/861` do card em telas >768px permanece igual).
  - Verificado ao vivo via Claude in Chrome: animação de hover na home em 1440px; card do "Perfil do Atleta" em ~500px de largura (mobile) antes/depois do ajuste, e conferido que o desktop (1440px) do "Perfil do Atleta" segue idêntico. Build (`npm run build`) validado.

- **Fallback de foto por posição (goleiro) no "Perfil do Atleta" e nos cards da home; cor da borda do hover ajustada ao design system; estratégia de "toque revela, toque de novo navega" pros cards da home no mobile**:
  - **"Perfil do Atleta" (`PlayerDetailContent.tsx`)**: a foto do jogador (`.playerFigure`) passa a usar `player.image` (do banco) quando existe; na ausência dela, mostra `goalkeeper.png` se a posição (futsal ou campo) for "Goleiro", e `player-line.png` nos demais casos — antes o componente sempre mostrava um placeholder fixo, ignorando tanto a foto do banco quanto a posição do jogador.
  - **Cards da home (`RosterSection.tsx` / `page.tsx`)**: `HomeRosterAthlete` ganhou o campo `isGoalkeeper` (calculado em `page.tsx` via `positionLabelFor(player.positionFutsal, "futsal") === "Goleiro"`); o fallback de imagem do SVG (antes `/squad/player.png` fixo) agora escolhe entre `player-line.png` (padrão) e `goalkeeper.png` (goleiro), igual à lógica do "Perfil do Atleta".
  - **Cor da borda do hover/pressionado**: extração mais recente do design system (Figma) trouxe o token oficial da borda como `#FFBDAD` ("cor do acf sport/L-04", descrita como "muted backgrounds/borders") — trocado do `#F5B4A6` usado antes (tipografia do nome/número já batia com esse mesmo design system e não mudou: Outfit Medium 52.39/57 8% e Oleo Script Regular 40/48).
  - **Estratégia "toque revela, toque de novo navega" (mobile)**: como o card só mostra nome/número no `:hover`/`:active`, no touch esse estado passava rápido demais (ou nem aparecia) antes do `Link` navegar direto pros detalhes. Cada `AthleteCard` da home ganhou estado local `revealed` (`useState`) — o `onClick` verifica `window.matchMedia("(hover: none)").matches` (dispositivo sem hover real, não apenas "tela estreita") e, se for touch e o card ainda não estiver revelado, cancela a navegação (`preventDefault`) e ativa uma classe `athleteRevealed` (mesmo efeito visual do hover: nome, número e borda salmão aparecem com a mesma animação "de dentro pra fora"); um novo toque com o card já revelado deixa a navegação normal do `Link` acontecer. O estado se desfaz sozinho depois de 2,5s se o usuário não tocar de novo. Em desktop (`hover: none` falso) o comportamento não muda — primeiro clique já navega, como antes.
  - Verificado ao vivo via Claude in Chrome: `/clube/elenco/giurizatto` (goleiro sem foto no banco) mostrando `goalkeeper.png`; cards da home sem foto mostrando `player-line.png`/`goalkeeper.png` conforme a posição; e a estratégia de toque testada simulando `matchMedia("(hover: none)")` — primeiro toque revela sem navegar (`preventDefault` do próprio handler), segundo toque navega (`preventDefault` já é do Next.js fazendo a troca de rota client-side). Build (`npm run build`) validado.

- **Mesma estratégia "toque revela, toque de novo navega" aplicada aos cards de `/clube/elenco` (`AthleteCard.tsx`)**:
  - `AthleteCard.tsx` virou Client Component (`"use client"`) e ganhou a mesma lógica já usada nos cards da home: estado local `revealed`, `onClick` que checa `window.matchMedia("(hover: none)").matches` e, em touch com o card ainda não revelado, cancela a navegação e ativa a classe `components-roster-athlete-card-revealed`; um novo toque com o card já revelado deixa o `Link` navegar normalmente. Estado se desfaz sozinho depois de 2,5s sem novo toque. Só afeta os cards de jogador (que usam `Link`) — cards de comissão técnica/staff continuam sendo um `<article>` estático, sem link.
  - `globals.css`: a nova classe `.components-roster-athlete-card-card.components-roster-athlete-card-revealed` replica, em conjunto com os seletores `:hover`/`:focus-visible`/`:active` já existentes, todo o efeito visual do hover (elevação do card, zoom da foto, overlay laranja, nome/número/número aparecendo) — sem duplicar a lógica de easing/timing.
  - Verificado ao vivo via Claude in Chrome em `/clube/elenco`, simulando `matchMedia("(hover: none)")`: primeiro toque revela "Giurizatto"/24 sem sair da página; segundo toque navega pra `/clube/elenco/giurizatto`. Confirmado também que em desktop (`hover: none` falso) o clique único continua navegando direto, sem regressão. Build (`npm run build`) validado.

- **Cards do elenco na home (`RosterSection.tsx`): nome/número escaláveis com o tamanho real do card (corrige "muito grandes" e posicionamento), e overlay de degradê laranja→preto no hover/pressionado**:
  - **Causa raiz do tamanho/posição "errados"**: `.components-roster-section-athlete` renderiza em `width: min(256px, calc((100% - 160px) / 5))` — em telas comuns (ex.: 1366px) o card real fica bem abaixo de `256px`, mas `.athleteName`/`.athleteNumber` usavam valores fixos em `px` (tirados 1:1 do Figma, que usa um card de `256px`). O SVG do card (viewBox `256×448`) encolhe proporcionalmente com o container, só que o texto/caixa do número sobrepostos em HTML não — daí pareciam desproporcionalmente grandes e "fora do lugar" em relação à foto/moldura por trás.
  - **Correção**: `.components-roster-section-athlete` ganhou `container-type: size`; todos os valores de `.athleteName` (`top`, `left`, `font-size`, `line-height`, `-webkit-text-stroke`) e `.athleteNumber` (`right`, `width`, `height`, `border-radius`, `font-size`, `line-height`) foram convertidos de `px` fixo pra `cqw` (percentual do card de `256` unidades do Figma, ex. `16px → 6.25cqw`), então agora escalam junto com o tamanho real do card em qualquer largura — sem precisar de valores "chutados" pra cada breakpoint. O breakpoint mobile (`≤768px`), que já tinha `font-size` próprio (`24px`/`20px`) tunado à parte pro card compacto de toque, não foi alterado.
  - **Degradê no hover/pressionado**: cada card ganhou um `<path>` extra no SVG (`components-roster-section-athleteOverlay`), preenchido com um `<linearGradient>` (laranja `#ff3203` embaixo → `#020c14` em cima, tokens `--laranja`/`--azul-preto`), com a mesma forma octogonal do card. Fica em `opacity: 0` em repouso e sobe pra `opacity: 0.85` em `:hover`/`:active`/`.athleteRevealed` (mesma transição `460ms` já usada no resto do hover), sobrepondo a foto do jogador com o degradê a pedido do usuário.
  - Verificado ao vivo via Claude in Chrome em 1366px (card abaixo de 256px, onde o bug era visível), 1600px (card no tamanho cheio do Figma) e 390px (mobile, sem regressão) — nome/número proporcionais ao card e degradê aplicado no hover em todas as larguras. Build (`npm run build`) validado.

- **"Perfil do Atleta" mobile: foto do jogador subida um pouco pra parar de tapar o boi do card**:
  - A pedido do usuário, `.components-roster-player-detail-content-playerFigure` no breakpoint `≤768px` teve o `bottom` aumentado de `11.9%` pra `16%` (altura `52.1%` inalterada) — como a foto é ancorada em `object-position: bottom center` dentro dessa caixa, aumentar o `bottom` sobe a caixa inteira ~4,2% da altura do card, afastando o topo da foto (cabeça do jogador) do logo do boi (marca d'água do fundo `player-profile-mob.png`), sem alterar o tamanho da foto. Desktop (`top: 12.9%`) não foi tocado — o problema só foi reportado no card mobile.
  - Verificado ao vivo via Claude in Chrome em ~500px de largura (`/clube/elenco/bernardino`) — mais respiro entre a cabeça do jogador e o boi. Build (`npm run build`) validado.

- **"Perfil do Atleta" mobile: foto do jogador e badge "#número" subidos mais um pouco, corrigindo o número em cima do logo pequeno do boi no rodapé do card**:
  - A pedido do usuário ("agora o número está pegando em cima do boi"), identificado que o ajuste anterior só tinha movido `.playerFigure` (`bottom: 16%`), deixando `.components-roster-player-detail-content-number` (o badge "#16") no `bottom: 11.9%` original — como o card ficou mais "limpo" no meio depois da foto subir, ficou visível que o badge do número já encostava no logo pequeno "ACF NA VEIA" (boi + linha de batimento) no canto inferior direito do frame (`player-profile-mob.png`).
  - Ambos os elementos, no breakpoint `≤768px`, tiveram o `bottom` igualado e aumentado pra `20%` (`.playerFigure` de `16%→20%`, `.number` de `11.9%→20%`) — sobem juntos mantendo o alinhamento relativo entre foto e badge que já existia (originalmente os dois nasciam no mesmo `11.9%`), agora com respiro suficiente até o logo do rodapé.
  - Verificado ao vivo via Claude in Chrome (zoom no card em `/clube/elenco/bernardino`, ~500px) — badge "#16" claramente acima do logo "ACF NA VEIA", sem sobreposição. Build (`npm run build`) validado.

- **Rodapé: título "mídias sociais" quebrando em duas linhas ("MÍDIAS" / "SOCIAIS")**:
  - A pedido do usuário, `.components-site-footer-column h2, .components-site-footer-contact h2` ganhou `white-space: nowrap`. Causa raiz: a segunda coluna do rodapé (`.components-site-footer-columns`, `grid-template-columns: 176px 176px 400px`) tem só `176px` de largura fixa em desktop, e o título em uppercase (`MÍDIAS SOCIAIS`, Rubik 24px) não cabia numa linha só nessa largura, quebrando entre as duas palavras — as outras colunas ("Informações", "Contato") já cabiam por acaso e não quebravam. Com `nowrap`, o texto agora estende um pouco além da faixa de `176px` da coluna, ocupando parte do `gap` de `204px` ao lado (espaço livre, sem colidir com a coluna "Contato").
  - Verificado ao vivo via Claude in Chrome em 1440px (desktop, 3 colunas), 1000px (tablet, 2 colunas) e 390px (mobile, 1 coluna) — título numa linha só nas três larguras, sem sobreposição com as colunas vizinhas. Build (`npm run build`) validado.
  - Verificado ao vivo via Claude in Chrome em múltiplas larguras e jogadores (incluindo um sem nenhuma rede social cadastrada, caso em que a faixa de ícones corretamente não renderiza nada); build (`npm run build`) validado.

- **Novo: botão flutuante "incentivar sem plano" na home, levando para `/clube/patrocinadores`**:
  - Novo componente `SponsorFloatButton.tsx` (`Link` para `/clube/patrocinadores`): ícone `public/sponsor/bull-float-button.svg` (touro, 38×48 nativo) + texto em duas linhas "incentivar" / "sem plano", renderizado em `src/app/page.tsx`.
  - **Aparência** (conforme referência do Figma): fundo `#FF6A47`, `border-radius: 8px`, `padding: 8px 24px`, `gap: 8px` entre ícone e texto, altura `64px` (o ícone ocupa a altura útil inteira entre os paddings verticais: `64 - 8 - 8 = 48px`, batendo com a altura nativa do SVG). Texto em Montserrat 700 uppercase, duas linhas com `gap: 2px` entre elas.
  - **Posicionamento — corrigido para `position: fixed` de verdade**: a primeira versão posicionava o botão de forma absoluta sobre a costura entre a seção de notícias e a seção "planos." (só visível/"flutuando" naquele ponto específico da página). O usuário esclareceu que o pedido era um botão flutuante de verdade — visível o tempo todo, em qualquer ponto do scroll, como um FAB (floating action button). Trocado para `position: fixed; right: max(20px, calc((100% - 1440px) / 2)); bottom: 32px; z-index: 40;` (ajustado para `1070px`/`right:16px + bottom:16px` fixo nos breakpoints `≤1200px`/`≤768px`) — o botão agora fica sempre no canto inferior direito da viewport, do topo da home até o fim da página.
  - **Bug corrigido durante a implementação anterior (absoluta) — segunda linha do texto cortada**: naquela versão, o botão era filho da seção de notícias (envolvida por `ScrollReveal`, que aplica `opacity`/`transform` — ambos criam um novo *stacking context* em CSS), o que prendia seu `z-index` dentro desse contexto isolado e deixava a seção seguinte pintar por cima da metade inferior do botão. Esse problema desapareceu com a mudança para `position: fixed` (o componente `SponsorFloatButton` agora é renderizado direto em `page.tsx`, fora de qualquer `ScrollReveal`/wrapper, e `position:fixed` já pinta acima do fluxo normal do documento).
  - Verificado ao vivo via Claude in Chrome em 1600px (desktop) e 390px (mobile) — botão fixo no canto inferior direito, visível já no topo da home (sobre o hero) e mantendo a posição ao rolar por todas as seções; clique navegando para `/clube/patrocinadores` nas duas larguras. Build (`npm run build`) validado.

- **Página de patrocinadores: cabeçalho com nova copy**: `src/app/clube/patrocinadores/page.tsx` — o texto do cabeçalho mudou de "nossos parceiros" / "patrocinadores." para "faça parte do ACF" / "seja um parceiro." (preparação para a próxima refatoração dessa página).
- **Rodapé: formatação e flag `hiddenButton` no botão "voltar ao topo"**: `SiteFooter.tsx` recebeu reformatação (JSX multilinha) e o link `.components-site-footer-backTop` passou a ser renderizado condicionalmente por uma constante `hiddenButton` (atualmente `true`, então o botão continua aparecendo normalmente — nenhuma mudança visível ainda).

- **Página de patrocinadores: seção "Porque sua empresa deveria estar com o ACF?" refeita — nova intro, lista com 3 motivos, divider e bloco "quer apoiar sem plano" com botão de WhatsApp** (`SponsorsPageContent.tsx`):
  - **Intro (`.components-sponsors-page-content-intro`)**: novo bloco acima da lista, com etiqueta laranja uppercase "Patrocínio" (`.eyebrow`), título "Porque sua empresa deveria estar com o ACF?" e subtítulo "O esporte cresce quando equipes amadoras e parceiros, caminham juntos." — conforme referência do Figma (frame `PATROCÍNIO` + título + subtítulo, gap 8px).
  - **Lista de motivos**: os 3 itens da antiga lista (`Viabilidade Financeira e Estrutura`, `Fortalecimento da Identidade e Profissionalismo`, `Conexão com o Público`) foram substituídos pelo novo conteúdo do Figma — "1. Fortaleça nossa estrutura", "2. Fortaleça sua marca junto com o ACF" e "3. Conecte sua marca ao nosso público" (esse último agora com 3 parágrafos numerados `3.1`/`3.2`/`3.3` em vez de um só). `reasons` em `SponsorsPageContent.tsx` mudou de `{title, text}` para `{title, texts: string[]}` pra suportar múltiplos parágrafos por item; `.components-sponsors-page-content-reason p + p` ganhou `margin-top: 8px` pra dar respiro entre os parágrafos 3.1/3.2/3.3.
  - **Divider cinza (`.components-sponsors-page-content-divider`)**: novo `<hr>` de 2px, largura igual ao container de 1160px, cor `rgba(18,18,18,0.12)`, separando a lista de motivos do novo bloco "quer apoiar sem plano".
  - **Bloco "Quer apoiar o acf sem contratar um plano?" (`.components-sponsors-page-content-noPlan`)**: totalmente novo — título + subtítulo "Sem problemas. Você também pode contribuir de outras maneiras e fazer parte do crescimento do projeto.", um item "1. Apoie de outras formas" com o texto explicando a ajuda simbólica sem plano, e o botão **"quero apoiar o acf"** (`.components-sponsors-page-content-whatsappButton`, gradiente `linear-gradient(165deg, #ff7a5c 0%, #8f1d00 99%)`, mesmo padrão visual do botão de WhatsApp já usado em `ContactContent.tsx`).
  - **Botão abre WhatsApp**: o botão usa `href="https://wa.me/${SPONSOR_WHATSAPP_NUMBER}?text=..."` (reaproveitando `SPONSOR_WHATSAPP_NUMBER` já exportado de `src/data/sponsorPlans.ts`, o mesmo número usado nos cards de plano) com `target="_blank"`, mensagem pré-preenchida "Olá, gostaria de apoiar o ACF Sports mesmo sem contratar um plano de patrocínio." — verificado ao vivo que o clique abre `api.whatsapp.com/send` com o número e a mensagem corretos.
  - O restante da página ("Nossos Planos" e "Patrocinadores Master") não foi alterado — só a seção de motivos, ganhando a intro, o divider e o novo bloco antes dela.
  - Verificado ao vivo via Claude in Chrome em 1600px (desktop) e 500px (mobile) — layout, textos e botão de WhatsApp corretos nas duas larguras. Build (`npm run build`) validado.

- **Google Analytics 4 (GA4) adicionado — somente em produção**: `src/app/layout.tsx` carrega o `gtag.js` via `next/script` (`strategy="afterInteractive"`), envolto por `process.env.NODE_ENV === "production"`. Como o `next dev` roda com `NODE_ENV=development`, a tag não é injetada localmente — só em builds de produção (`npm run build && npm run start` ou no deploy), evitando poluir os dados do GA4 com acessos de desenvolvimento. Confirmado inspecionando o HTML gerado por `npm run build`.
  - **Measurement ID via env var**: o ID (`G-MY7R56K1T4`) não fica mais hardcoded no código — é lido de `process.env.GA_MEASUREMENT_ID` (sem prefixo `NEXT_PUBLIC_`, pois `layout.tsx` é um Server Component: o valor é interpolado no HTML/script no servidor, não precisa ir para o bundle client-side). Definido em `.env.local` (gitignorado, não commitado); `.env.example` documenta a variável na raiz do repo. O `.gitignore` ganhou uma exceção `!.env.example` para que esse arquivo de exemplo continue versionado apesar da regra geral `.env*`. Se a env var não estiver definida, a tag simplesmente não é renderizada (checagem `isProduction && GA_MEASUREMENT_ID`).

- **Limpeza de código morto (varredura de uso) — parte 1**: removida a função `positionGroupLabelFor` de `src/data/players.ts` (exportada, sem nenhum uso em todo o repo) e a dependência `lucide` do `package.json`/`yarn.lock`/`package-lock.json` (só `lucide-react` era importado em `src/`; o pacote `lucide` "puro" nunca era usado). `.gitignore` ganhou uma regra para `.DS_Store` (evita que novos arquivos desse tipo sejam versionados por engano).
  - **Pendente**: a varredura também identificou `public/.DS_Store` (já versionado, ainda não removido) e várias imagens estáticas em `public/` sem referência no código nem nas linhas do banco (Neon) que servem `players`/`news`/`sponsors` — candidatas a remoção (`public/carousel/*`, parte de `public/home-news/`, parte de `public/contact/`, `public/youtube-section/acf-sovereign.png`, `public/squad/jogador.jpg`/`hash_tag.svg`, `public/header/header.png`, `public/backgrounds/background-youtube.png`) — ficou para uma próxima passada.

- **Notícias: novo atributo `draft`, visível em stage mas não em produção**: `src/data/news.ts` — `NewsItem`/`NewsRow` ganharam o campo `draft: boolean` (lido da nova coluna `draft` da tabela `news` no Neon, default `false`). `getAllNews` filtra as notícias com `draft: true` quando `process.env.VERCEL_ENV === "production"`.
  - **Por que `VERCEL_ENV` e não `NODE_ENV`**: `next build` sempre define `NODE_ENV=production`, inclusive no deploy de preview de `stage` — então gatear por `NODE_ENV` esconderia os rascunhos em qualquer deploy, não só em produção. `VERCEL_ENV` (injetada automaticamente pelo Vercel) diferencia `"production"` (deploy de `main`) de `"preview"` (deploy de `stage`), permitindo o fluxo pretendido: publicar como rascunho, validar em `stage.acfsports.com.br`, e só liberar em produção depois de aprovado (trocando `draft` para `false` no banco).
  - Como `getLatestNews`/`getNewsById`/`getRelatedNews` chamam `getAllNews`, o filtro cobre todos os pontos de consumo (home, grid, arquivo e detalhe).
