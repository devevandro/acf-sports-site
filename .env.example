# Google Analytics 4 measurement ID (e.g. G-XXXXXXXXXX).
# Only loaded when NODE_ENV=production (see src/app/layout.tsx) — dev traffic never hits GA4.
GA_MEASUREMENT_ID=
DATABASE_URL=postgres://neondb_owner:
